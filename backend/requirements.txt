# Backend dependency manifest - Excel Clone (FastAPI / Python 3.9)
#
# Runtime: Python 3.9 (infrastructure/docker/Dockerfile.backend -> FROM python:3.9-slim).
# Resolved and installed together against CPython 3.9.25 with no resolver conflict.
#
# Load-bearing pins (relaxing any of these breaks the application, not merely changes it):
#   pydantic<2                 backend/app/core/config.py imports BaseSettings from pydantic,
#                              which is the Pydantic v1 API. v2 moved it to pydantic-settings,
#                              so a v2 resolution raises ImportError on config.py line 1.
#   bcrypt==4.0.1              passlib 1.7.4 fails against bcrypt 5.0.0
#                              ("module 'bcrypt' has no attribute '__about__'", then
#                              "password cannot be longer than 72 bytes"). security.py builds
#                              CryptContext at module scope, so an unpinned bcrypt breaks import.
#   python-jose>=3.4.0         CVE-2024-33663 (GHSA-6c5p-j8vq-pqhj, PYSEC-2024-232, CWE-327)
#                              affects < 3.4.0. 3.5.0 is above the fix boundary.
#   numpy==2.0.2               highest numpy release supporting Python 3.9.

# --- Web framework -----------------------------------------------------------
fastapi==0.125.0
starlette==0.49.3
uvicorn[standard]==0.39.0

# --- Database ----------------------------------------------------------------
SQLAlchemy==1.4.54
psycopg2-binary==2.9.12

# --- Configuration and validation --------------------------------------------
pydantic==1.10.26

# --- Authentication and cryptography -----------------------------------------
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
firebase-admin==7.5.0

# --- Rate limiting -----------------------------------------------------------
slowapi==0.1.10
limits==4.2

# --- Google Cloud clients ----------------------------------------------------
google-cloud-storage==3.9.0
google-cloud-firestore==2.27.0
google-cloud-secret-manager==2.28.0

# --- Background jobs ---------------------------------------------------------
celery==5.6.3
redis==7.0.1

# --- Calculation engine ------------------------------------------------------
numpy==2.0.2
pandas==2.3.3

# --- Test tooling ------------------------------------------------------------
pytest==8.4.2
pytest-cov==7.1.0
httpx==0.28.1
