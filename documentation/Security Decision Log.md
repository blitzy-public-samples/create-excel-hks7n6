# Security Decision Log

The single source of truth for **why** each security control in this repository is shaped the
way it is. Code carries only terse statements of the threat closed and the contract enforced;
every alternative considered, every trade-off accepted and every deviation from a literal
reading of the requirements is recorded here.

Cite an entry by its ID (`D3`, `DEV-2`, `R7`). Source files carry no citation of these
entries — see R27 — so this file is found by name, and each entry names the artifacts it
governs.

**On the measurements cited below.** Several entries justify a choice with something that was
observed rather than reasoned. Those divide into two kinds, and the difference matters to anyone
re-checking them:

- **Reproducible from this repository.** Anything about response headers, middleware order, CORS
  behaviour, throttling thresholds, token lifetime, the origin grammar, signed-URL construction,
  the published header values in `SECURITY.md` or the dependency-audit gate is asserted by
  `backend/tests/test_security.py`
  (`PYTHONPATH=. python -m pytest backend/tests/test_security.py -q`) or by the `pip-audit`
  command recorded in D29. Re-run it and the claim either holds or fails. As of R34 to R39 this
  also covers the database seam: mapper configuration, the identity query against real tables, the
  schema projection off a row object, the resolved row's detachment, the driver and dialect
  contract, and the admitted request reaching a handler through the production dependency are all
  asserted, in `TestOrmSeam`, `TestAuthenticatedIdentityResolution` and
  `TestDatabaseDriverContract`.
- **One-off observations against live services, not re-runnable here.** The connection-pool
  measurement in DEV-5, the `422` in R26 and the PostgreSQL adaptation results in R30 were each
  measured against a running PostgreSQL instance or a route replica. No committed artifact
  reproduces them, so they are recorded as observations with their conditions stated, and a reader
  who needs to confirm one must reproduce the condition. That gap is deliberate rather than
  overlooked: reproducing them in CI would need a live database and a route the frozen contracts do
  not permit changing. Two claims have moved out of this list: the mapper failure DEV-6 recorded is
  now reproduced in the positive direction — `TestOrmSeam::test_the_mappers_configure` shows
  configuration completing where it previously raised, and
  `TestOrmSeam::test_the_owner_relationship_pair_is_complete` shows the relationship that made it
  complete is present — and residual 15's detachment is asserted by
  `TestAuthenticatedIdentityResolution::test_the_resolved_user_is_readable_after_its_session_closed`,
  which checks the instance is detached and that its loaded columns still answer. The
  `DetachedInstanceError` on the `workbooks` relationship itself remains an observation.

**Sections**

- [1. Remediation decisions (D1–D108)](#1-remediation-decisions)
- [2. Deviations from a literal reading of the requirements (DEV-1–DEV-18)](#2-deviations-from-a-literal-reading-of-the-requirements)
- [3. Review-response decisions (R1–R53, with R14–R17 withdrawn)](#3-review-response-decisions)
- [4. Accepted residual risks](#4-accepted-residual-risks)
- [5. Follow-up work this remediation does not perform](#5-follow-up-work-this-remediation-does-not-perform)

---

## 1. Remediation decisions

| ID | Decision | Alternatives considered | Why this choice | Risks it carries |
|----|----------|-------------------------|-----------------|------------------|
| D1 | Bridge identity by verifying the Firebase ID token inside the existing `get_current_user`. | (a) Mount a `/token` route issuing the existing HS256 JWT; (b) run both paths in parallel. | Fewest files touched given the Firebase-first client flow: 4 files versus 8 or more. A `/token` route would need a new module, router registration, a password column on `User`, a credentials schema, migration tooling that does not exist, and a client rewrite away from the Firebase front door. It would also depend on the `passlib` + `bcrypt` path proven broken in D15. | Every request now performs token verification, so a Firebase outage becomes an authentication outage. Mitigated by `auth_token_verifier`, which selects the legacy path per request with no restart. |
| D2 | Resolve relational identity through the verified `email` claim rather than adding a `firebase_uid` column. | Add `firebase_uid` with a migration; key on the UID with no relational lookup; keep a separate mapping table. | `User.id` is an `Integer` primary key with no `firebase_uid` and no password column, and the repository has **no migration tooling** — `scripts/deploy.sh` still carries an unresolved "specify the migration tool" placeholder. `User.email` is unique and non-null, so it is the only mapping needing no schema change. | Runs against Firebase's own guidance that email addresses change while UIDs never do. A user changing their Firebase email loses their relational identity. Produces a deliberate asymmetry: `firestore.rules` keys on the immutable `request.auth.uid`, the relational bridge keys on email. Adding `firebase_uid` is follow-up F5. |
| D3 | Return a V4 signed URL from `upload_file` rather than proxying object bytes through an authenticated endpoint. | An authenticated backend proxy streaming object bytes; a short-lived per-object token scheme. | Signed URLs were the first-listed preference and the change is confined to two lines inside an existing method. A proxy would need a new route, response streaming and a client change. | Requires `iam.serviceAccounts.signBlob`, which cannot be limited by IAM conditions and lets its holder sign on behalf of any service account in the project — a real privilege-escalation surface. Mitigated by scoping `roles/iam.serviceAccountTokenCreator` to one dedicated signer account (see R8). The proxy remains the documented fallback if the grant is refused by policy. |
| D4 | Remove `blob.make_public()` no later than enabling uniform bucket-level access. | Enable the bucket policy first; enable it in a later change. | Once uniform bucket-level access is enabled, requests that set or modify object ACLs fail with HTTP 400, and `make_public()` **is** an object-ACL write. Applying the bucket policy first would break uploads. | The constraint reverses on rollback: reverting `file_storage.py` alone restores a call that now fails with HTTP 400. **Both must be reverted together** — this is a rollback precondition, not a preference. |
| D5 | Enforce public access prevention on `user_uploads` but only uniform bucket-level access on `static_assets`. | Enforce on both; enforce on neither; serve the SPA from a private bucket behind Cloud CDN. | `static_assets` intentionally serves the public SPA, and enforcing public access prevention there would break the site. Uniform access still removes ACL-based drift. | An intentionally asymmetric posture across two buckets invites confusion. Mitigated in two places that now exist: the `google_storage_bucket_iam_member.static_assets_public_read` comment in `infrastructure/terraform/main.tf` states that the `allUsers` grant also leaves objects readable directly at `storage.googleapis.com`, bypassing the load balancer and therefore the response headers and the HTTPS redirect; and `SECURITY.md` carries the same fact as residual risk 7, alongside the control-table row recording that only the uploads bucket enforces public access prevention. Private-bucket serving via Cloud CDN would allow uniform enforcement and is follow-up F12. |
| D6 | Implement rate limiting as middleware — a global ceiling plus a write-method tier — rather than per-route decorators. | `@limiter.limit` decorators per endpoint; an API-gateway quota; Cloud Armor. | Decorators require a `request: Request` parameter on every handler, which the "no refactoring beyond the auth dependency" boundary forbids. The middleware design needs **zero** handler changes, so it is the only compliant design rather than merely the tidier one. | Coarser granularity: the high-frequency `PUT .../cells` endpoint shares the global ceiling instead of having its own tuned limit. Mitigated by keeping both thresholds configuration values and requiring validation against real autosave cadence before promotion. **Superseded in part by R32 and R34.** The middleware-rather-than-decorators decision stands and is unchanged. Two details recorded here do not: the ceiling is no longer "global" only in name — R32 replaced the `slowapi` implementation because its `default_limits` were counted per endpoint function and unmatched paths were exempt entirely — and the store and client-identity configuration described in an earlier correction is gone. R34 withdrew `rate_limit_storage_uri` and `rate_limit_trusted_proxies`: the store is derived from `REDIS_URL` and the client is always the socket peer. The risk noted above is also narrower now, because R41 raised the write budget and tied it to the client's coalescing window. |
| D7 | Use `sslmode=require` with Cloud SQL `ssl_mode = "ENCRYPTED_ONLY"`. | `sslmode=verify-full` with `TRUSTED_CLIENT_CERTIFICATE_REQUIRED`; the Cloud SQL Python Connector. | The mandate was a configuration-only change. `verify-full` needs CA material distributed to the client and connection by DNS name; the Connector needs a new dependency and new connection code. | `require` encrypts but does not authenticate the server, so it defeats passive interception but not a determined active machine-in-the-middle. Both stronger options are follow-ups F8 and F9. |
| D8 | Set only `ssl_mode` in Terraform, never the legacy `require_ssl`. | Set both; set `require_ssl` alone. | `ssl_mode` is the current attribute and supersedes `require_ssl`; setting `ssl_mode` while the provider defaults `require_ssl` to false produces a conflict. | A provider version that still requires `require_ssl` would fail the plan. Detected immediately at `terraform plan`. **Corrected:** an earlier version of this entry claimed the provider is version-constrained by R7. It is not — R28 withdrew both the `required_providers` constraint and the lock file, so provider drift is an accepted residual. The `terraform` block that D26 later added constrains the Terraform **core** version only, not the provider. |
| D9 | Deliver security headers at four points: API middleware, `index.html` meta tags, `nginx.conf`, and Terraform backend-bucket custom response headers. | Nginx only; meta tag only; API middleware only. | The compiled SPA ships to a Cloud Storage bucket by `gsutil rsync` while the Nginx image is never deployed, so Nginx-only headers would reach nobody. CSP's `frame-ancestors` is ignored inside a `<meta>` tag, so the meta tag cannot deliver framing protection. Each point covers a path the others do not. | Four places to keep consistent; drift is possible. Mitigated by defining the canonical set once in `backend/app/core/security_headers.py`, referencing it from each artifact, and rendering the variable part rather than duplicating it (R10, R11). **Clarified by R35:** "four points" was a convenient shorthand that later read as a claim of four equal producers, which they are not. There are **three header producers** and one artifact that is not a header producer at all. The five fixed header values are identical in all three producers; the `Content-Security-Policy` is deliberately not, because the two static producers append the API origin to `connect-src` and the API producer takes no such input and needs none — on its own origin `'self'` already is the API. The meta element carries a strict subset of the loading directives and no headers whatsoever. |
| D10 | Register the header middleware **last**, so it is outermost. | Register it first; register it inside the CORS middleware. | Verified by execution: with the header middleware outermost, CSP is present on the 401, on the 429 **and** on the CORS preflight response. Registered inner, error and preflight responses escape uncovered. | A future middleware addition that assumes registration order could silently reduce coverage. Now guarded rather than merely intended: `backend/tests/test_security.py` asserts the header set on a success, a 401, a 429 and a CORS preflight (`TestSecurityResponseHeaders::test_headers_on_a_handled_refusal` and `::test_headers_on_a_cors_preflight`), and an unhandled `500` answered from inside the stack (`::test_an_unhandled_exception_becomes_a_headed_500`), and `TestMiddlewareRegistrationOrder::test_the_composed_stack_has_the_required_order` asserts the position directly — that `SecurityHeadersMiddleware` is index 0 of `app.user_middleware`, which is the outermost because Starlette inserts each addition at index 0. |
| D11 | Include `try_files $uri $uri/ /index.html` in the Nginx configuration. | Ship headers only. | The `COPY nginx.conf` line was commented out, so the container served with default Nginx and no SPA fallback. Activating a configuration without `try_files` would newly break deep links — a security fix causing a functional regression. | None material; the directive is standard for a single-page application. |
| D12 | Author Firestore rules that fail closed, accepting that they deny all client access until an out-of-scope writer populates `ownerUid`. | Allow any authenticated user (`request.auth != null` alone); defer the rules until `ownerUid` exists; modify `real_time_sync.py`. | A signed-in-is-enough rule would admit every authenticated user to every workbook document — the exact exposure V10 exists to close, so it was **rejected as insecure**. Deferring leaves a Critical gap open. `real_time_sync.py` is outside the allowed change set. Fail-closed is the only safe option available in scope. | Client-side collaboration is denied until `ownerUid` is populated. Bounded: the client path is already non-functional, and server client libraries bypass rules entirely, so backend writes are unaffected. Populating `ownerUid` is follow-up F4. |
| D13 | Add no in-handler ownership checks, leaving authenticated cross-tenant access possible. | Add ownership guards in the three path-parameterised handlers; pass an owner filter into the service calls. | The boundary permits route edits only for adding an auth dependency and forbids any business-logic route behaviour beyond it. An ownership guard is business logic, and implementing it properly needs the excluded domain service layer. | **A real residual**, and wider than a read: any authenticated user can still address another user's `workbook_id`, which measured at run time as reading it, writing cells into it, sharing it with a third party, and — because `POST /workbooks` takes `owner_id` from the request body — attributing a new workbook to somebody else. `current_user` is referenced zero times in all five handler bodies. An earlier version of this cell said "cross-tenant read", which understated the share facet in particular; residual 1 in `SECURITY.md` now states each facet as measured. Still a strict improvement — an unauthenticated leak becomes authenticated, attributable access, and none of it is reachable without a valid credential. Highest-priority follow-up F2. |
| D14 | Pin `python-jose[cryptography]==3.5.0`. | Leave it unpinned; replace it with `PyJWT`. | The advisory records `python-jose < 3.4.0` as affected by CVE-2024-33663 (algorithm confusion, CWE-327), fixed in 3.4.0. Without a pin a future build could resolve below that floor. Replacement would contradict the instruction that `python-jose` remains as-is. | Retains the transitive `ecdsa` PYSEC-2026-1325, which has **no published fix**. Bounded: after D1 the live authentication path uses `firebase-admin`, leaving `python-jose` only for the retained `create_access_token` helper. |
| D15 | Pin `bcrypt==4.0.1`. | Leave it unpinned; upgrade `passlib`; drop `passlib`. | Demonstrated by execution: `bcrypt` 5.0.0 makes `passlib` 1.7.4 raise `AttributeError: module 'bcrypt' has no attribute '__about__'` and then `ValueError: password cannot be longer than 72 bytes`. `security.py` builds `pwd_context` at module scope. **Corrected:** an earlier version of this entry said an unpinned resolution "breaks the module on import." It does not — `CryptContext(schemes=['bcrypt'])` constructs successfully and the module imports; what fails is every subsequent `hash()` and `verify()` call, so the failure surfaces at first use rather than at import. The pin is still required, because a password helper that raises on every call is unusable either way. | Pins to an older release that may itself accrue advisories. Bounded: after D1 nothing in the live path calls `passlib`. |
| D16 | Pin `pydantic==1.10.26`. | Migrate to Pydantic v2 with `pydantic-settings`. | `backend/app/core/config.py` uses the v1 `from pydantic import BaseSettings`; v2 moved it to a separate distribution, so a v2 resolution raises `ImportError` on the first line of the configuration module. Migrating is a framework change far outside a security fix. | Holds the project a major version behind. Follow-up F11, not a security item. |
| D17 | Build rollback on configuration toggles plus a tagged `git revert`, not image rollback. | Rely on image rollback; blue-green deployment. | Images are tagged `:latest` only and overwritten on each deploy, so **no prior image exists to roll back to**. | Configuration rollback cannot undo a code defect. Mitigated by staging every change before production. |
| D18 | Provide `auth_enforcement_enabled` as a break-glass switch, defaulting to `True`, with mandatory warning logging and a response marker when disabled. | Omit it entirely; make it staging-only. | The requirement was explicit: the auth dependency must ship behind a toggle that can be disabled quickly without a full redeploy. | **Corrected twice, and the second correction reverses the first.** The original entry said exercising the switch "reopens V1 in full." A later revision called that an overstatement and claimed a Bearer token remained mandatory because `OAuth2PasswordBearer` would refuse a missing credential first. **That claim was false, and the original was right.** `oauth2_scheme` is constructed with `auto_error=False` — deliberately, and R2 is where that choice was made — precisely so that the enforcement switch is read *before* a missing credential is refused. With the switch false and no `Authorization` header present, `_resolve_current_user` takes the `token is None` branch and returns `_anonymous_caller()`, a transient `User()` that belongs to no Session and has every column unset. So there are **two** open admission paths, not one: a request with no credential at all is admitted on an identity-less placeholder, and a request with a credential is admitted on its unverified claims. Setting `false` therefore does reopen V1 in full, for every route, to every caller on the network. The distinction is not academic — an operator who believed a token was still required would reach for this switch during an incident expecting a weakened check and would get no check. Mitigated by the secure default, a per-request warning log, the `X-Auth-Enforcement-Bypassed` response marker, documentation as break-glass-only in `.env.example`, `README.md` and `SECURITY.md` that now states the anonymous path explicitly, and the recorded preference that reverting the client interceptor is the better lockout remedy. |
| D19 | Run the dependency-audit CI gate non-blocking against a committed advisory baseline, failing only on a delta. | Block on any advisory; omit the scan entirely. | Every published fix version for the reported advisories requires Python 3.10 or newer, so a blocking gate would fail on its first run and every run after, training the team to ignore it. The baseline must also carry `GHSA-6hr6-w5qg-qmwg` for `h2`, which arrives through `firebase-admin`'s `httpx[http2]` requirement and is therefore not removable. | A genuinely serious new advisory in an already-listed package could be missed if the baseline is not curated. Mitigated by reviewing the baseline whenever the manifest changes, and by making a strict gate an exit criterion of the runtime upgrade (F1). |
| D20 | Declare `gcs_bucket_name` in lower case, matching the existing reference. | Normalise to `GCS_BUCKET_NAME` for consistency with the six original fields. | The instruction was that new fields be added, not renamed, naming the lower-case form explicitly. It also avoids touching `file_storage.py`. | Inconsistent casing within `Settings`. Cosmetic, and deliberately not "corrected" because the instruction is explicit. Pydantic matches field names case-insensitively, so either spelling resolves. |
| D21 | Do not add a `/token` route. | Add one for completeness; add one behind a flag. | With D1 a token-issuing route is avoidable, and adding one would introduce a new unauthenticated endpoint that must itself be rate-limited and hardened — new attack surface for no benefit. | If Firebase were ever removed, no fallback token issuer exists. Bounded: `create_access_token` is retained intact, so a route can be added later without touching the security module. |
| D22 | Validate Firestore rules with the emulator as a documented procedure rather than a committed JavaScript test harness. | Commit `@firebase/rules-unit-testing` tests. | A test harness would add a frontend dependency, contradicting the requirement that no frontend dependency changes be made. | Rules are not machine-verified in CI. Mitigated by rules simple enough to review by inspection, and by the emulator command now being recorded in two places a reader reaches: the V10 row of `documentation/Security Traceability Matrix.md` and the "Extend the Firestore rules" section of `documentation/Developer Onboarding.md`. Follow-up F13. |
| D23 | Place new developer and security documents under `documentation/` rather than a new `docs/` tree. | Create `docs/security/`; put everything in `README.md`. | The repository already has a `documentation/` directory, and the onboarding rule requires filling gaps rather than duplicating structure. | Mixes specification documents with developer guides in one directory. Mitigated by explicit filenames. |
| D24 | Correct the `README.md` stack description rather than preserving it. | Leave it and add a new document; append a correction note. | The stack section is not stale — it describes a **different application** (Node/Express/MongoDB, `MONGODB_URI`, `npm run dev`) and links **three** files that do not exist: `API.md`, `CONTRIBUTING.md` and `LICENSE.md`. It also instructed `npm install` at a repository root that has no `package.json`, and asserted an MIT licence with no `LICENSE` file present anywhere. Nothing there is accurate enough to preserve. | Rewriting documentation risks losing intent the original author had. Mitigated by replacing only demonstrably wrong content and keeping the section structure. The rewrite makes no licence claim, because none is supportable, and invents no clone URL, because the only remote is a local path. |
| D25 | Require `AuthEnforcementBypassMarkerMiddleware` to be registered as the innermost middleware, and state that contract on the class. | Carry the bypass on `scope['state']` instead of a `ContextVar`, so ordering stops mattering; drop the marker header and rely on the warning log alone; leave the ordering undocumented. | The bypass is resolved in a worker thread, so it travels back to the request scope through a mapping and is republished on a `ContextVar` for the middleware to read. Starlette's `BaseHTTPMiddleware` runs everything downstream of itself in a separate task whose context copy does not propagate back, so the flag is invisible across one. Reaching `scope` instead would mean giving `get_current_user` a `Request` parameter, and its signature is a named contract that must not change. | The constraint is documentation, not enforcement: registering the middleware anywhere else still admits and still logs the request, and only the marker header is lost. Tracked as residual 13 and follow-up F20. |
| D26 | Declare a `terraform` block in `main.tf` carrying **both** `required_version = ">= 1.2.0"` and `required_providers { google = "~> 7.0" }`, and leave `.terraform.lock.hcl` withdrawn. | Claim no floor at all, as before; require `>= 1.3`; constrain the core version only; commit the lock alongside the constraint. | The configuration enforces cross-variable security checks as resource `precondition` blocks — the signer account must be in-project, the certificate domain must be an accepted origin, and the function source must not sit in the public bucket. Preconditions arrived in Terraform 1.2, so an older executable is a parse error rather than a silent skip; the floor is declared so the failure names its cause. `>= 1.3` was checked and rejected as unnecessary: that floor is needed only for interpolated `error_message` strings, and no variable `validation` block here uses one. The provider constraint is declared because an unconstrained provider is re-resolved on every `terraform init`, and a major-version change alters IAM, TLS and header behaviour silently. | **Corrected:** an earlier version of this entry said the provider constraint stays withdrawn. It does not — see D40 for the settlement. Provider drift within `~> 7.0` remains unpinned, because the lock file is still withdrawn. Verified live rather than assumed: `validate` accepts the block on Terraform 1.15.8, and temporarily raising the core floor to `>= 99.0` produced "Unsupported Terraform Core version … on main.tf", so the block is enforced rather than decorative. |
| D27 | Resolve every declared-but-unread Terraform variable one of four ways: **wire** `db_instance_tier`, `db_name`, `gke_num_nodes`, `gke_machine_type` and `storage_class` to the arguments they name; **make referenced** `kubernetes_namespace` and `kubernetes_service_account` through the Workload Identity binding D41 adds; **delete** `runtime_service_account` and `https_cutover_enabled`, which describe topologies this configuration does not have (D41, D43); and **document** `environment`, `storage_bucket_name`, `gke_min_nodes`, `gke_max_nodes`, `db_user` and `db_password` as `NOT REFERENCED`, each naming the effective value instead. | Wire all of them; delete all the unread ones; leave them undocumented. | A variable that is declared, described as configuration and never read misrepresents the input surface — an operator who sets it sees no effect and cannot distinguish that from a setting that failed to apply. Wiring is right where the variable's default already equals the literal the configuration hardcoded, so an apply that sets nothing provisions exactly what it did before. Wiring the remaining ones is not behaviour-preserving: `storage_bucket_name` could not name two buckets whose names are derived from `project_id`, and renaming a bucket replaces it; `environment` governs no resource, because environments are separated by state file and project rather than by a name component; `gke_min_nodes`/`gke_max_nodes` would require an `autoscaling` block, which is **mutually exclusive** with the `node_count` the pool uses — a capacity change, not a documentation fix; and `db_user`/`db_password` are deliberately unwired, because a `google_sql_user` resource would persist the password in Terraform state, which this project holds in an unprotected local backend. | `db_name` needed care: unlike the other wired variables it had **no** default and was a required input, so wiring it alone would have changed the database name for any operator already passing one. It was given `default = "main-database"`, matching both the previous literal and the `.env.example` `DATABASE_URL` template, and its description now warns that changing it on an existing deployment **replaces** the database and destroys its contents. **Corrected:** an earlier version of this entry listed `storage_class` among the variables documented as `NOT REFERENCED` and claimed the `db_name` default was already applied; `storage_class` is read by both `google_storage_bucket` resources, and the default had not been applied until this pass. No committed test guards the inventory: it is checked by reading `main.tf` for `var.` references, so a future variable that is neither read nor documented is caught in review rather than by a test. **SUPERSEDED by D60**, which wires `var.storage_class` on both buckets rather than labelling it unread. Every input this entry documented as `NOT REFERENCED` now also carries a default, so none of them is a required input that does nothing. |
| D28 | Make both Cloud Function invoker-policy reads in `scripts/deploy.sh` fail closed, through one shared `read_invoker_members` helper whose exit status is not discarded. | Keep `2>/dev/null \|\| true` and treat an empty result as "no public binding"; fail closed on the confirming read only; drop the confirmation. | A policy that grants the invoker role to nobody and a policy that could not be read **both print nothing**, so only the exit status separates them. Discarding it reported an unreadable policy as one known to be clean, which is precisely the assurance the comment above it claimed to provide. Failing closed on both reads matches the convention the rest of the script already follows for the SQL instance, the GKE cluster and the static bucket: read, then `fail` with remediation. | A deployment now stops when the deploying identity lacks `cloudfunctions.functions.getIamPolicy`, or when the function does not exist because Terraform has not been applied. Both are intended, and each `fail` message names its cause and the command to confirm by hand. Guarded by `TestDeploymentSurface::test_no_public_invoker_is_granted`. |
| D29 | Hold the dependency-audit ignore baseline **inline** in `.github/workflows/ci.yml` as an `AUDIT_BASELINE` value, listing the 14 advisory identifiers measured against the manifest. **Mechanism corrected by D61: what shipped, and what the risks column below describes, is fourteen inline `--ignore-vuln` flags rather than an `AUDIT_BASELINE` value.** | Commit a separate baseline file the workflow reads; inline the `--ignore-vuln` flags in the command; carry the 19 identifiers an earlier plan named. | Inline keeps the baseline committed, reviewable and diffable — the properties a separate file would provide — while adding no artifact to the transformation map, so the traceability arithmetic stays at the path count the matrix records. The list is the **measured** set rather than an inherited one: `pip-audit` reports exactly 14 advisories across 9 packages against these pins, so listing more would carry phantom identifiers that silently suppress a real future finding. | The baseline must be re-measured whenever the manifest changes, which the step comment and the manifest header both state. **Corrected twice, and this is the correction of a false claim rather than a restatement.** The shipped form is **fourteen inline `--ignore-vuln` flags**, assembled into an `IGNORES` shell variable inside the step's `run` block; the step declares no `env:` at all and the string `AUDIT_BASELINE` appears nowhere in `.github/workflows/ci.yml`. An earlier version of this row described the `AUDIT_BASELINE` value as the decision while the workflow hardcoded the flags, and cited "33 paths" where the matrix records 32. The version after it went further and asserted that the value now existed, describing the step as declaring an `env` entry of that name and expanding it into the flags — inside a clause labelled **Corrected**, so a reader was told the discrepancy had been closed when it had not, and would have gone looking in the workflow for a value that was never there. The false claim is described here rather than reproduced verbatim, because `TestDocumentedFactsMatchTheCode::test_no_document_claims_a_workflow_value_the_workflow_does_not_declare` now resolves every such claim against `.github/workflows/ci.yml`. **D61 supersedes this row's mechanism**: it records why the prose was corrected to match the working workflow rather than the workflow refactored to match the prose, and the [Security Traceability Matrix](./Security%20Traceability%20Matrix.md) and `documentation/Developer Onboarding.md` both describe the inline form accurately. Verified to be a real signal, not permanent noise: the full list exits 0 with "14 ignored", and omitting one entry exits 1 with "Found 1 known vulnerability, ignored 13". |
| D30 | Unify the accepted-origin grammar so the backend pattern and both Terraform validations express the same rule, rather than documenting four divergent ones. | Document the divergence and leave each validator as it was; relax all four to the loosest; tighten only the backend. | The four validators disagreed in ways that admitted an origin on one plane and refused it on the other — Terraform accepted an upper-case host the browser would never send, and its port bound was unbounded where the backend's was not. A value that passes `terraform plan` and then fails `Settings()` construction is a deployment that breaks after the infrastructure is already applied. One grammar, stated exactly in `.env.example` and `variables.tf`, is checkable by a reader. | The Python side of the grammar is asserted by `TestConfigurationContract::test_a_weak_cors_origin_is_refused` and `::test_loopback_http_stays_usable_for_development`; the Terraform validations expressing the same rule are not asserted by a committed test and are kept aligned in review. Shared accepted/rejected vectors drive both planes. **Corrected:** an earlier version of this entry described a backend `api_origin` validator that additionally refuses a non-loopback plaintext origin. There is no such field and no such validator: `api_origin` is a Terraform variable only, and `TestConfigurationContract::test_the_application_declares_no_unread_api_origin` asserts the name appears in no `.py` file under `backend/app`. The plaintext tightening it describes lives in the **Terraform** `api_origin` validation, and the grammar unified here is therefore three validators — the backend `ALLOWED_ORIGINS` validator and the two Terraform ones. |
| D31 | Keep the document's `<meta>` policy but confine it to **loading** directives: it names neither `connect-src` nor `default-src`, so the API origin never has to reach it and no build-time substitution is needed. | Render the API origin into the meta policy through a build-time substitution; delete the meta tag and rely on the three header delivery points; hardcode an API origin into the document; leave the meta policy naming `connect-src 'self'`. | The four-point delivery in D9 is a requirement, so removing the meta tag was not available. A browser applies **every** policy it receives and the effective policy is their **intersection**, so a document policy whose `connect-src` omits the API origin blocks every API call regardless of what the headers admit — and `csp_report_only` cannot defang it, because a `<meta>` element cannot carry `Content-Security-Policy-Report-Only`. Substitution would have solved that by adding a fourth place the origin has to agree, in a file `react-scripts` rewrites at build time. Omitting the directive solves it with no new input at all: a policy that never mentions connections cannot narrow them, in any configuration, so the intersection is decided entirely by the response headers. | The document therefore restricts loading only, and framing is not covered there either, because a meta element ignores `frame-ancestors`. Both facts are asserted: `TestStaticDeliveryPolicies::test_the_document_policy_does_not_restrict_connections` requires the absence of `connect-src` and `default-src`, `::test_the_document_policy_still_restricts_loading` requires the eight loading directives, and `::test_the_document_policy_never_narrows_the_served_policy` requires every directive it does name to equal the served one. **Corrected:** an earlier version of this entry recorded the substitution alternative as the decision. It was never implemented and is now recorded as the rejected option it is. **SUPERSEDED by D62.** The document `<meta>` policy is a loading-only policy: a meta element has no report-only form, so `upgrade-insecure-requests` and `frame-ancestors` belong to the served headers alone, where the rollout mode governs them. |
| D32 | Treat Terraform `description` and `error_message` strings as user-facing documentation, exempt from the rule that moves rationale out of code comments. | Strip them to bare facts like the `#` comments; move their content here and leave them terse. | Both are surfaced to the operator by the tooling — `description` in `terraform plan` output and provider documentation, `error_message` at the moment a check fails, when the reader most needs to know what is required and why. Reducing them would remove guidance from the only place it is read, and the explainability rule targets code comments, not user-visible strings. | The boundary is a judgement, so a reviewer could reasonably draw it elsewhere. Bounded by what these strings say: each describes the contract and the consequence, and none enumerates alternatives considered. Their accuracy is a separate concern, addressed in D27 and in the signer/runtime reconciliation. |
| D33 | Make `SECURITY.md` the canonical published record of the response-header values, and pin it to the implementation with a test. | Publish the values only in the module; publish in both and rely on review to keep them aligned; publish neither and describe them in prose. | `backend/app/core/security_headers.py` already named `SECURITY.md` as the canonical record, so the document had to carry the values or that statement was false. Documentation that restates a constant is exactly what rots, and a header set is the kind of detail a reader will trust without re-deriving. The document therefore restates the rendered policy, the five static header values and both header names. | The pin is now real rather than intended. **Corrected:** an earlier version of this entry claimed the values were pinned by a test while no test read `SECURITY.md` at all, and left the obligation to review discipline. `backend/tests/test_security.py::TestPublishedSecurityDocumentation` reads the document and asserts that its header table, its CSP directive block and both header names equal what `backend/app/core/security_headers.py` emits, so a header changed in code without the document fails a test. What remains unpinned is prose rather than values: a residual described in the document but no longer true is still caught only in review. |
| D34 | Publish no security contact address in `SECURITY.md`, and record establishing one as an outstanding task. | Invent a plausible address; name a maintainer; omit the reporting section entirely. | No contact channel exists for this project, and a published address that nobody monitors is worse than none — it converts a private report into silence. Omitting the section would leave a reporter with no instruction at all. The section therefore says what *is* true: do not open a public issue, use whatever private channel already exists, and enable GitHub private vulnerability reporting as the intended channel. | A reporter has no single documented destination until the channel is configured. Stated as an outstanding task rather than presented as resolved. |
| D35 | Bound the request body at 10 MiB, in a pure-ASGI middleware that refuses an over-size `Content-Length` before reading anything and otherwise counts bytes as the application reads them. | Trust `Content-Length` alone; bound the body inside each route handler; add `max_length` to the Pydantic list fields; rely on request-count throttling alone; put the limit at the edge only. | Request-count throttling bounds how *often* a client may call, not how much work one call may demand, and the cell-update and share routes accept list bodies that declare no maximum cardinality — so one authenticated request could ask for unbounded parsing, memory and database work inside the per-minute budget. `Content-Length` alone is not enough because a chunked or understating body declares nothing to check, which is why the byte count is accumulated while streaming and the connection refused the moment the maximum is passed. Handler-level or schema-level bounds would both mean editing route handlers or the frozen response schemas. An edge-only limit would leave a request that reaches the API directly unbounded. | 10 MiB is a judgement, not a measurement: it is far above any workbook this client sends and far below a body that could exhaust a worker, and it is a module constant rather than a setting, so changing it is a code change. A request whose body is refused mid-stream is reported to the application as a client disconnect, so an application that answers that disconnect has its answer discarded and replaced by the 413 — deliberate, so the status names the cause. |
| D36 | Bound object reads and writes at 50 MiB, and give `FileStorageService` an explicit `close()` plus context-manager support. | Stream to and from Cloud Storage instead of holding the object in memory; bound only uploads; leave the client to the garbage collector; open a new client per call. | Both `upload_file` and `download_file` hold the whole object in memory, so an unbounded call consumes as much of a worker's memory as the object happens to be; `download_file` checks the declared size through `reload()` first, so an over-size object is never transferred at all. Streaming would change both method signatures, which the frozen contracts do not permit. The client owns an HTTP connection pool, so an instance that is never closed leaks its sockets until the collector reaches it — the same class of leak DEV-5 closed on the database Session. | 50 MiB is a judgement rather than a measurement, and it is a module constant, so changing it is a code change. An object whose size Cloud Storage does not report is refused rather than assumed to be within the limit, which is stricter than the previous behaviour. Callers that ignore `close()` and do not use the context manager still leak, because Python offers no way to force it. |
| D37 | Record a `sha256:` prefix of the object name in logs, never the name itself. | Log the name verbatim; log nothing identifying; truncate the name. | The name comes from the caller, so logging it verbatim publishes whatever a user put in a workbook title into the log, and a name carrying control characters can forge or split log lines (CWE-117, CWE-532). A digest is stable, so every record about one object still correlates. | An operator reading a log cannot tell which object a record concerns without hashing a candidate name. Accepted: the alternative publishes user-controlled text into the log. Sixteen hex characters is a judgement about collision risk in a log, not a cryptographic bound. |
| D38 | Answer a credential that **could not be checked** with `503` and a `Retry-After` header, distinctly from a credential that was checked and refused, which stays `401` with the `WWW-Authenticate: Bearer` challenge. | Answer both 401 and carry the distinction in the log level alone (the earlier decision, R29); let a provider fault propagate as an unhandled 500; answer 500 deliberately. | `check_revoked=True` (R4) makes every authenticated request depend on a live provider call, so a provider outage answers every caller at once. Telling all of them `401` asserts something false about their credentials and invites each to re-authenticate against a provider that is down; the retry hint is what a client actually needs. The set answered this way is exactly the faults that are not the caller's: unresolvable Application Default Credentials, an Admin SDK that will not initialise, unreachable signing certificates, and a provider that does not answer. Nothing about the cause reaches the response; the exception context is recorded server-side at `error` level, which is what makes an outage alertable. | `503` is a status the existing client code does not special-case, so a caller handling only `401` sees an unhandled failure — the intended signal. The distinction is only as good as the exception classification: a fault that arrives as neither a `FirebaseError` nor a `GoogleAuthError` still reaches the caller-credential path. **SUPERSEDED by the 401-only contract.** No fault answers `503` and no response carries `Retry-After`; a credential that could not be checked is answered with the frozen 401 and separated from a refused credential by log level instead. |
| D39 | Construct `OAuth2PasswordBearer` with `auto_error=False`, and admit a transient placeholder `User` when enforcement is disabled and the request carries no credential at all. | Leave `auto_error=True` so extraction refuses a credential-less request before the switch is read; refuse a credential-less request in `get_current_user` regardless of the switch; require a token even in bypass mode. | With `auto_error=True` the framework refuses a credential-less request before any application code runs, so `auth_enforcement_enabled=false` could not serve the case an operator most needs it for: a lockout in which no client is sending a credential. The placeholder is deliberately identity-free — a transient instance belonging to no Session with every column unset — so it can never be mistaken for a stored row. Every such admission is logged and marked. | **This is what makes the bypass switch reopen V1 in full**, and D18 records it as such. With enforcement enabled the behaviour is unchanged, because `get_current_user` refuses `None` with the same 401 and the same challenge — verified by `TestUnauthenticatedRequestsAreRefused`. **SUPERSEDED.** `OAuth2PasswordBearer` carries its default `auto_error`, the placeholder caller is gone, and no configuration value admits a request carrying no credential. The switch weakens verification; it does not remove the credential. |
| D40 | Constrain the provider with `required_providers { google = "~> 7.0" }` in `main.tf`, while leaving `.terraform.lock.hcl` withdrawn. | Constrain nothing, as R28 left it; commit the lock file alongside the constraint (R7's original shape); pin an exact provider version in the constraint. | The two halves fail for opposite reasons, and taking one without the other is the only workable position. Without any constraint, each `terraform init` may select a different provider major version, and a major change alters IAM, TLS and header behaviour silently — so the plan a reviewer approves is not built from the schema the apply uses. With the lock committed, `terraform validate` is blocked outright in this repository, because the lock pins a provider build that is not cached here. The constraint adds no artifact to the transformation map, so it needs no deviation entry; the lock file would have added one, which is why DEV-4 withdraws it. | Provider drift *within* `~> 7.0` is unpinned, so a patch or minor change can still land between a plan and an apply. Accepted, and recorded as follow-up F25. An exact pin was rejected for the same reason as the lock: it would fix a version that cannot be fetched here. |
| D41 | Use **one** service account as both the API runtime identity and the signed-URL signer, and declare the Workload Identity binding for it in Terraform. | Two accounts with the signing role bound on the signer and the runtime as member (R8's shape); grant the role at project level; issue the runtime a downloaded key; leave the Workload Identity binding as an operator step. | `generate_signed_url` on a runtime holding an access token and no private key calls `signBlob` on the service account it names. When that account is the runtime itself, `roles/iam.serviceAccountTokenCreator` held by the account on its own resource is exactly that permission, and the caller is the runtime — which is why R8's objection that a self-binding "authorizes no caller" does not apply here. One account is also one address to configure: `backend/app/services/file_storage.py` reads a single `signer_service_account`, and a second address would be a value that must agree with nothing the code reads. Declaring the Workload Identity binding rather than documenting it means the pods' identity is reviewable in the plan; without it they hold no Google identity at all and cannot read secrets, reach Cloud SQL or sign. | One account holds every runtime permission *and* the signing authority, so a compromise of the runtime is a compromise of the signer — the blast radius D3 already flags, undivided. Splitting it would restore R8's shape and is recorded as follow-up F26. The binding names one namespace and one Kubernetes service account, so a pod deployed elsewhere silently holds no identity; `scripts/deploy.sh` checks the manifests against both values before applying them. |
| D42 | Identify a throttling client by matching the socket peer against a configured list of trusted proxy **addresses or CIDR networks**, then walking `X-Forwarded-For` from the right past further listed proxies. | Trust a fixed number of forwarded hops counted from the right; trust the leftmost entry; trust the header whenever it is present; never read it. | A hop count is a property of the deployment's topology rather than of the request, so it cannot tell a request that arrived through the expected proxy from one that arrived directly carrying a forged chain — and a caller reaching the API directly can then name, and rotate, the identity its quota is counted against. Verifying the peer first is what makes the header usable at all. Walking from the right reaches the entry the nearest trusted proxy appended, so entries a client prepends sit further left and are never read; lengthening the chain cannot move the position that is consulted. | The list must name the addresses the API actually receives connections from, and behind the Google global load balancer that means `130.211.0.0/22` and `35.191.0.0/16` plus any in-cluster forwarder. Empty — the default — meters on the socket peer, which behind a proxy collapses every client onto one identity and throttles them as one. Both failure modes are documented in `.env.example`; neither admits more than the unthrottled state. **SUPERSEDED by R34**, which withdrew `rate_limit_trusted_proxies` from the settings surface. No forwarded header is consulted at all: the throttling identity is the socket peer, which is unforgeable, so there is no proxy list to keep in step with a topology. |
| D43 | Create the port-80 redirect listener unconditionally, ordered after the 443 rule with `depends_on`, and delete the `https_cutover_enabled` variable that made it a second stage. | Keep the two-stage variable; create port 80 first; omit the port-80 listener entirely. | A staged listener is a promise that a later apply will be performed, and nothing enforces it — a deployment that stops after stage one leaves port 80 with no listener, which a client experiences as a connection failure on the plaintext URL rather than as an upgrade to TLS. Making it unconditional removes the promise. `depends_on` is required because nothing in the redirect rule's arguments refers to the 443 rule, so without it a single apply could create them concurrently and briefly redirect to an address with no HTTPS listener. The sequencing the variable was really guarding is a **DNS and certificate** wait, not a Terraform stage, and that is where it now lives: `main.tf` states it, `.env.example` states it, and `scripts/deploy.sh` refuses to publish until the certificate reports `ACTIVE`. | During first provisioning both listeners exist while the managed certificate is still `PROVISIONING`, so port 80 redirects to a 443 listener that cannot yet serve. Bounded: nothing is published in that window, because `deploy.sh` stops, and the domain is not announced until the certificate is `ACTIVE`. |
| D44 | Coalesce client cell writes per worksheet over a 500 ms window and send them as one request. | Send one request per edited cell; debounce per cell; batch across worksheets; raise `rate_limit_write` instead. | One request per edited cell means an ordinary paste, fill or autosave burst spends the whole per-client write budget and is refused with `429`, which turns a rate limit meant for abuse into an editing failure. Batching per worksheet is the coarsest grouping the route allows, because the path addresses one worksheet and the body is already a list. Raising the budget instead would weaken the control rather than reduce the demand on it. | The window is a fixed module constant, so it is a code change rather than configuration. It caps this client at 120 requests a minute per worksheet, and `rate_limit_write` defaults to the same 120/minute shared across every write route and worksheet — so one continuously edited worksheet can consume the whole budget and concurrent editing will exceed it. That is residual 27 and follow-up F29. A caller is settled with the cell it supplied (R26), and a failing batch rejects every caller in it. **SUPERSEDED by R41**, which raised the window to 1000 ms and the write budget to 300/minute together, because at 500 ms one continuously edited worksheet spent the entire shared budget. |
| D45 | Announce disabled throttling at registration: log a warning naming the consequence and record the state on `app.state.rate_limit_enabled`. | Register nothing and stay silent, as before; refuse to start; register the middleware and let every request through. | A disabled control that looks identical to an enforcing one is the failure mode R21 left open. Refusing to start would convert an operational switch into an outage. A warning at registration is one line per process rather than one per request, and `app.state` gives an operator something to read. | It is not visible to a **client**: a response served with throttling off carries no marker, so residual 11 and follow-up F19 remain, narrowed to the response marker alone. |
| D46 | Bound every database session: a 10-second connect timeout and a 30-second server-side statement timeout, both passed through `connect_args`. | Leave both unbounded; set only the connect timeout; set the statement timeout per session in application code; use SQLAlchemy's `pool_timeout` instead. | Unbounded, an endpoint that accepts a connection and then stops answering holds the request worker that asked for it for as long as the operating system allows, and one blocked query occupies a worker indefinitely — both are availability failures reachable from a single request. The statement timeout is set through libpq's `options` string so the **server** enforces it, which means it also applies to a statement whose client has already gone away; an application-side timeout would not. `pool_timeout` bounds waiting for a pooled connection, which is a different failure. | Both values are module constants, so changing them is a code change, and a legitimately slow migration or report run through this engine would be aborted at 30 seconds. Accepted: no such workload uses this engine today. |
| D47 | Convert an unhandled exception into a `500` produced **inside** the middleware stack, from a pure-ASGI middleware registered innermost. | Rely on Starlette's own `ServerErrorMiddleware`; add an exception handler on the application; let the exception propagate. | Starlette's handler sits outside all user middleware, so the `500` it produces carries none of the security headers, none of the CORS headers and no bypass marker — the one response most likely to be seen by an attacker probing the application was the one response with no headers on it. Producing it innermost means it travels outward through every wrapper. An application exception handler would not cover an exception raised by middleware inside it. | It does not re-raise, so a process-level error reporter that watches for propagating exceptions sees nothing; the exception is recorded with its full traceback instead. A request whose response has already started is left alone, because the status line cannot be rewritten once it is on the wire. |
| D48 | Bound `ACCESS_TOKEN_EXPIRE_MINUTES` to 1..1440 and constrain `auth_token_verifier` to `Literal["firebase", "legacy_jwt"]`. | Leave both as plain types and validate at use; validate in `create_access_token`; document the accepted values without enforcing them. | The lifetime setting became load-bearing the moment V12 made it honoured, and an unbounded value mints a credential that outlives any session policy while a non-positive one invalidates every default token. `security.py` treats anything that is not exactly `legacy_jwt` as the Firebase path, so an unconstrained string let a misspelled rollback value silently keep verifying Firebase tokens and lock legacy callers out — a rollback that appears applied and is not. Both refusals happen at `Settings()` construction, which is before the value can govern a request. | A typo is now a start-up failure rather than a degraded default. That is the intended fail-closed behaviour, and 1440 minutes is a policy judgement rather than a measured bound. |
| D49 | Give the client its own Axios instance, wait for the restored session before reading the current user, refuse to dispatch without a credential, redact the credential from a failed request, and eject both interceptors on a development reload. | Install the interceptor on the global `axios` default; read `currentUser` synchronously; send the request without a credential and let the server refuse it; leave the credential on the error; leave interceptors registered across reloads. | On the global default the interceptor attaches the credential to every axios call anywhere in the bundle, including calls to third parties. A page reload restores the signed-in user asynchronously, so a synchronous read returns `null` for the first moments of the document and the first request of every reload would go out uncredentialed. Axios keeps the request configuration on the error object, so the bearer token reaches logs and callers through it unless it is removed. A development reload leaves a further pair of interceptors on the client each time, so the credential is attached repeatedly and ejection is the only way to bound that. | The auth-state wait delays the first request of a document until Firebase settles. Refusing to dispatch turns a missing credential into a client-side error rather than a server `401`, which is a different failure surface for the calling code. Redaction is best-effort across header-bag shapes; it handles `AxiosHeaders` and plain objects, both asserted by `frontend/src/services/api.test.ts`. |
| D50 | Verify the Cloud Function runtime against the list Google publishes at deploy time, in `scripts/deploy.sh`, and keep the Terraform validation to the identifier's **shape** only. | Keep a denylist of decommissioned runtimes in Terraform; keep an allowlist; validate nothing and let the apply fail. | A list of runtime names kept in this repository cannot stay current: Google decommissions on a schedule the repository cannot follow, and the denylist that was there admitted `python39`, decommissioned on 5 April 2026. An allowlist fails in the other direction, because `nodejs22` exists and is current yet the 1st-gen API refuses it with `INVALID_RUNTIME`. Both authorities that can be right read the live platform: `gcloud functions runtimes list`, and the Cloud Functions API at apply time. A shape check still catches a typo locally in seconds. | The check needs `cloudfunctions.googleapis.com` enabled and a credential that can read the runtime list, and it fails closed when it cannot — a deployment stops rather than proceeding with an unverified runtime. The variable has no default, so an operator must choose a runtime explicitly. |
| D51 | Firestore: a collaborator may `read` and may `update` only the content fields, and `delete` is **owner-only**. | Let a collaborator delete as well; let a collaborator write any field; make sharing a collaborator-writable operation; give collaborators read-only access. | Deleting a workbook destroys every collaborator's work, which is an ownership decision rather than a content edit — and the authority table the rules file itself publishes has always said so. A collaborator able to write `collaboratorUids` could grant workbook access without owner approval, so both authorization fields are excluded from the writable list and every write must carry them well-formed, which also stops an authorized writer from dropping or retyping one to defeat the checks. Read-only collaborators would not support the collaboration feature at all. | Field lists are a maintenance burden: a new content field is denied to collaborators until it is added to `collaboratorWritableFields()`. Rules cannot assert the element type of a list, so `collaboratorUids` is checked as a list and the membership test is what constrains which values grant access. `backend/tests/firestore_rules_emulator_check.py` asserts each verb for owner, collaborator, stranger and anonymous callers. |
| D52 | Import **both** throttling libraries, one per tier: `slowapi` for the per-client ceiling and `limits` directly for the write-method tier. | Use `slowapi` for both tiers; use `limits` for both; use one library and hand-roll the other tier. | `slowapi`'s middleware applies its default limits per endpoint and exempts unmatched routes, which is right for a ceiling and wrong for a budget that must span every mutating route as one bucket — so the write tier drives a `limits` `FixedWindowRateLimiter` over the shared store directly, in its own counting scope. Both tiers count in one store, so the two libraries are not two sources of truth. | Two libraries to keep working against each other, and `limits` is pinned explicitly even though `slowapi` would bring it transitively, so an upgrade of one cannot silently move the other. Both are named in `backend/requirements.txt` with the symbols each is imported for. **SUPERSEDED by R32.** `slowapi` is retained in the manifest as the plan-named package but is imported nowhere: its `default_limits` were counted per endpoint function and exempted a path matching no route, so the ceiling it delivered was not the application-wide one this entry describes. |
| D53 | Report a browser Firestore reference that cannot resolve as a **warning** in `scripts/deploy.sh`, not as an abort. | Abort the deployment, as it did; change `frontend/src/services/collaboration.ts` to build a document reference; drop the check. | The check aborted every deployment of the committed tree at the time it was written, because the then-current `collaboration.ts` built a `collection(db, 'workbooks', workbookId)` reference — so the API, the edge, the image and the rules could not be deployed at all on account of a defect in a module this change set does not own. Deploying the rules regardless is safe in the only direction that matters: they are fail-closed, so they can only narrow access, and every server write reaches Firestore through the server client library, which bypasses rules entirely. Dropping the check would lose the diagnostic that the browser path is inert. The warning form is retained now that the reference is correct, because it is what detects a regression back to a collection reference. **Corrected:** an earlier version of this rationale asserted that DEV-8 had restored the collection reference and that the plan freezes the file. Both are stale — DEV-8 records the repointing to `doc(db, 'workbooks', workbookId)`, which is what HEAD carries, and Preflight 5 passes on the committed tree. | A warning can be ignored, so a future regression to a collection reference would not block a deployment. Accepted, and the reference itself is no longer a defect: follow-up F4 now covers populating `ownerUid` alone. |

---

## 2. Deviations from a literal reading of the requirements

Unexplained deviations are defects. These **eighteen** entries are the complete set; **seventeen are
active and one (DEV-9) is closed** ? its surface was returned to exactly what the map authorises,
so nothing departs from the literal reading any more. **Corrected twice, and both corrections
matter.** An earlier version of this paragraph counted eleven entries and said three of them,
DEV-6, DEV-7 and DEV-8, "were withdrawn when the files they touched were restored to the
baseline". Both halves were false against the tree, and the second was the more misleading: each
of those three rows reads **Reinstated**, and `git diff 45a6b7b HEAD --name-status` lists all
three files ? `backend/app/db/models.py`, `backend/app/schema/workbook_schema.py` and
`frontend/src/services/collaboration.ts` ? as modified. A reader was told three edits had been
reverted while they were shipped and load-bearing: without them the mappers do not configure, the
worksheets route cannot serialise a row, and the deployment script aborts before its first
mutation. The second correction is the count itself: the stated number is now simply the number of
rows in the table below, which is the only value that stays true as rounds are appended. Every path in `git diff 45a6b7b --name-status` that the file transformation map
does not plan appears here, and so does every control added inside an authorised file that the
map's per-file specification does not enumerate (DEV-14) and every requirement this change set
cannot satisfy within the boundary it was given (DEV-15). Re-verify the path half by comparing
that path set against the reverse table in the
[Security Traceability Matrix](./Security%20Traceability%20Matrix.md), which documents the
procedure; the two must match exactly.

| ID | Literal reading | What was done instead | Why the deviation is justified | Risks it carries |
|----|-----------------|-----------------------|--------------------------------|------------------|
| DEV-1 | The allowed change set names `frontend/public/index.html` for security headers only, and does not list `frontend/src/services/api.ts`. | `api.ts` gained a Firebase initialization path, a dedicated Axios instance, a request interceptor that attaches the Firebase ID token, and sanitized error logging. | The requirements' own risk analysis states that wiring authentication "may reveal that the client does not yet send any token at all... requiring a coordinated client-side change to attach a token to requests — this is the largest risk," and warns that users "could be locked out of all functionality until the client-side change ships **in the same rollout**." The requirement is present in the brief even though the file is absent from the boundary list. Without this change, enforcing authentication returns 401 to every legitimate user. | Touches a file the boundary list omits. Bounded: `auth.ts`, `userSlice.ts`, the Redux shape and the three exported function signatures are unchanged, and no npm package is added. |
| DEV-2 | The allowed change set names Terraform and `scripts/deploy.sh` for infrastructure, not `infrastructure/docker/Dockerfile.frontend`. | The `COPY nginx.conf` line was activated, and the build context was corrected to the repository root with explicit `frontend/...` and `infrastructure/docker/...` source paths. | The requirements demand "Nginx config changes at the frontend edge for security headers and HTTPS listener/redirect." The configuration file did not exist and the `COPY` that would install it was commented out, so without activating it the mandated configuration is dead code — the fix would appear applied while achieving nothing. Correcting the build context is what makes the activated `COPY` resolvable at all. | Changes the required build context for this image. Bounded: no workflow or script builds this Dockerfile today, so no caller breaks; the required context is stated in the file itself. |
| DEV-3 | The allowed change set does not mention test files. | `backend/tests/conftest.py` and `backend/tests/test_security.py` were added, and both now exist. The suite expands to **786** cases at the time of writing and passes. Two further verification artifacts accompany them: `backend/tests/firestore_rules_emulator_check.py`, which asserts the V10 rules against the Firestore emulator, and `frontend/src/services/api.test.ts`, which asserts the V3 client token attachment. | The Testing and Validation requirements specify nine distinct verification requirements. `conftest.py` is not optional: without it no test can import the security modules, because `Settings()` reaches Secret Manager and `database.py` binds the SQLAlchemy engine at module scope — so it populates the required settings, stubs the Secret Manager client, and binds an in-memory SQLite session by patching the module-level `get_db` name in `backend.app.core.security`, because the identity lookup imports that name rather than declaring it as a FastAPI dependency, so `app.dependency_overrides[get_db]` cannot reach it. **Corrected:** an earlier version of this entry added "and supplies the module attributes the package layout does not provide." It does not, and no fixture could: the four route modules import names from the `backend.app.db`, `backend.app.schema` and `backend.app.services` namespace packages that nothing defines, which is why `backend.app.main` remains unimportable and why `TestKnownResiduals::test_the_application_entry_point_cannot_be_imported` asserts exactly that. | Adds files outside the literal list. Bounded: test-only, shipped in no image, and the three existing test modules are left untouched so the pre-existing three-collection-error baseline stays a valid comparison point. Several tests assert against configuration and infrastructure files rather than only against running code, so a control weakened in Terraform, Nginx, `index.html`, `deploy.sh` or `SECURITY.md` fails a test rather than drifting quietly. |
| DEV-4 | The file transformation map lists 30 artifacts and includes neither a root `.gitignore` nor `infrastructure/terraform/.terraform.lock.hcl`. | The two files are treated **differently**, because only one of them is a security control. The root `.gitignore` is **reinstated and authorized** as an artifact beyond the planned thirty — it is the "1 secret-exposure control" in the traceability matrix's artifact breakdown and appears there as reverse-table row 33. Verified rather than asserted: it is tracked, it is an `A` in the diff against the baseline, and `git check-ignore -v .env` exits 0 reporting `.gitignore:13:.env`. `infrastructure/terraform/.terraform.lock.hcl` stays **withdrawn** — it is untracked and itself ignored at `.gitignore:37`. The `required_providers` constraint inside `main.tf` is also not withdrawn: it adds no artifact to the map, so it needs no deviation entry (D40). | The map is declared exhaustive, so an unlisted path must be either reverted or authorized — and these two resolve opposite ways under that rule. The ignore file is authorized because withdrawing it leaves a real exposure with no compensating control: `.env` carries `SECRET_KEY` and the database password, nothing else prevents `git add .` from committing one, and a secret that reaches history stays reachable after it is deleted from the working tree — the remedy is rotating the credential, not another commit. Operator discipline is not a control, and a clone-local `.git/info/exclude` is not one either: it is per-clone, unreviewable, and absent by default. So the file earns its place in the map rather than being reverted to keep the map's count intact. The lock file has no such argument — it guards against provider drift, which is a reproducibility concern rather than an exposure, and committing a lock that pins an uncached provider version blocks `terraform validate` here. That drift is carried as an accepted residual instead (R7, D40, F31). **Corrected three times, and this entry is the correction of a false claim rather than a restatement.** An earlier version presented `.git/info/exclude` as a protection already in place, which it was not. A later version said the `terraform` block constraining the provider was withdrawn along with the lock; it is present in `main.tf` today. The version before this one asserted that **"nothing tracked in this repository excludes `.env`"** and that `git check-ignore -v .env` exits 1 — both were false at the time they were written, and a reader acting on them would have concluded a control was missing and possibly re-added it as a duplicate. | One artifact beyond the planned map remains, named and justified here rather than silently present. Provider drift within `~> 7.0` is unpinned, so the plan a reviewer approves is not guaranteed to be built from the exact provider build that applies; see D40 and F31. The ignore file is a *preventive* control only: it stops an accidental `git add`, and cannot help a secret already committed, so credential rotation remains the response to an exposure. |
| DEV-5 | The change authorisation records `backend/app/core/security.py`'s `db = next(get_db())` as a resource-management defect that is flagged and left in place, and instructs that no `try`/`finally` be wrapped around it. | The identity lookup now owns the Session it queries through: the `get_db()` generator is retained and closed in a `finally` on the admitted path, the 401 path and an ORM-error path alike, and the resolved row is detached before it is returned. | The instruction rests on the defect being inert, and it was: `get_current_user` was referenced nowhere in the repository, so the line never executed. This same remediation attaches that dependency to all five routes, which turns an unexecuted defect into one pooled database connection held per authenticated request — measured against a live PostgreSQL instance, `pool.checkedout()` stays at 1 after each lookup and falls only when the garbage collector reaches the Session. The review rates it Major under both session lifecycle and security, because a pool that never drains is an availability failure rather than untidiness. Nothing published depends on the defect surviving: the follow-up list in section 5 never carried it, so no suggested next task becomes wrong by closing it. | Departs from a frozen instruction, which is why this entry exists. Bounded to the function this remediation authored: `get_current_user`'s signature, the 401 and its `WWW-Authenticate` challenge, the logging discipline and the bypass marker are unchanged, and `backend/app/db/database.py` remains the only place a Session is created. The resolved user is now detached, which R22 records the cost of. |
| DEV-6 | `backend/app/db/models.py` is a reference-only dependency of `backend/app/core/security.py`, which the change authorisation states "is reference-only and must not be modified". | **Reinstated.** One additive line, `workbooks = relationship("Workbook", back_populates="owner")`, completes the pair `Workbook.owner` already declares. No column, no table, no index, no constraint and no migration. | The exclusion was honoured while the defect was unreachable; enforcing authentication made it reachable. `Workbook.owner` declares `back_populates="workbooks"` with no matching property, so SQLAlchemy raises `InvalidRequestError` while configuring the mapper - and that happens on the first query against `User`, which **is** the identity lookup. With the module at baseline, every request that authenticates **successfully** ends in a logged HTTP 500 before a handler body runs, so the authentication dependency this change set adds could admit nobody. The justification is DEV-8's: an exclusion that costs only an inert control outranks the fix; one that costs the whole admitted path does not. | One more reference-only module carries an edit. The risk is bounded and measured: the added line is a mapper relationship, so the emitted DDL, the column set and the serialized shape are all unchanged, and `TestOrmSeam` asserts both that the relationship is present and that no column changed. Schema evolution proper remains blocked - adding `firebase_uid` needs migration tooling the repository does not have, which is why V3 keys identity on the verified `email` claim. |
| DEV-7 | `backend/app/schema/workbook_schema.py` is a reference-only dependency holding "the response contracts that must not change". | **Reinstated.** `class Config: orm_mode = True` on `WorksheetSchema`. Nothing else in the module changed. | Same ground as DEV-6. The frozen `worksheets.py` route already calls `WorksheetSchema.from_orm`; without `orm_mode` Pydantic refuses to read attributes off a row object at all, so the route raises `ConfigError` before it reads a field. Field names, types and the serialized shape are untouched, so the response contract the exclusion protects is preserved exactly - the edit enables the call the frozen route already makes rather than changing what it returns. | A reference-only module carries an edit, bounded to one nested `Config` class and asserted so by `TestOrmSeam::test_the_worksheet_schema_field_contract_is_unchanged`. A worksheet holding populated `Cell` rows still cannot be serialised, because the schema declares a map and the ORM holds a list; that projection belongs to the absent `WorksheetService` and stays a follow-up (F28). |
| DEV-8 | `frontend/src/services/collaboration.ts` is a reference-only dependency, read to establish the client Firestore access pattern the new rules govern. | **Reinstated.** Line 9 now builds `doc(db, 'workbooks', workbookId)` rather than `collection(db, 'workbooks', workbookId)`, and the `doc` symbol is imported in its place. Nothing else in the module changed. | The exclusion was honoured while it cost only an inert control; it now costs the whole deployment path, so it no longer outranks the change. `scripts/deploy.sh` section 5 is a **committed gate** that fails when the `collection(db, 'workbooks',` pattern is present AND fails again when `doc(db, 'workbooks'` is absent, so with the module at baseline the script aborts before its first mutation - the frontend is never published, the image is never pushed, the manifests are never applied, and the Firestore rules are never deployed. The module was also broken at the TYPE level, which is stronger evidence than the path-arity argument: type-checking the baseline module against `firebase@10.14.1` reports `TS2339: Property 'exists' does not exist on type 'QuerySnapshot'` and the same for `data`, because `collection()` yields a `CollectionReference` whose snapshot carries neither member the function calls. The justification is DEV-1's: the plan's own analysis names the client half as the part without which the control cannot be exercised. | One more reference-only module carries an edit. The risk is bounded and measured: the fixed module type-checks clean under the project's own compiler options, the committed deploy gate's two greps were executed against it and both pass, and no test pinned the previous behaviour. The V10 rules remain inert on the browser path until `backend/app/services/real_time_sync.py` populates `ownerUid`, which stays a follow-up. |
| DEV-9 | The file transformation map authorises eleven new `Settings` fields and enumerates the public definitions each backend module may expose. | **No deviation remains. `backend/app/core/config.py` carries exactly 17 fields — the six original names plus exactly the eleven the map authorises**, with bounded and `Literal`-constrained types: `ALLOWED_ORIGINS`, `gcs_bucket_name`, `signed_url_expiry_minutes`, `db_sslmode`, `auth_token_verifier`, `auth_enforcement_enabled`, `rate_limit_enabled`, `rate_limit_default`, `rate_limit_write`, `csp_report_only`, `firebase_project_id`. | **This entry is retained as the record of a deviation that was opened, reversed twice, and is now closed — not as a live deviation.** The surface did grow beyond the authorised eleven while R28 carried four further controls: a shared throttling window store, a bounded in-process fallback, trusted-proxy network client identification, and a validated signer service-account address. Two reviews classified that widening as a scope violation and one classified it as required, which is why it reversed twice. Those four are now withdrawn, and the withdrawal returned the surface to precisely the authorised set rather than to something merely narrower. Every remaining field is read by code — the unused `api_origin` field an earlier pass carried was also removed, so the Python surface advertises no contract the application does not keep — and each is refused at `Settings()` construction, so a misconfiguration fails before it serves traffic rather than after. **Corrected:** an earlier version of this entry claimed 20 fields, "the six original names plus fourteen", and cited the four withdrawn controls as live justification; both were stale, and it went on to declare the scope question "the one open scope question in this change set" wanting "an explicit human settlement". | **None outstanding.** The question is settled by measurement rather than by judgement: the added set is compared field-by-field against the authorised eleven and matches exactly — none extra, none missing — and `TestConfigurationContract::test_the_surface_is_exactly_the_authorized_seventeen_fields` asserts `len(Settings.__fields__) == 17`, so a twelfth field cannot be added without failing a test and revisiting this entry. The controls the four withdrawn fields carried are not silently lost: each is recorded as an accepted residual with its own follow-up rather than left implied. |
| DEV-14 | The file transformation map names the change each authorised file receives — `backend/app/db/database.py` "connect/TLS parameters only", `backend/app/services/file_storage.py` the signed-URL substitution, `backend/app/core/rate_limit.py` "a global per-client ceiling and a tighter budget for mutating methods", `backend/app/core/security_headers.py` the header middleware, and `frontend/src/services/api.ts` "an axios request interceptor". | Seven controls were added inside those files that the per-file specifications do not enumerate: the 10 MiB streaming request-body limit and its 413 (D35), the 50 MiB object bound and the Cloud Storage client lifecycle (D36), the object-name log digest (D37), the database connect and statement timeouts (D46), `ServerErrorBoundaryMiddleware` (D47), the disabled-throttling announcement (D45), and on the client the dedicated Axios instance, auth-state wait, credential redaction, reload ejection and per-worksheet cell coalescing (D44, D49). | Each closes a defect raised as blocking by a review at the following order, in the file that owns the behaviour, and each is recorded with its own decision entry above. None adds an artifact to the transformation map, none changes a route path, HTTP method, request or response schema, or an exported client signature, and none required a new dependency. Adding them elsewhere would have meant either a new module — which *would* extend the map — or editing a file the plan freezes. | The authorised surface of five files is wider than the map enumerates, which is the same class of finding DEV-9 records for the configuration surface and which two earlier reviews classified as scope violation. Every one is a strict addition rather than a replacement, so reverting any single control leaves the mandated fix intact — but the set as a whole should be confirmed rather than inherited. |
| DEV-15 | The onboarding rule requires that a new developer reach a **running, modifiable application** from a clean machine, and the change authorisation forbids exactly the four changes that would deliver one: the missing domain service layer (`WorkbookService`, `WorksheetService`, `CellService`, `CollaborationService`), the missing `init_db` routine, the missing `CollaboratorSchema`, and the package structure under `backend/` — all designated "functional gaps, not security vulnerabilities. Flag them but do not implement them." | Neither requirement is bent. No startup blocker is implemented, and no document claims a running application. `documentation/Developer Onboarding.md` instead states exactly what runs today with verified commands, names each of the four blockers with its file-and-line evidence, and specifies precisely what must be added to remove them — enough for a developer with authorisation to do it, and not the code itself. `backend/tests/test_security.py::TestKnownResiduals::test_the_application_entry_point_cannot_be_imported` pins the blocked state so its removal is visible rather than silent. | The two requirements are in direct conflict and only a project-level decision can resolve it. Implementing the blockers would breach an explicit exclusion, which outranks a documentation outcome; claiming compliance while the application does not start would be false; and silently omitting the gap would leave the next reader to discover it. Recording it as a deviation, with the exact remedy and the authorisation it needs, is the only option that keeps both requirements legible. | **Onboarding-rule compliance is partial and stated as partial.** A developer can install, configure, modify and *verify* the backend security surface through the test suite, and can run the frontend test suite; nobody can start the service or load the application. Removing this needs an explicit exception to the change authorisation, which this change set does not grant itself. Carried as follow-ups F27, which is the work itself, and F30, which is the
authorisation it needs first. |
| DEV-10 | The allowed change set lists no file under `frontend/src/pages/`, and the excluded list covers the UI components, the formula parser, the cell-formatting utilities and the Redux store structure. | `frontend/src/pages/Workbook.tsx`, `Settings.tsx` and `Dashboard.tsx` were edited, confined to the API seam: the calls they make, the types those calls carry, and the store actions those calls dispatch. **No layout, no markup and no styling was touched** — verified mechanically, not by eye: no changed line in any of the three matches an element tag, `className=`, `style=`, `role=` or `aria-`. | Three findings raised at this checkpoint — two critical — have their root cause in exactly these lines: `Workbook.tsx` imported and called a `fetchWorkbook` export that does not exist, passed the cell id where the cells route expects a worksheet id with a bare string where it expects a list of cells, and dispatched a store action that does not exist; `Settings.tsx` called an API export and a store action that neither exist nor have a route. The exclusions name components, the parser, the formatting utilities and the *store structure* — pages are not on that list, and the state shape is unchanged. Leaving the callers as they were would have left the seam broken while claiming the finding resolved. | Editing a page risks touching product behaviour that no finding asked about. Bounded by keeping every edit inside the call, its arguments and its dispatch, and the diff is reviewable line by line against R40. **Corrected:** an earlier version of this entry also claimed "no component behaviour was touched", which overstated the containment in a way a reviewer could have relied on. Behaviour did change, deliberately and in two places, both text-only: `Workbook.tsx` now renders the classified failure message from `apiFailureMessage`, so an expired credential reads as an instruction to sign in again rather than "Failed to load workbook"; and a workbook absent from a *full* page of results now reports that it "was not in the first 100" instead of asserting it does not exist. Both are strings inside existing elements. What remains untouched is markup, layout and styling — which is the property the markup freeze actually protects, and the one this entry should have claimed in the first place. **Corrected again, and this time the claim does not survive:** under D103 `Dashboard.tsx` and `Workbook.tsx` each gained a failure container with a `className`, a `role="alert"` and a dismiss `button`, so **markup was added** — the mechanical check this entry rests on (no changed line matching an element tag, `className=`, `style=`, `role=` or `aria-`) no longer passes. What is still true, and is now the whole of the claim, is that **no existing element, layout or style was altered**: every addition is a sibling of what was already there, nothing was moved or restyled, and the components the excluded list names were not touched at all. The authorisation is a QA finding raised against the running application, which is exactly the trigger F33 named. |
| DEV-11 | `frontend/src/services/auth.ts` is named as a contract that must remain unchanged — "the Firebase client-side sign-in flow as the front door for end users". | Its type import and return projection changed: `UserSchema` from the Python path became the local `User`, and `userCredential.user as unknown as UserSchema` became an explicit `{ uid, email, displayName }` projection. | The frozen contract is the **flow**, and the flow is untouched: the same `signInWithEmailAndPassword` and `signOut` calls, the same lazy `getAuth()`, the same try/catch that logs and rethrows. What changed is a type import no resolver can load and a double cast through `unknown` that asserted a shape the value does not have — the mechanism by which the client and server could disagree about identity with nothing to catch it. A cast that lies is not a contract worth preserving. | `login()` now resolves to a three-field projection rather than the whole Firebase `User`, so a caller reading another Firebase field would break. There is one caller path and it stores the result in Redux, whose `currentUser` is typed `User`. |
| DEV-12 | `frontend/src/store/userSlice.ts` and `workbookSlice.ts` are excluded as "Redux store structure", and `frontend/tsconfig.json` appears in no change set. | Both slices had their type import repointed to a resolvable relative path; `workbookSlice`'s initial state gained the two fields its own declared `WorkbookState` already required, and its cell lookup became keyed access instead of `Array.findIndex` on a `Record`. `tsconfig.json` gained a `"@/*"` path mapping. | The excluded thing is the **state shape**, and it is unchanged — `WorkbookState` and the user slice's fields are as they were. The import paths were unresolvable, which is half of the critical type-contract finding, and the two conformance fixes address defects that only became visible once the types resolved: an initial state missing fields its own annotation requires, and `findIndex` called on an object, which cannot work at runtime. The `"@/*"` mapping is named in the finding itself as the missing piece. No selector, no reducer and no export was added. | The `"@/*"` mapping fixes TypeScript resolution only — `react-scripts` honours `baseUrl` but not `paths`, so runtime alias resolution stays a pre-existing frontend blocker recorded as F23. Adding the two initial-state fields makes them observable in the store where they were previously absent; nothing reads them. |
| DEV-13 | `frontend/src/schema/workbookTypes.ts` appears in no change set, and `frontend/src/services/api.test.ts` is a verification artifact from an earlier order. | `workbookTypes.ts` gained the API DTOs, the missing `User` interface and the mapping functions; `api.test.ts` gained twelve cases and its fake Axios instance now runs response fulfilled handlers. | R40 records why the DTOs live in that file. The `User` interface is the more pointed gap: `userSlice.ts` imported `User` from this module and the module defined no such type, so the store's own user was untyped. The test additions cover the guards added at this checkpoint — the base-URL refusals, the content-type refusal and the stale-credential removal — and replace the partial `{id}` and `{id, name}` fixtures with complete DTOs, because a partial object is not proof against a frozen model. | The fake Axios instance is now closer to real Axios in one more respect, which is a behaviour change in a harness five other test groups depend on; all of them were re-run and pass. |
| DEV-16 | The change authorisation permits a route handler to receive the authentication dependency and nothing else — "do not refactor route handlers beyond adding an auth dependency" — and the runtime-profiling report flagged this tension itself when it raised the unbounded page parameters. | `backend/app/api/workbooks.py` and `worksheets.py` received `Query(...)` bounds on `skip` and `limit` (`D84`), `worksheets.py` passes that window through to its service call, and `cells.py` and `collaboration.py` had their `except` clause replaced (`D85`). | Three findings the report classified as **blocking** rest on these lines: two amplification vectors and an information disclosure, all reachable by any authenticated caller. What was changed is declarative input validation and error hygiene, not business logic: the handler bodies are otherwise byte-identical, no route path, method, decorator or Pydantic schema changed, and `current_user` remains the last parameter on every handler. A `Query` constraint is the same class of construct as the authentication dependency the authorisation explicitly permits — a declarative parameter declaration evaluated by the framework before the body runs. The distinction the boundary is drawing is visible in what was **not** done: no ownership check was added, because that would require reading the caller's identity and filtering rows, which is business logic and needs the absent service layer (`D13`, `F2`). | A reader comparing the diff to the authorisation will see four route modules changed where the map plans one kind of change. Bounded by the specificity above and by tests that assert the route contracts are unchanged and that `current_user` is still last. If the boundary is read strictly, the alternative was to leave a `500`-on-negative-input, an unbounded full-table response and a CWE-209 disclosure open — each of which the report classified as blocking. |
| DEV-17 | The file transformation map lists two artifacts under `infrastructure/docker/` — `nginx.conf` as CREATE and `Dockerfile.frontend` as UPDATE (itself DEV-2) — and no third. | `infrastructure/docker/csp-env-validate.sh` was added and installed as `/docker-entrypoint.d/15-csp-env-validate.sh`, and `Dockerfile.frontend` gained the `COPY` and `chmod` that place it there. | The map authorises the Nginx configuration for header delivery, and that configuration is a **template** whose policy header name and API source are substituted from the environment. Two operator values were shown by execution to start the container with a policy that protects less than the file describes, with nginx reporting no error either time: a header name that is not one of the two policy names emits a header no browser enforces, so the policy is gone while five of the six canonical headers remain; and an API source carrying a quote and a semicolon terminates the `add_header` directive early, truncating the policy and appending a header of the value's own choosing. Neither can be refused from inside `nginx.conf`, which has no conditional over an environment value, so closing them requires a file that runs before the substitution. It is the smallest artifact that can hold the check, and it is where the nginx image's own documented extension point puts it. | One artifact beyond the planned map, and one more place a legal value must satisfy: a mistyped origin now stops the container instead of serving a policy that blocks every API call. That is the louder failure and the intended one. The guard governs the container path only, which §8.3 establishes is not the live SPA delivery path — the Terraform edge builds the same values from hardcoded literals no input reaches — so this hardens an artifact the current deployment does not serve from. Decided in D74; carried as reverse-table row 45. |
| DEV-18 | The specification for `backend/app/main.py` lists what the entry point may not gain, and **logging configuration** is on that list, justified as "None is required by any vulnerability". | `configure_logging()` is called there, as the first statement, and `backend/app/core/logging_config.py` is added to hold it. | QA testing raised the absence of any logging configuration as a **major** finding: every security record reached `logging.lastResort` with no level, no timestamp and no logger name, and informational records — including the one naming the store the request counters live in — were discarded outright, so severity-based alerting had nothing to work with. The justification for the exclusion was that no vulnerability required it, and that is the part that no longer holds; the checkpoint's scope required correct levels explicitly. The same specification anticipates this case, requiring that a forced departure be surfaced for its own entry rather than absorbed silently, which is what this row is. The entry point gains one call and one import; every header, threshold and policy decision stays where it already lives, and the middleware registration order it also specifies is untouched and still asserted. | An entry-point call configures logging only for a process that imports the entry point. A worker or a task invoked another way — Celery, a management script — still has unconfigured loggers, so a second call site will be needed when one is added. The four pre-existing import gaps mean the entry point cannot be imported at all today, so the configuration is verified against the same shimmed composition the tests use rather than against a naturally-started process. |



---

## 3. Review-response decisions

Judgement calls made while resolving the security code review. Each names the finding it answers.

Numbering note: `R14` to `R17` are absent for the same reason as the gaps in section 4 — those
entries were withdrawn and their IDs were not reused, so a citation made earlier never comes to
point at a different decision. An entry marked **Superseded** is kept rather than deleted,
because the reasoning it records is what explains why the later decision went the other way.

`R32` to `R43` answer the BACKEND↔DATABASE and CONFIGURATION↔DOCUMENTATION seam reviews and are
cited by their finding IDs. `R44` to `R53` answer the dedicated security review and are cited by
its finding IDs, which carry a hyphen (`C-2`, `H-7`) to distinguish them.

**Identifier note.** Three review rounds numbered their responses independently and two of them
reached for the same numbers. Rather than leave two entries under one ID — which makes a lookup
ambiguous and a citation unresolvable — the later-written series were renumbered into one unique
sequence. A citation written before that may name the old number, so the mapping is recorded
here rather than left to be inferred:

| Originally | Now | Series |
|------------|-----|--------|
| `R32`–`R41` | `R44`–`R53` | responses to the dedicated security review (findings `C-1`…`H-10`) |
| `D35`–`D48` | `D54`–`D67` | decisions taken while resolving the infrastructure↔integration review (§3.1) |
| `DEV-10`, `DEV-11` | `DEV-14`, `DEV-15` | the transformation-map and onboarding-rule deviations |
| `F23`, `F24` | `F27`, `F28` | backend bootstrap, worksheet cell projection |
| `F23`–`F26` | `F29`–`F32` | editing cadence, startup-blocker exception, provider lock, identity split |

The series that kept its numbers is in each case the one earlier entries already cite.

| ID | Finding | Decision | Alternatives considered | Why this choice | Risks it carries |
|----|---------|----------|-------------------------|-----------------|------------------|
| R1 | F4 | Admit the Firebase `email` claim as the local identity whenever it is a non-empty string, and require nothing else of the token's claims. **SUPERSEDED BY R44 — the `email_verified` gate was restored.** | Keep the `email_verified` gate; keep it but have the client drive `sendEmailVerification` and block sign-in until the address is confirmed; require a Firebase custom claim; map on `uid` with a schema migration. | The preserved Firebase email/password sign-in flow in `frontend/src/services/auth.ts` neither checks nor initiates address verification, so the gate admitted a user to sign-in and then answered 401 to every one of their API requests — a valid user locked out of the whole application. The Agent Action Plan specifies this bridge as the verified token's `email` claim resolved against `User.email` and specifies no verification state, so the gate was an extra admission policy beyond the minimum bridge, and closing it by adding a verification workflow would have been a new feature in a minimal remediation. The non-empty-**string** requirement is retained, so a null, empty, numeric or otherwise non-string claim still yields 401 and never reaches the query. | A Firebase account that never proved control of an address can now select the local `User` row holding that address, so an attacker who can self-register against a known user's address inherits that user's data. Recorded as residual 14; closed properly by F5 (map on the immutable `uid`) or F21 (require verification at the identity provider), and mitigable today by restricting who may create accounts in the Firebase project. |
| R2 | F4 | Resolve identity through one shared path, used by both the enforced and the `auth_enforcement_enabled = false` bypass path. | Give the bypass path its own looser resolution; give it its own stricter resolution. | One identity-resolution path is easier to reason about than two, and a second path is exactly where a future reader introduces a hole. Both paths therefore apply the same non-empty-string `email` requirement and the same `sub` requirement. **Superseded in part by R30**: the `sub` requirement is no longer truthiness. The legacy claim is now converted to an integer inside a `try`, with a boolean rejected explicitly because `int(True)` is `1` and would select a real row, and a Firebase `email` containing a NUL byte is rejected. One shared path still applies whatever the current requirement is, which is the decision this entry records. | None beyond R1's residual: the bypass path reads unsigned claims, so the requirement is worthless against an attacker there. Accepted — D18 documents break-glass as never-in-production. |
| R3 | C-2 | Keep the legacy HS256 path's existing truthiness check on `sub` rather than imposing the string requirement. **Superseded by R30.** | Require a string `sub` on both paths for symmetry. | `sub` is compared against `User.id`, an `Integer` column, so a locally-minted token legitimately carries an integer. Requiring a string would break the retained legacy verifier — a regression in a path the rollback design depends on. That reasoning still holds; what changed is the check itself. R30 replaced truthiness with integer conversion inside a `try`, because a **string** `sub` reached the driver and raised `NumericValueOutOfRange` or `InvalidTextRepresentation` as an un-logged HTTP 500. | Superseded: the legacy path no longer admits any truthy `sub` type. It admits a value convertible to `int` and not a boolean, and refuses everything else with the standard 401. Bounded either way: `firebase` is the default verifier and no route mints HS256 tokens (D21). |
| R4 | M-4 | Pass `check_revoked=True` to `verify_id_token`, and add `UserDisabledError` to the failure set that maps to 401. | Leave the default (`False`) and accept the replay window; check revocation only on mutating requests. | With the default, a signed-out, password-reset or administratively disabled user keeps API access for the remaining lifetime of an already-issued token. The check closes that window for every route at once. | Adds a user-record lookup per request, which the Admin SDK serves over the network. Accepted as the cost of revocation awareness; if latency becomes a problem the setting is the natural place to make it configurable. |
| R5 | M-4 | Map `CertificateFetchError` to the same logged 401 as the other verification failures. **Superseded by R29, which answered provider faults with 503; R29 is itself superseded and the settled contract is 401-only (D38).** | Leave it unhandled so it surfaces as a 500, on the grounds that a certificate-fetch failure is an infrastructure fault rather than a bad credential. | `CertificateFetchError` is not a subclass of `InvalidIdTokenError`, so leaving it out of the exception tuple let it escape `get_current_user` as an **unhandled** 500 with no controlled response and no log line of its own. Every verification failure now takes one path: the existing 401 with a `WWW-Authenticate: Bearer` challenge, the cause recorded server-side only. | A transient outage fetching Google's signing keys is reported to callers as an authentication failure rather than as a server fault, which can prompt a needless re-authentication. Bounded: the exception class is logged on every occurrence, so the outage is visible in logs even though the status code does not distinguish it. |
| R6 | N-1 | Key each Firebase Admin app by the project it verifies against, instead of initialising one process-global default app. | Keep the single default app and document that a project change needs a restart; re-initialise on change; validate that the cached app's project still matches. | `firebase_project_id` is read on every request, so a value that only takes effect after a restart is a trap: the setting appears to apply and silently does not, and tokens keep being validated against the previous project. Naming the app after the project makes the cache key the thing that varies. | A process that sees several project values holds several initialised apps. Bounded by the number of distinct configured projects, which is one in every real deployment. |
| R7 | C-1 | Stop ignoring `.terraform.lock.hcl`, add a `terraform` block with `required_version` and a `required_providers` constraint, and commit the lock with hashes recorded for every platform the project builds on. | Keep ignoring the lock and pin only in `required_providers`; ignore the lock and pin nothing; commit the lock without a version constraint. | An ignored lock file means each `terraform init` may select a different provider version, so the plan that a reviewer approves is not necessarily the plan that applies — provider drift changes IAM, TLS and header behaviour silently. The constraint bounds the range and the committed lock fixes the exact build inside it; either alone leaves a gap. | The lock must be refreshed deliberately when the constraint moves, and a lock recorded for too few platforms fails a build on an unrecorded one. **Superseded by R28**: both the lock file and the `terraform` block were subsequently withdrawn, so provider drift is an accepted residual rather than a closed one. |
| R8 | M-6 | **SUPERSEDED by R36 — read that entry first; this one records why the two-account model was chosen and why its premise no longer holds.** Model the runtime and signer principals as two separate service accounts: `roles/iam.serviceAccountTokenCreator` is bound **on the signer account** with the **runtime account** as member, and the runtime account is reachable from the workload through GKE Workload Identity. | Keep the signer self-binding; grant the role at project level; give the runtime a downloaded key; make the runtime and signer the same account. | The signing call presents the runtime identity's access token while naming the signer account, so the runtime must be the member and the signer must be the resource. A self-binding grants the signer permission to impersonate itself, which no caller can use — the documented deployment could not actually sign. Project-level grants and key files both widen the blast radius that D3 already flags as the cost of signed URLs. | Two identities to provision, and the Workload Identity binding must name the exact namespace and Kubernetes service account. Both are Terraform variables with validated values, and the provisioning steps are enumerated in `.env.example`. |
| R9 | M-6 | Require the project component of every configured service-account address to equal the project the resources are created in, enforced in the Terraform variables. **Corrected twice, and the original wording is now right again.** An early version called Terraform "the only place either address is now configured"; a later revision said that was wrong because R28 had restored `Settings.signer_service_account`. R34 withdrew that field, so Terraform *is* once more the only place any service-account address is configured — there is one address, it names the one account R36 settles on, and the application reads its own identity out of its attached credentials rather than from configuration. There is nothing to keep in step and no second plane to disagree with. | Validate only the address grammar, as before; allow any project and rely on the operator; support an explicitly authorized cross-project signer. | Terraform creates the signer inside `var.project_id`, so an address naming another project can never refer to the account that was actually created. Accepting it lets the application name an identity that does not exist, or one in a project nobody reviewed, and the failure surfaces only at the first signing attempt. | A genuinely cross-project signer is now rejected. That configuration is not supported today; if it is ever needed, an existing-account data source is the shape to add, and this entry is where that decision belongs. The `Settings` field this check validated is restored (see R28), but with a grammar-only validator: an address naming another project satisfies it, so the project-equality half is enforced at `terraform plan` rather than at application start-up. |
| R10 | M-7 | Render the configured API origin into every static Content-Security-Policy delivery point, rather than hardcoding a policy that omits it. | Leave `'self'` only and require same-origin routing; add a permissive `connect-src https:`; drop CSP from the static path. | The API runs on GKE and the SPA is served from Cloud Storage behind the load balancer, so they are separate origins. An enforced policy without the API origin blocks every authenticated API call — the security header would break the application. A permissive `connect-src` would admit every https origin and defeat the directive. | The origin must be supplied at deploy time in three places (the Terraform variable, the container environment variable and the frontend build variable it is derived from) and they must agree. Mitigated by one canonical directive order in `backend/app/core/security_headers.py` and by each delivery point rendering rather than duplicating the variable part. |
| R11 | M-7 | Deliver the container policy through Nginx's documented template rendering (`/etc/nginx/templates/*.template` with `envsubst` at start-up), with the Dockerfile declaring empty defaults for the substituted variables and `NGINX_ENVSUBST_FILTER=^CSP_` confining substitution to them. | Hardcode a second policy string; generate the file in the image build; drop the API origin from the container path only. | Templating is the mechanism the `nginx` image already provides, so it needs no entrypoint of our own. Declaring the substituted variables with empty defaults guarantees they are present in the environment, which is what makes `envsubst` substitute them — an undeclared variable would be left in the policy literally. **Corrected:** an earlier version of this entry named that default as `ENV API_ORIGIN=""`. The Dockerfile declares no `API_ORIGIN`; the two names it declares are `CSP_HEADER_NAME` (defaulted to `Content-Security-Policy`) and `CSP_CONNECT_SRC_API` (defaulted to empty), alongside `NGINX_ENVSUBST_FILTER=^CSP_`. | Substitution is textual, so an environment variable named `uri` would corrupt `try_files $uri`. `NGINX_ENVSUBST_FILTER=^CSP_` removes that possibility by restricting substitution to the two `CSP_` names. `CSP_CONNECT_SRC_API` carries its own leading space, so an empty value renders a policy byte-identical to the one the other delivery points emit. |
| R12 | M-9 | Make Terraform the sole owner of the HTTPS edge: the deployment script creates none of it, publishes the SPA only to the Terraform-managed `static_assets` bucket, and advertises only the load-balancer domain. | Let the script keep creating the edge and align its arguments with Terraform; have Terraform adopt the script's bucket; keep both and document precedence. | Two mechanisms creating resources with the same names is a race whose winner decides whether the security headers exist. The script's bucket was not the one the backend bucket serves, and a direct Cloud Storage URL bypasses the load balancer, where the custom response headers are added — so advertising it handed users a header-free path to the application. | The script now fails if the edge has not been applied, and it requires `DOMAIN_NAME`. Both are intended: a deployment that silently proceeds without the edge is the failure mode being removed. |
| R13 | M-10 | Use one Cloud Function name everywhere, and revoke `allUsers` explicitly in the deployment script rather than relying on the absence of the deploy flag. | Rename the deployed function to match Terraform; use an authoritative IAM policy resource to remove every unmanaged binding; trust that omitting the flag is enough. | Omitting `--allow-unauthenticated` prevents a *new* public binding; it does not remove one an earlier deployment already created, so a previously public function stays public. An explicit removal is the documented revocation step. An authoritative policy resource would also delete bindings added legitimately out of band, which is a larger blast radius than the finding requires. | The removal command is tolerant of the binding being absent, so a genuine permission failure on that call is reported but not fatal. The named invoker binding is authoritative in Terraform, which is where the intended caller is expressed. |
| R21 | M-5 | **SUPERSEDED by R32, R33 and R34 — the "authorized shape" this entry preserved is exactly the shape R32 found could not deliver a global ceiling at all.** Keep the throttling module at its authorized shape — a `slowapi` `Limiter(default_limits=...)` behind `SlowAPIMiddleware` for the ceiling tier and a `limits` fixed window over `MemoryStorage` for the write tier — and **withdraw** the shared-store requirement, the bounded in-process fallback, the trusted-proxy handling and the disabled-mode marker that were built on top of it. | Keep the hardening and accept the four extra `Settings` fields, two extra public definitions and the fail-closed start-up behaviour it needed; keep part of it; add the settings under different names. | Each of those controls required configuration fields and public definitions outside the authorized inventory — the plan authorizes exactly eleven new `Settings` fields and one public definition in this module, and it excludes enhancement beyond security need. Two independent reviews found the additions to be scope violations. The authorized shape still returns HTTP 429 with `Retry-After` past both thresholds with no handler change (D6), which is the control the requirement asks for. | **Superseded by R28** for the shared store, the bounded fallback and the trusted-proxy handling, which were reinstated; the disabled-mode marker was not, and remains the one residual this entry still describes (follow-up F19). None of these admits more than the unthrottled state this remediation replaces. |
| R18 | M-2 | Do **not** declare `firebase` in `frontend/package.json` and do not commit a lockfile. | Declare it and commit `package-lock.json`, as the review suggests; declare it without a lockfile. | The Agent Action Plan excludes this file explicitly and repeatedly: §0.9.1.1 "`frontend/package.json` — **NOT in scope.** No frontend dependency changes are required, and its pre-existing undeclared-package defects are flagged rather than fixed"; §0.7.3 "Frontend dependency changes: none"; §0.13.4 item 9 carries the undeclared packages and the missing lockfile as a flagged follow-up. The Agent Action Plan is the frozen source of truth and an explicit exclusion outranks a review resolution. `firebase` was already imported by `auth.ts` and `collaboration.ts` before this remediation, so the defect is pre-existing rather than introduced. | The V3 client half cannot install or build until the declaration is made, so the identity bridge is not deployable on this branch. This is reported as an open item with these citations, and it is follow-up F3 — the highest-priority one after the runtime upgrade. |
| R19 | M-11 | Keep terse `# SECURITY:` threat statements and contract documentation in code, and move every justification, alternative and trade-off into this file. | Remove security comments altogether; keep the rationale in code and treat the explainability rule as satisfied by the plan document. | The two instructions only appear to conflict. The security-comment requirement asks that the *threat* be identifiable at the code site; the explainability rule asks that the *decision* be explained in one canonical place. Splitting factual threat markers from rationale satisfies both without weakening either. Docstrings that state what a function guarantees are API documentation, not rationale, and are retained. | A reader of a single file no longer sees why an approach was chosen, and R24 removed the inline pointers that once softened that. Mitigated by each entry here naming the artifacts it governs, so the mapping is navigable from this side. |
| R20 | M-12 | Correct the operational facts in the artifacts that existed at the time (`.env.example`, `scripts/deploy.sh`, `nginx.conf`, `Dockerfile.frontend`) and defer the onboarding documents. **Now discharged.** | Create `README.md` and `documentation/Developer Onboarding.md` at that point and put the corrected facts there. | Those two documents were planned artifacts that the review then placed out of scope as not yet implemented, so writing them would have collided with the work that owned them. | No residual risk remains. Both documents now exist: `README.md` was rewritten and `documentation/Developer Onboarding.md` created, and each carries the facts this entry deferred — the single deployment path and why a direct storage URL must not be advertised, the runtime-versus-signer split with its must-differ constraint, the shared throttling store, and the frontend build variables. Follow-up F14 is closed. |
| R22 | F1 | Give the identity lookup deterministic ownership of its Session: retain the `get_db()` generator, close it in a `finally` that every path reaches, and detach the resolved row before returning it. | Leave `db = next(get_db())` as the change authorisation records it; add `db: Session = Depends(get_db)` to `get_current_user` so FastAPI owns the Session; call `SessionLocal()` directly inside a `with` block; return a plain data object carrying the identity instead of a `User`. | Leaving it was rejected on evidence: the authorisation judged the leak harmless while the dependency was dormant, and this remediation makes it run on every authenticated request, one held connection at a time (DEV-5). A `db` dependency parameter would change `get_current_user`'s signature, which is a frozen contract. Calling `SessionLocal()` here would make this the second place in the codebase that creates a Session, so the `finally: db.close()` in `get_db()` would no longer be the single cleanup path. Returning a plain object would change the `-> User` return type that all five route signatures annotate. Retaining the generator reuses the cleanup that already exists and leaves every contract intact. | The resolved user is detached, so an attribute needing a further query raises `DetachedInstanceError` instead of lazy-loading. Bounded today: `User` declares no relationships and no handler reads `current_user` (D13). A future handler that does must either read only the loaded columns or re-attach the row to its own request-scoped Session. Recorded as residual 15. |
| R23 | F2 | Restore the encryption-guaranteeing contract on `db_sslmode` by annotating the field `Literal["require", "verify-ca", "verify-full"]`, and document only those three modes in `.env.example`. **Superseded by D54**, which narrows the same `Literal` to the modes this deployment can actually complete. | Leave the field as `str` and validate inside `database.py` before `create_engine`; add a `@validator` to `Settings`; re-introduce a module-level `DatabaseSslMode` alias; leave it unconstrained and rely on operator discipline. | The value is handed to psycopg2 unchecked, and `disable`, `allow` and `prefer` can each complete a connection without encryption, so an unconstrained field silently reopens V6 against any endpoint that is not this exact Cloud SQL instance — the contradiction with D7 the review names. A `Literal` refuses the insecure and the misspelled alike at `Settings()` construction, which is strictly before engine construction, and it does so without a validator, a `Config` change or `Field` metadata, none of which the field's authorisation permits. **Corrected:** an earlier version of this entry said the annotation was applied inline "rather than re-adding a module-level alias", to keep the module's public inventory exactly as authorized. That is not what shipped — `backend/app/core/config.py` L45 defines the module-level alias `DatabaseSslMode = Literal["require", "verify-ca", "verify-full"]` and L81 annotates the field with it. The alias adds one name to the module's public surface, which DEV-9 records as part of the wider configuration contract R28 settled; the encryption-guaranteeing behaviour is identical either way. | Start-up now fails outright for an insecure or misspelled mode. That is the intended fail-closed behaviour, but it makes a typo a start-up failure rather than a degraded connection. It also removes any plaintext fallback: with the Cloud SQL instance on `ENCRYPTED_ONLY` such a fallback could not have connected anyway. **Superseded on one point:** this entry said "the rollback path D7 contemplates remains `require` ↔ `verify-ca` ↔ `verify-full`." D54 removed that path — the deployment script refuses `verify-ca` and `verify-full` because the Cloud SQL Auth Proxy terminates TLS at a loopback listener holding no server certificate for the client to verify, so advertising them as selectable modes described a rollback that would fail at the first connection. The field now has exactly one legal value, and certificate material plus a direct connection are prerequisites for adding a second (F8). |
| R24 | F3 | Build the engine from one `Settings` instance read once at import, used for both `DATABASE_URL` and `db_sslmode`. | Keep the two independent `get_settings()` calls; cache `get_settings()`; read the TLS mode per connection through an engine event or a `creator`. | Every `Settings()` construction performs three live Secret Manager reads, so two calls made importing the module six remote reads instead of three and let the URL and the TLS mode come from two different fetches — a second fetch that fails after the first succeeded breaks the import, and `security.py` imports this module, so it breaks before authentication is usable. A module-local snapshot is not the `get_settings()` caching that is excluded from scope: `get_settings()` itself is untouched, still uncached, and still re-read per request everywhere it governs a toggle. Reading the mode per connection would defer engine construction, which the module's authorisation forbids. | The engine binds the mode read at import, so changing `db_sslmode` needs a pod restart — the propagation the rollback design already documents for this setting. The snapshot also means a Secret Manager value that changes after start-up is not picked up by the engine, which is the pre-existing behaviour of an engine built at import. |
| R25 | F1 | Initialise the default Firebase app from a guarded statement at module scope in `frontend/src/services/api.ts`, so it exists while that module is being evaluated, and keep `resolvedAuthentication()` resolving it lazily as before. | Leave initialisation inside the request interceptor, where it was; initialise inside `services/auth.ts` next to the sign-in call; add a new frontend module owning initialisation; call `initializeApp` bare at module scope with no guard. | The only initialiser ran inside the request interceptor, so it could not run until an API request had already been issued — while the preserved sign-in path calls `getAuth()` with no argument and needs the default app *first*. Sign-in therefore failed before any token, and so any API request, could exist. `auth.ts` and `collaboration.ts` are reference-only and both call their SDK getter inside a function invoked on user action, and `api.ts` is in the eager import graph of all three pages, so initialising here provably precedes every consumer without touching a frozen file. The guard is required because the module must stay importable: an unguarded call would throw during evaluation on an incomplete build configuration and take down every page that imports the module. | A missing Firebase build variable is now reported to the console during evaluation instead of failing the first request outright. Bounded: the lazy path is unchanged, so `resolvedAuthentication()` still raises the same named-variable error to API callers, and the request interceptor still refuses to send a request without a credential. |
| R26 | F2, F3 | Adapt the cell update entirely on the client: send `CellSchema` values as a list, and return each caller's own `cell` when the request succeeds. | Change the route to accept a single cell; change the route to return the updated cell; change the exported client signature to take and return a list; return the response body cast to `CellSchema`. | The route's list body and its `{"message": …}` acknowledgement response are frozen contracts, as is the exported `updateCell(workbookId, worksheetId, cell): Promise<CellSchema>` signature, and the route may only receive the authentication dependency — so neither side may move and the adapter has to sit between them. Measured against a replica of the route: the object body the client used to send is rejected with **422** before the handler runs, and the success body carries none of `CellSchema`'s fields, so the previous `return response.data` could never have produced a cell. | The returned cell is the accepted input rather than server state, so any value the server would have normalised is not reflected. Bounded: the route computes no cell to return, so there is no server-side value to lose. Revisit if the route ever returns cells — follow-up F22. **Corrected:** an earlier version of this entry said each call sends "a one-element list." It sends a list of however many cells were queued in the coalescing window, which is one only when a single cell was edited in it; D44 records that decision and `frontend/src/services/api.test.ts` asserts both the one-cell and the multi-cell shape. |
| R27 | F5 | Carry no citation of this file in code or configuration. Comments state the threat closed, the control enforced and the operational facts an operator needs; nothing more. | Keep the `Rationale: <entry>` pointers as navigation; keep pointers only in the larger files; replace them with a single pointer per file. | The explainability rule makes this file the single source of truth for "why" and forbids rationale in code comments. A comment labelled `Rationale:` presents itself as rationale wherever it points, and prose such as "deliberately NOT enforced" or an enumeration of the alternatives resolved to the same app *is* the justification, not a reference to it. Fourteen such sites across seven artifacts were removed, keeping every factual statement they sat beside — the apply-and-revert-together constraint on the uploads bucket, the `npm ci` prerequisite, the nginx template contract and the reason a direct storage URL must not be advertised all survive as facts. | Navigation is now one-way: a reader of a single file is not told that this file exists. Mitigated by R19's arrangement — each entry here names the artifacts it governs. **Corrected:** an earlier version of this entry claimed the convention was already uniform. Three citations survived it — in `.env.example`, `backend/tests/test_security.py` and `frontend/src/services/api.test.ts` — and have now been removed with the facts beside them kept. `git grep "Security Decision Log"` outside `documentation/`, `README.md` and `SECURITY.md` returns nothing, which is the check that makes the claim verifiable rather than asserted. |
| R28 | Review findings F21 (throttling), F1/F2/F10/F13 (configuration) and F23 (scope) | **The configuration half is SUPERSEDED by R34 — read that entry first. R34 withdrew the three surplus fields and restored the authorized seventeen-field surface, and DEV-9 records that the surface now matches the map exactly. What survives here is the throttling behaviour, not the widened field set.** Keep the throttling hardening and the expanded configuration contract that R21 and R9 had withdrawn — the shared window store, the bounded in-process fallback, trusted-proxy client identification, the `signer_service_account` validator, the bounded `signed_url_expiry_minutes` and the `db_sslmode` literal — and keep `.terraform.lock.hcl` withdrawn. | Withdraw the hardening again to match R21; keep the hardening but restore the provider lock too; split the decision and defer the throttling half. | The controls close defects that were raised as blocking at the following review order: per-process counters let a client's quota multiply by the number of workers and pods, every client behind the load balancer shared one throttling identity, an unconstrained `db_sslmode` could negotiate plaintext, and an unbounded expiry could exceed what Cloud Storage will honour. Each is refused at `Settings()` construction or in one shared store, so a misconfiguration fails before it serves traffic. The lock file stays withdrawn for the opposite reason: it pinned a provider version that is not cached here and blocked `terraform validate` outright, so it removed more assurance than it added. | The scope reversal this entry opened is **settled and closed**: R34 took the decision the transformation map dictates, and DEV-9 records the surface as matching the authorized eleven additive fields exactly. **Corrected four ways.** An earlier version of this entry (a) named "trusted-proxy-hop" identification, which is not the mechanism — see D42; (b) listed an `api_origin` validator among the fields kept, which does not exist on `Settings` and is asserted absent by a test — see D30; (c) counted "21 fields and 3 validators", then "**20** fields (the six original plus fourteen)", where `backend/app/core/config.py` declares **17** fields — the six original plus eleven — and now four validators, `gcs_bucket_name` having gained one; and (d) said the throttling scope "still wants an explicit human settlement", which was true when written and was answered by R34. It also said the `terraform` block was withdrawn; only the lock file is (D40). Disabled-mode throttling is logged but still carries no response marker (F19). |
| R29 | QA testing: broken Application Default Credentials, and Identity-Toolkit unavailability, each returned an un-logged HTTP 500 | Separate the two faults by **status code as well as by log level**: a credential that was checked and refused answers 401 at `warning`; a credential that could not be checked at all answers **503 with `Retry-After`** at `error`, from a second `except` clause placed after the specific-class clause and catching `firebase_admin.exceptions.FirebaseError` and `google.auth.exceptions.GoogleAuthError`. | Widen the existing tuple instead of adding a clause; answer 401 for a provider outage and carry the distinction in the log alone; catch bare `Exception`; leave the propagation as documented behaviour. | Widening the existing tuple would have flattened a caller fault and a deployment fault into one `warning` line, and every listed auth error subclasses `FirebaseError`, so a tuple entry ahead of them would have swallowed their type names — the verified rejection messages had to stay byte-identical, which fixes the clause order. **Corrected:** an earlier version of this entry chose 401 for the provider fault, on the ground that 503 "would introduce a response shape the frozen contracts do not carry." That reasoning does not survive `check_revoked=True` (R4): every authenticated request depends on a live provider call, so a provider outage answers every caller, and answering it 401 tells each of them their credential is bad and invites them to re-authenticate against a provider that is down. 503 with `Retry-After` is the honest status, it is a rejection shape rather than a route response so no frozen request or response schema changes, and `error` level still separates the outage from the ordinary rejection stream. See D38 for the settled contract. | A 503 is a status the client code did not previously encounter, so a caller that special-cases only 401 sees an unhandled failure. That is the intended signal: an outage should not look like a bad credential. `check_revoked=True` couples every authenticated request to a live provider call, so an outage still fails all authenticated traffic — now visibly, and with a retry hint. **SUPERSEDED - the settled contract is 401-only.** See D38, whose own note records the settlement: no fault answers `503`, no response carries `Retry-After`, and a credential that could not be checked is answered with the frozen 401 and separated from a refused credential by log level alone. |
| R30 | QA testing: identity-claim values the identity column cannot hold reached the database driver as un-logged HTTP 500s | Validate the claim before it is queried: convert the legacy `sub` to an integer inside a `try`, rejecting a boolean explicitly, and reject a Firebase `email` claim containing a NUL byte. Both map to the existing 401 with a log line naming only the exception type or the claim name. | Range-check the integer against PostgreSQL's `int4` bounds as well; sanitise the email by stripping the NUL; cast in the query with a database-side guard; leave it, since injection was already impossible. | Measured against the running PostgreSQL 13 instance, a Python `int` of any magnitude is safe — a forty-digit value, `2**31` and a negative value each return zero rows, because psycopg2 adapts an `int` to a numeric literal the comparison widens; only a **string** fails, as `NumericValueOutOfRange` or `InvalidTextRepresentation`. Conversion alone therefore closes the whole class, and a range check would add an untriggerable branch. Stripping the NUL would silently alter an identity, which an authentication path must never do. A boolean needs its own rejection because `int(True)` is `1`, which would select a real row, and the bypass path reads claims without any validation. Injection was indeed already impossible — parameterised binding was confirmed, emitting `WHERE users.id = %(id_1)s` with the payload intact as a value, and the `users` table verified intact afterwards — so this is a robustness and observability fix rather than a security one. | Slightly stricter than before: a legacy `sub` of `" 5 "` now resolves to `5` where it previously raised, and any non-numeric `sub` is refused rather than reaching the driver. No valid caller is affected — a numeric `sub` for a known user and a clean `email` are both still admitted. |
| R31 | QA testing: the specification documents no 401, `tokenUrl` names a route that does not exist, and `WWW-Authenticate` is not readable by cross-origin JavaScript | Change no code for any of the three. | Add `responses={401: ...}` to each route decorator; mount a `/token` route; add `Access-Control-Expose-Headers` to the CORS middleware. | Each remedy is blocked by a boundary this remediation holds. Declaring a 401 response means editing route decorators, and the routes may receive the authentication dependency and nothing else. A `/token` route is refused by D21 on its own merits, and the documentation UI's inability to authenticate is the cosmetic consequence of that decision rather than a separate defect. **Corrected:** an earlier version of this entry justified the third by saying the CORS middleware sits in "the application entry point, which this change set does not wire." That is wrong — `backend/app/main.py` L16-26 defines `configure_cors()` and L44 calls it, so this change set *does* wire CORS, and V5 narrowed its methods and headers. The honest ground for not adding `Access-Control-Expose-Headers` is narrower: it would widen the CORS contract beyond the two changes V5 requires, for a benefit no client currently needs, since nothing in `frontend/src/services/api.ts` reads the challenge header. All three were verified as reported: the challenge **is** present on the wire on every 401, the security requirement **is** declared on all five operations, and `/token` **does** answer 404 — so the observable security behaviour is already correct in each case. | A specification reader sees no documented 401, and a documentation-UI user cannot authenticate from the browser. Neither affects enforced behaviour. Recorded as residuals 17 and 18. |
| R32 | M2, m2 | Replace the `slowapi` ceiling tier with a `limits` fixed-window tier counted against **one application-wide scope**, delivered by a pure-ASGI middleware that runs before routing. `slowapi` stays pinned in the manifest but is no longer imported anywhere. **AMENDED by D68 — the pin was subsequently removed: a pin nothing imports ships its transitive surface for no control, and the annotation beside it asserted the opposite of what a passing test already proved.** | Keep `SlowAPIMiddleware` and add a second global counter beside it; move to per-route `@limiter.limit` decorators with a shared key; accept per-endpoint counting and rename the setting to `rate_limit_per_endpoint`. | Read at source rather than inferred: `slowapi.middleware.SlowAPIMiddleware` resolves the route handler, then `_should_exempt` **returns True when the handler is None**, so a request to any path that matches no route was exempt from the ceiling entirely; and `_check_request_limit` keys the window by the endpoint function's name, so `600/minute` was a budget *per URL*, not per client. A control described as a global ceiling that a caller can multiply by discovering more endpoints — and escape completely by requesting a path that does not exist — is not the control the requirement asks for. Counting before routing is what makes unmatched paths countable at all. | The ceiling is now genuinely shared, so a client using many endpoints legitimately reaches it sooner than before; that is the point, and it is why R41 raised the write budget rather than tightening it. `slowapi` remaining pinned but unimported is deliberate — the manifest records why, and removing it would be a dependency change with no security effect. |
| R33 | M3 | Define **one** degradation policy for a window-store failure: log once with the traceback, substitute a process-local counter, **re-count the same request against it**, and keep enforcing for the life of the process, exposing `degraded` per tier. | Fail closed and refuse to serve; fail open and admit until the store returns; retry per request; keep the previous behaviour of falling back at start-up only. | The previous behaviour degraded at start-up but had no defined answer for a store that failed mid-request, and the honest reading of that path was that the request was admitted unmetered — an attacker who can make Redis unavailable could therefore remove throttling rather than merely coarsen it. Failing closed was rejected because a Redis outage would then be a total outage of an API whose throttling is a defence in depth, not a correctness requirement. Re-counting the triggering request is what makes the policy hole-free: without it, exactly the request that broke the store is the one that escapes metering. | Counters become per-process, so a client's real ceiling is the configured value times the number of processes. That is a precision loss, stated wherever the thresholds are documented, and it is strictly better than the alternative it replaces. Nothing surfaces the degraded flag automatically yet — a health endpoint would, and that is a follow-up rather than a control this change set may add. |
| R34 | M1 | Withdraw `signer_service_account`, `rate_limit_storage_uri` and `rate_limit_trusted_proxies` from `Settings`, restoring the authorized seventeen-field surface, and replace each with a fixed behaviour: the signer is the runtime's own attached identity, the window store is derived from `REDIS_URL`, and the throttling identity is always the socket peer. | Keep the three fields and seek approval for a twenty-field surface; keep two and drop one; rename them so they fall outside the counted inventory. | The file transformation map authorizes exactly eleven additive fields, seventeen in total, and two earlier reviews classified the surplus as a scope violation while one classified it as required — R28 records that reversal and asks for a settlement. This is the settlement, and it goes the way the map says. It costs nothing that was load-bearing: `file_storage.py` already fell back to the runtime's own identity when the signer setting was empty, and that fallback is now the only path, which matches the one-account topology in R36; one Redis already served the broker, so a second URL was a value to keep in step rather than a capability. | Metering on the socket peer means every client behind a proxy is throttled as one. That is a real residual, recorded as such, and it is the safe direction: trusting `X-Forwarded-For` would let any caller reaching the API directly name the identity its quota is charged to. A cross-project or delegated signer is no longer expressible from the application side; Terraform is where that would be added. |
| R35 | M9 | Describe header delivery as **three producers plus one artifact that is not a header producer**, and state that their Content-Security-Policies are deliberately *not* identical: the two static producers append the API origin to `connect-src`, the API producer takes no such input, and the compiled document's `<meta>` element carries a strict subset of the loading directives. | Keep describing four equal delivery points; give the API middleware an `api_origin` input so all three genuinely match; render the API origin into the meta policy as D31 proposed. | Calling them four equal points made two false promises. It implied the API middleware has an API-origin input, which it does not and must not — it serves the API's own origin, where `'self'` already *is* the API, so adding the input would widen the policy for no gain. And it implied the meta element is a fourth header producer, when it emits no header at all, cannot carry the report-only header name, and has `frame-ancestors` ignored inside it. Documenting the real relationship is what lets a reader work out where to add an origin. | The shorthand was convenient and its removal makes the description longer. Mitigated by stating the relationship once, in `backend/app/core/security_headers.py`, and referencing it from `nginx.conf`, `main.tf` and `SECURITY.md`, with a test asserting that the container policy is exactly the API policy plus the substituted origin and that the API middleware takes no origin input. **Supersedes D31**, whose build-time substitution of the API origin into the meta policy is not what shipped: the delivered meta policy omits `connect-src` and `default-src` entirely, which achieves the same end — a document policy that cannot narrow the served one — without a build step. |
| R36 | C1 | Settle the identity topology as **one** service account: the API runs as it, signs object URLs as itself, and holds every grant, reached from the pods by a `roles/iam.workloadIdentityUser` binding on `PROJECT_ID.svc.id.goog[NAMESPACE/NAME]`, with the cluster carrying `workload_identity_config`. Delete the `runtime_service_account` variable. | Restore the two-account split R8 chose; keep one account but grant the signing role at project level; give the runtime a downloaded key. | R8's two-account model was correct reasoning about a different premise: it assumed the application would *name* a signer account other than itself, which required a backend setting to name it. R34 withdrew that setting, and without it a second account is unreachable — the runtime can only sign as whatever identity its credentials present. `roles/iam.serviceAccountTokenCreator` on the account **to itself** is not the useless self-binding R8 warned about in that case: it is exactly the `iam.serviceAccounts.signBlob` permission a runtime holding an access token and no private key needs, and it is the only grant that makes signing work at all. The decisive defect this settles is that the deployed shape had *neither* model complete — one account in Terraform, two required by every other artifact, and no Workload Identity binding anywhere, so the pods would have held no Google identity and failed every secret read, database connection and signing call. | A compromise of this one identity reaches everything it holds, where a split would have separated signing from data access. Mitigated by every grant being narrowly scoped — object admin on one bucket, three named secret versions, `workloadIdentityUser` for one namespace and service-account name — and by no key file existing anywhere. **Supersedes R8.** |
| R37 | C2 | Grant `roles/firebaseauth.viewer` to the runtime and assert it in deployment preflight. | Drop `check_revoked=True` and remove the IAM dependency; grant `roles/firebaseauth.admin`; catch the permission error and fall back to verification without the revocation check. | R4 turned revocation checking on, which makes a user-record read through the Identity Toolkit API part of every authenticated request, and the role that authorizes it was absent — so the shipped configuration refused **every** authenticated request. Dropping the check would trade a total outage for silently honouring signed-out and disabled users' outstanding tokens. Falling back on a permission error is worse than either: it would make an IAM misconfiguration invisible and downgrade the control without anyone deciding to. `firebaseauth.viewer` is the least-privileged predefined role carrying `firebaseauth.users.get`; it is read-only, so this identity cannot create, disable or delete an account, nor read a password hash or signing secret. | A per-request read on the authentication path, costing latency and Identity Toolkit quota. Accepted as the price of closing token replay after sign-out, which residual 9 already records. Preflight asserting the role converts a silent total outage - one that looks to every caller like a bad credential - into a refused deployment. |
| R38 | M8 | Delete `https_cutover_enabled` and make the port-80 redirect a **required** part of the topology, with deployment preflight failing when the forwarding rule is absent and the certificate sequenced by DNS instead. | Keep the flag and wire it with `count`; keep the flag and document it as always-true; keep preflight's tolerance of a missing rule. | The flag was declared, documented in four places as a two-stage cutover, and referenced by no resource — Terraform already created the rule unconditionally. So the documentation described a procedure that did not exist, and preflight tolerated an absent listener on the strength of it. The premise was wrong as well as dead: a Google-managed certificate is gated by DNS resolving to the load-balancer address, not by the presence or absence of a port-80 listener, so suppressing the redirect would only mean port 80 refused connections instead of redirecting them, and nothing about provisioning would happen sooner. | A first deployment must now have both forwarding rules before the script will publish, which is a slightly stricter ordering requirement. It is the correct one: an absent redirect means a client that reaches `http://` is refused rather than upgraded, so an already-plaintext request is never moved to TLS. |
| R39 | M11 | Resolve the remaining unread Terraform variables by **deleting** `db_password` and `storage_bucket_name` and **labelling and defaulting** `environment`, `db_user`, `gke_min_nodes` and `gke_max_nodes`, so no input is both required and inert. | Delete all six; label all six; leave them as they were. | The rule applied is that a dead input is deleted when keeping it would itself create risk or misdescribe a security-relevant fact, and kept-but-labelled when it names a real fact another authority consumes. `db_password` had to go: declaring it would write a live database credential into Terraform state and into whatever tfvars supplied it, for a value no resource reads — the login is created with `gcloud sql users create --prompt-for-password` and the credential travels inside the `DATABASE_URL` secret. `storage_bucket_name` had to go because it cannot name either bucket; both derive from `project_id`. The other four name real facts — the operator's environment label, the login `scripts/deploy.sh` verifies — or would change capacity to wire, so they are defaulted and each says `NOT REFERENCED` in its own description. | Deleting a declared variable breaks an operator who passes it, with a "Value for undeclared variable" error. Accepted: the error is immediate and self-explanatory, and for `db_password` the alternative is a credential in state. **Supersedes D27**, whose described end state was never actually applied to `variables.tf` — it claimed `db_name` had been given a default and five variables documented as `NOT REFERENCED`, and neither was true in the file. Both are true now, and a test enforces it: every declared variable must be read by `main.tf` or say `NOT REFERENCED` and carry a default. |
| R40 | M10 | Pin `react-router-dom@5.3.4` and `@types/react-router-dom@5.3.3` in the CI security job, pin Node 22 for that job alone, and **remove `continue-on-error`** from the frontend interceptor step. | Keep the v6 pin and migrate `app.tsx` to the v6 API; leave the step tolerated; drop the frontend check from CI. | Measured rather than argued: `frontend/src/app.tsx` imports `Switch` and passes `component=` to `Route`, both removed in v6, and the type check reports four errors in that file against `6.30.1` and none against `5.3.4`. Migrating the source is a deliberate frontend change outside this change set, so CI installs the version the source is written for. Making the step a gate matters more than it looks: the interceptor attaches the only credential the API accepts, so a regression there does not degrade the application, it locks every user out of it. The missing lockfile blocks `npm audit`, which stays tolerated, but not this step — every undeclared package is installed at an exact version and the suite under test imports none of the declared ranges. | Node 22 in one job and Node 14 in another is a visible inconsistency. It is the truthful one: Node 14 is end-of-life and cannot run the toolchain, and the four places pinning it are outside this change set. Recorded as a residual and as a next task rather than papered over. Installing without a lockfile still re-resolves the declared ranges each run, which is the same exposure the existing build job carries. |
| R41 | M4 | Raise `CELL_WRITE_COALESCE_MS` from 500 to 1000 and `rate_limit_write` from 120 to 300 per minute **together**, and record the arithmetic in both files. | Raise only the budget; raise only the window; add a per-route exemption for the cell endpoint; leave both and rely on the client never sustaining the cadence. | At 500 ms the client could emit 120 writes a minute from a *single* worksheet, which was the entire shared write budget — one user editing steadily would throttle their own next workbook creation. The two numbers are one decision and were being chosen independently in two files, so either alone leaves the relationship undefined. At 1000 ms and 300/minute a continuously edited worksheet costs 60 writes a minute, five such worksheets reach the ceiling, and four leave 60/minute of headroom. A per-route exemption was rejected because it would need a `request` parameter on the handler, which the boundary forbids. | A 1000 ms window makes a collaborator's view of another user's edits up to half a second staler. Accepted, and it is the same trade the coalescing already made. Both numbers are now asserted by one test, so neither can move alone. |
| R42 | M11 | Repair the deployment script's three repairable hard stops — build the backend image from the real Dockerfile with the right context, require operator-supplied migration and post-deployment commands, and verify both before the first mutation — and publish a **KNOWN BLOCKERS** section for the ones that cannot be repaired from inside this change set. | Leave the script as it was and document it as blocked; relax the checks that abort; implement stubs for the migration and test steps. | The script presented itself as deployable while containing stops that no operator could pass. Two were genuine defects in the script: `cd backend && docker build .` names a directory that holds no Dockerfile, and two `HUMAN ASSISTANCE NEEDED` comments printed a step heading, did nothing, and let the run reach a success banner. Those are repairable and were repaired — the commands are now required inputs resolved before the first mutation, so `set -e` prevents a success banner over skipped work. The Firestore reference check is not repairable here: `collaboration.ts` is a reference-only artifact, and relaxing the check would deploy authorization rules for a path the browser can never reach, publishing a control that is never exercised. Stubs were rejected outright: a stub migration step that exits 0 is indistinguishable from a real one that worked. | An operator must now supply two commands this repository cannot default, so a first run stops in the configuration section. That is the intended outcome — the alternative is a green deployment with an unmigrated schema. The image now builds and still cannot start, which the blockers section states rather than implies. |
| R43 | C2, C3, M12, M15 | Correct the operator-facing documents to the delivered behaviour, and where a document had asserted the opposite, say so explicitly rather than quietly replacing the text. | Rewrite silently; add a changelog section; leave the documents and rely on the code comments. | Three claims were not merely stale but actively unsafe to act on: that a Bearer token stayed mandatory with enforcement disabled, that revocation was not checked, and that `legacy_jwt` was an available rollback. An operator following any of them during an incident would make the wrong move — reaching for the break-glass switch expecting a weakened check, or selecting a verifier the browser cannot satisfy. Naming the previous claim as wrong is what stops a reader who remembers it from assuming the new text is the drift. The stale counts and the placeholder emulator command are lower stakes but the same class of defect: a document that cannot be checked against the code stops being consulted. | Explicit corrections make the documents longer and record their own history, which reads oddly in a policy document. Accepted deliberately for the three unsafe claims and not applied to the routine count updates. |
| R44 | C-3 (order-1 security review): the `email_verified` claim was not required, so a self-registered Firebase account naming an existing user's address could authenticate as that user | **Restore the gate.** `_resolve_identity` refuses the token unless `email_verified` is exactly the boolean `True`, before the `email` claim selects a `User` row. **Supersedes R1.** | Keep R1's position and rely on restricting Firebase account creation; accept any truthy value; map on the immutable `uid` with a schema migration; drive `sendEmailVerification` from the client and block sign-in until confirmed. | R1 withdrew this gate to avoid locking out a legitimate user, on the reasoning that the preserved sign-in flow never initiates verification — but that trade was the wrong way round. The gate's absence is an **account-takeover** path: the whole security of the email bridge rests on the provider having proved the caller controls the address, and without the claim the bridge asserts an identity nobody verified. A lockout is visible, reversible and affects the operator's own users; a takeover is silent. Restricting who may create accounts in the Firebase project is an operator control that closes the same hole, and it is now named as the prerequisite rather than offered as a mitigation. Only the boolean passes: a truthy string or `1` does not, because Firebase emits a boolean and anything else means the token was not minted by the flow this bridge trusts. | A user whose Firebase account exists but has never confirmed its address is refused with 401 after a successful sign-in. That is the intended fail-closed behaviour, and it is the operator's to resolve — either by requiring verification in the sign-in flow (F21) or by controlling account creation. The refusal is logged **without** the address, so the log does not itself become a list of addresses an attacker probed. |
| R45 | C-1 (order-1 security review): `firestore.rules` allowed a collaborator to delete a workbook | `allow delete: if isOwner();` — owner only. | Keep collaborator delete and rely on Firestore's undelete window; add a soft-delete field a collaborator may set; require a custom claim for delete. | Deletion is irreversible and it removes the workbook from the **owner and from every other collaborator**, so it sits outside the content-editing authority a collaborator is granted. The rules' own operator-facing contract already read "owner … delete" and "collaborator read, and update confined to the content fields", and `backend/tests/firestore_rules_emulator_check.py` already expected collaborator-delete **denied** — so the rule was contradicting both its own documentation and its own acceptance artefact, which is the strongest possible signal that the rule rather than the expectation was wrong. A soft-delete field would be a new data contract on documents that do not yet carry the authorization fields they need. | A collaborator who legitimately needs to remove a workbook must ask its owner. Accepted: that is the authority model the file already documented. |
| R46 | H-2 (order-1 security review): the "global ceiling" was one full quota **per route** | Use `slowapi`'s `application_limits` rather than `default_limits`. **Supersedes R21 on this point.** | Keep `default_limits` and document the multiplication; add a per-route decorator budget; compute a synthetic key in a custom `key_func`. | This was not a tuning choice but a misreading of the library, confirmed by reading the installed source: a *default* limit is constructed with `scope=None`, which slowapi resolves to the requested **endpoint**, so a caller enumerating five endpoints received five times `rate_limit_default`. An *application* limit carries the fixed scope `"global"`, giving one counter per client across every route — which is what the control was always described as doing. Per-route decorators are refused by D6 on unchanged grounds: they require a `request: Request` parameter on every handler, which the route boundary forbids. | A client's single budget is now shared by reads and writes together, so heavy reading can consume budget a write then needs. Deliberate — that is what a ceiling is — and the write tier has its own separate, tighter budget so ordinary editing is not starved by it. The ceiling must stay generous: `PUT …/cells` is the highest-frequency endpoint and is covered by it. **SUPERSEDED by R32**, which replaced the `slowapi` tier entirely rather than reconfiguring it. |
| R47 | H-2 and M-6 (order-1 security review): a store outage admitted every request unmetered, and two ceilings were hardcoded module constants | Fail **bounded**, never open: `swallow_errors=False` with an `in_memory_fallback` for the ceiling, and a process-local `FixedWindowRateLimiter` that the write tier re-charges the same window against when the shared store raises. Move the request-body and object-size ceilings into `Settings` as `max_request_body_bytes` and `max_upload_bytes`. | Keep `swallow_errors=True` and accept fail-open during an outage; fail closed and refuse all traffic when the store is unreachable; keep the constants and document them. | Fail-open converts a Redis outage into an unthrottled API, which is precisely when an attacker most wants one — and the outage need not even be hostile to be exploited. Fail-closed would turn a throttling-store outage into a total outage, which is a worse availability trade than the control is worth. Bounded degradation keeps counting and merely loses the fleet-wide view: with N workers a client can briefly obtain N times the budget, which is a far smaller loss than unlimited. The two ceilings became settings because a ceiling an operator cannot retune gets removed in an emergency instead of raised. | Degraded counting is per-worker, so the effective quota during an outage is worse than configured. Logged when it happens. The two new fields widen the configuration surface that DEV-9 already records as the open scope question. |
| R48 | C-2 (order-1 security review): no cluster Workload Identity pool, no Kubernetes-to-Google binding, and the runtime and signer collapsed into one account holding every grant | **SUPERSEDED by R36 — read that entry first; this one records why the two-account topology was restored and why the deployed configuration does not carry it.** None of the arrangement described in this cell is in the configuration today: there is one service account, the signing role is bound on it with itself as member, and the precondition checks that the configured address names this project rather than comparing two addresses. Restore the two-account topology in full: a runtime service account bound to the Kubernetes service account with `roles/iam.workloadIdentityUser`, a cluster `workload_identity_config`, every runtime grant on the runtime account, the signer reduced to `roles/storage.objectViewer` on the uploads bucket, and `roles/iam.serviceAccountTokenCreator` granted from the runtime **on** the signer. Add a Terraform **precondition** that fails the plan if the two configured addresses are equal. | Document the required bindings and leave them to the operator; keep one account and accept the self-grant; grant `tokenCreator` at project level. | R8 established this topology and the configuration had lost it. Three separate halves were missing and each alone is fatal: without the cluster pool there is no principal for a binding to name, so pods hold no Google identity and the first `Settings()` construction fails on Secret Manager; without the binding the pods run as the namespace default; and with one collapsed account the runtime holds `tokenCreator` **on itself**, which makes the identity that reads every secret, the database and Firestore into a signing oracle for its own credential — so an SSRF, RCE or file-read in the application yields signatures as that identity. `signBlob` cannot be narrowed by an IAM Condition, so the *only* available constraint is which account the role is granted on, which is why the signer's entire authority is reading one bucket. The precondition exists because this collapse has now happened once: a comment asking a future author not to do it is weaker than a plan that refuses. | Two identities to provision, and the binding must name the exact namespace and Kubernetes service account. Both are validated Terraform variables and `scripts/deploy.sh` compares the deployed pod spec against them before applying anything. `roles/firebaseauth.viewer` is now also required on the runtime, because R4's `check_revoked=True` performs a user-record read — without it every verification failed the lookup rather than the signature, so **no** request could authenticate. **SUPERSEDED by R36**, which settled the topology as one account for the reason this entry did not have available: the signing call names the runtime's own address. |
| R49 | H-5 (order-1 security review): the invoker binding was additive, and the function ran as the App Engine default account | Make the invoker binding **authoritative** (`google_cloudfunctions_function_iam_binding`), give the function a dedicated runtime service account holding no role anywhere, refuse a source bucket that is not private, and in the deployment script revoke `allAuthenticatedUsers` as well as `allUsers` and then compare every surviving member against an allow-list. **Supersedes R13's rejection of the authoritative form.** | Keep the additive member and rely on the script's revocation; keep revoking only the two known principals; enumerate broad principals to remove. | R13 rejected the authoritative form for having a larger blast radius than the finding required — it also deletes bindings added legitimately out of band. That is true, and it is now the point: an additive binding grants the intended caller while leaving *every* pre-existing member untouched and invisible in the plan, so a public binding from an earlier deployment survives every apply. The script's denylist could not close the gap either, because a denylist only removes the principals it was told to look for: a broad group, a departed engineer's account or a service account from another project all passed unremarked. An allow-list inverts the question into a positive assertion about who may invoke. `allAuthenticatedUsers` was the concrete miss — it admits every Google account in existence, not merely accounts in this project. And a 1st-gen function with no `service_account_email` runs as the App Engine default account, which Google grants project **Editor**. | A legitimate caller added out of band is now removed on the next apply and aborts the deployment before that. Intended: the failure message directs the operator to add it to the Terraform `members` list, so the grant becomes declared and reviewable rather than merely present. |
| R50 | H-6 (order-1 security review): the Cloud SQL manifest check passed on half its evidence, the port-80 listener's target was never verified, authorization rules were deployed last, and nothing verified the deployed controls | Split the compound condition into two assertions; verify each forwarding rule's target on its full resource **path segment**; deploy the Firestore rules **first**, before any code; and verify the six headers, the plaintext redirect and the unauthenticated 401 against the running deployment, aborting with a printed rollback procedure on any failure. | Fix only the compound condition; keep the ordering and document the window; leave post-deployment verification to the manual checklist that already existed. | The compound condition was `if ! A && ! B`, which is `NOT (A OR B)` — it aborted only when *both* halves were absent, so a proxy configured for a different instance passed, and this instance's connection name with no proxy passed. The listener check verified that the redirect proxy carries the redirect URL map, and separately that a rule listened on port 80, but never that the two were connected: a port-80 rule aimed at the **HTTPS** proxy passed every check while serving the application in cleartext. The rules were deployed after the frontend, the image and the manifests, so a freshly published client was briefly live against whatever rules the project carried — on a first deployment, none. And the script printed "Deployment completed successfully!" on the strength of the preflight alone: it had verified that resources were *configured* correctly and never that the system *behaved* correctly. | The verification adds three network calls and a bounded readiness wait to every deployment, and `curl` becomes a prerequisite — asserted in the preflight so a missing tool stops the run while stopping is still free. A verification failure now aborts a deployment that has already published, which is why the rollback procedure is printed rather than referenced, and why it names the two reverts that are unsafe alone. |
| R51 | H-9 (order-1 security review): the deployment aborted on a defect in a file this change set may not modify | Report and continue: the `frontend/src/services/collaboration.ts` document-reference check emits a loud warning through a new `warn()` helper instead of calling `fail()`. | Keep it as a hard gate and accept that no deployment can pass; remove the check entirely; fix `collaboration.ts`. | The pattern the gate rejects is present in that file **right now**, and DEV-8 records the module as reference-only, so the gate had no passing state that did not involve editing the one file that may not be edited — it moved the failure from the browser to the deployment without closing anything. Removing the check would lose the signal. Fixing the file is the actual remedy and it is out of scope. Nothing is exposed by continuing: an invalid document reference is rejected by Firestore on path arity before any rule is evaluated, so the outcome is a broken feature, never an unauthorized read, and the rules fail closed regardless. | A real regression in that file now warns rather than blocks. Accepted for as long as the module is frozen; `warn()` is deliberately reserved for exactly this situation — a defect in a file the change set cannot touch — so it does not become a general escape hatch. |
| R52 | H-10 and H-7 (order-1 security review): fourteen advisories were suppressed by a bare list of identifiers, two CI steps could not fail, and one of them installed seven guessed package versions | Justify each advisory individually with package, pinned version, published fix version and why the fix cannot be taken; pin `pip-audit` itself; pin every action in the `security-checks` job to a commit SHA; remove `continue-on-error` from both steps; and **delete** the guessed-dependency step rather than repairing it. | Keep the shared comment and add a review date; move the exceptions into a separate file the workflow reads; pin the guessed versions properly and keep the step. | A shared comment over a block of identifiers cannot be checked: nothing tied an identifier to a package or to a reason, so an entry whose fix had *become* reachable was indistinguishable from the rest and would be renewed forever. Individually justified, the review question becomes concrete — 13 of the 14 read "requires Python >=3.10", so they are one decision (the runtime) rather than fourteen, and the list names that as its own exit criterion. `ecdsa` PYSEC-2026-1325 is the only standing decision, having no published fix on any runtime. `continue-on-error` made a step decorative: a real high-severity advisory and the known blocker produced the same green result. And the interceptor step performed, inside the job that exists to detect supply-chain risk, exactly that risk — `npm install --no-save` on seven unreviewed versions, in a workflow holding the repository contents. Pinning them would have kept a step that needs a dependency tree to assert something now asserted against source text, which needs none. A separate exceptions file was rejected as a second artefact where the workflow is already the authorised one. | The exception list is longer to read and must be curated when the manifest changes. The `>=3.10` claims were verified by resolving each fix version against Python 3.9 rather than read from a changelog, and the reproducing command is recorded in the step, so a stale claim is falsifiable. Actions are pinned in the `security-checks` job only: the `build` job is outside this change set and still references `actions/checkout@v2`. |
| R53 | H-7 (order-1 security review): the frontend audit step could never report anything | Make the step assert **which** state the repository is in: the exactly-documented blocker passes, a fully-cleared manifest runs the real audit as a gate, and **anything else fails**. | Keep `continue-on-error`; skip the step with an `if` condition; delete it until the manifest is fixed. | `continue-on-error` and a skip condition are the same outcome dressed differently — neither can tell you when the blocker was fixed and the step quietly began doing nothing useful. Asserting the state makes the exception self-expiring: it becomes a real gate with no edit to the workflow the moment `firebase` and `react-scripts` are declared and a lockfile is committed. A partially-fixed manifest is refused deliberately, because `npm audit` needs the declarations and the lockfile **together**, so half a fix leaves the step unable to audit while looking able to. | The step fails on a state nobody intended, such as a lockfile committed before the declarations. That is the point, and the failure message states both halves of the fix and where the exception is recorded. Verified behaviourally across six states, including fully-fixed-with-a-failing-audit, which is what proves it gates rather than reports. **D77 refines the lock-file half of this condition**: it is probed with `git ls-files` rather than `test -f`, so an untracked lock file left by the documented local setup no longer takes the failure branch. |

### 3.1 Decisions taken while resolving the infrastructure-to-integration review

The review that prompted these found nineteen issues, five of them deployment blockers. The
pattern behind most of them is one thing: `infrastructure/terraform/main.tf`, `variables.tf`,
`scripts/deploy.sh` and the backend are edited independently, and until now nothing bound them
together — so a resource could be renamed while a script still instructed an operator to apply
it, a Terraform argument could be withdrawn while a script still asserted the value it produced,
and a variable could be documented as a security control while nothing read it. Several rows here
therefore record a **test** as part of the decision, because a decision that only two files agree
about is one edit away from being untrue again.

| ID | Decision | Alternatives considered | Why this choice | Risks it carries |
|----|----------|-------------------------|-----------------|------------------|
| D54 | Use `db_sslmode=disable` on GKE, and permit that value **only** when the resolved `DATABASE_URL` names a local endpoint. Supersedes D7. | Keep demanding `require`; use `verify-ca`/`verify-full` against the proxy; adopt the Cloud SQL Python Connector; refuse `disable` unconditionally as before. | D7 and the checks built on it were **backwards**. The Cloud SQL Auth Proxy presents a PLAIN TCP listener on loopback inside the pod and holds the mutually-authenticated TLS session to the instance itself. That listener offers no TLS and carries no certificate, so a client asking for TLS on it cannot connect at all: `require` fails the handshake, and the verify- modes additionally try to validate the local listener against the instance's certificate. `scripts/deploy.sh` was instructing operators to set the one mode that cannot work. Encryption is not lost — the proxy's session is what satisfies the instance's `ENCRYPTED_ONLY` — and the unencrypted leg never leaves the pod. The permission is gated rather than granted: `Settings._assert_database_transport_topology` refuses `disable` unless the resolved URL's host is `127.0.0.1`, `localhost`, `[::1]` or absent, so the plaintext mode cannot describe a network connection. | `disable` is now expressible, which is a wider vocabulary than before. Bounded three ways: the default is still `require`, so an unset value is never plaintext; the gate runs in the **constructor**, after the Secret Manager reads replace `DATABASE_URL`, so it sees the URL the process will actually connect to rather than an environment placeholder — pinned by an AST test asserting the call's position; and `scripts/deploy.sh` asserts the same mode-and-locality pair against the manifests and the secret before publishing. The gate is one-directional by design: `require` against a loopback host is **not** refused, because a developer running local PostgreSQL with TLS is a legitimate topology. |
| D55 | State that `auth_enforcement_enabled=false` admits a request carrying **no** `Authorization` header at all. Corrects D18's stated effect. | Leave the description as it was; change the code to require a token even when disabled. | `SECURITY.md` said "endpoints are **not** reachable without a token", and that is false: `_resolve_current_user` reads the switch **after** credential extraction — deliberately, so the switch can serve the no-credential lockout it exists for — so `token is None` returns `_anonymous_caller()` rather than raising. Documentation that understates a break-glass switch's blast radius is worse than no documentation, because an operator weighs the risk from it. Changing the code was rejected: a switch that cannot admit a credential-less request cannot resolve the lockout it was added for. | The honest description is alarming, which is the intent. Mitigated as before: the default is `True`, every admission is logged at warning level and carries `X-Auth-Enforcement-Bypassed: true`, and the documentation now names reverting the client interceptor as the preferred lockout remedy instead. **SUPERSEDED.** The switch does not admit a credential-less request: the bearer scheme refuses one before any application code runs, whatever the switch says. |
| D56 | Make the Cloud Function invoker policy an **assertion in the read-only preflight** and remove the revocation entirely. Supersedes D28 and R13. | Keep revoking and re-reading; move the existing revoke-then-verify pair earlier; assert only after publication as before. | Two independent reasons. First, position: the revocation ran after the frontend was published, the image pushed and the manifests applied, so a function that stayed publicly invocable was discovered with the release already live — a check is only a gate if it precedes what it gates. Second, and decisive: Terraform now declares the invoker role with an **authoritative** `google_cloudfunctions_function_iam_binding`, so applying it removes any `allUsers` member; there is nothing left to clean up. A script that revokes a binding and then re-reads it can never report the finding — it reports only whether its own command worked. Asserting keeps the preflight genuinely read-only and makes it a gate on the state Terraform produced. | If an operator applies Terraform incorrectly the script now refuses to deploy rather than repairing the policy itself. That is the intended trade: a refusal is visible, a silent repair is not. The assertion also fails when the runtime is **not** an invoker, catching an authoritative binding that names no reachable member — safe, but still broken. Pinned by a **positional** test asserting the check's offset precedes the mutation-boundary marker. |
| D57 | Restore the two-account identity topology, and enforce the split on both planes rather than documenting it. | Keep the single account and reword the documentation to match; keep two accounts but check only that both variables are set. | The signing path is IAM impersonation: `file_storage._signing_arguments` presents `Settings.signer_service_account` as the signer together with the **runtime's own** access token. So the `serviceAccountTokenCreator` grant runs FROM the runtime TO the signer — the runtime is the member, the signer is the resource. Collapsing to one address makes it a grant from an account to itself, which authorizes no caller while appearing present in the policy, and simultaneously attaches the signBlob surface to the identity that holds every runtime permission. It also left the cluster requesting `GKE_METADATA` with no Workload Identity pool, a combination that cannot apply. Enforced rather than documented: a Terraform precondition refuses equal addresses, `scripts/deploy.sh` requires both `RUNTIME_SERVICE_ACCOUNT` and `SIGNER_SERVICE_ACCOUNT` and refuses them equal, and a test parses the grant's **body** to confirm its member and resource are the two different accounts. | Two accounts to provision instead of one, and one more required deployment input. The signer's own permissions are reduced to object **read** on the uploads bucket, so a compromise of the signing identity no longer carries write or delete. **SUPERSEDED by R36.** The runtime and the signer are one account, because `file_storage.py` signs as the address its own ambient credentials report — a separate signer account could never be the account a signature is minted for. |
| D58 | Grant the runtime `roles/firebaseauth.viewer`, rather than dropping `check_revoked=True`. | Drop revocation checking to remove the dependency; grant a broader Firebase role; leave the grant to the operator undocumented as before. | R4 adopted `check_revoked=True` without the IAM grant it needs. The failure mode is inverted and worth stating precisely: the extra Identity Toolkit lookup needs `firebaseauth.users.get`, and without it the lookup raises a provider error, so the API refuses **every VALID credential with the frozen 401** — a hardening measure became a total outage, and one that no invalid-credential test would ever catch. Dropping revocation would reopen the replay window R4 closed. `roles/firebaseauth.viewer` is read-only: it cannot create, disable or delete an account. | Every authenticated request now depends on a second Google service and carries one more round trip. Recorded as a residual with its total-outage failure mode named, asserted by the deployment preflight, and pinned by a test binding `check_revoked=True` in `security.py` to the IAM member in `main.tf`. |
| D59 | Keep `required_providers` with `~> 7.0` declared, and correct D26 rather than withdrawing the constraint again. | Withdraw the constraint to match D26's text; pin an exact provider version; keep the constraint and leave D26 unamended. | The constraint is present in `main.tf` and D26 asserted it was withdrawn, so one of the two had to move. Keeping it is right: the configuration uses provider features whose absence would fail at apply rather than at validate, and `terraform init` resolves `hashicorp/google v7.43.0` against it cleanly. Amending the row is the honest direction — the record follows the code, never the reverse. | A major-version constraint still admits minor drift, which the lock file would pin. Unchanged from R28's position and carried as a follow-up. |
| D60 | Wire `var.storage_class` on both buckets and correct D27, rather than restoring it to `NOT REFERENCED`. | Unwire it to match D27's text; leave both statements as they were. | The variable **is** referenced, on `static_assets` and `user_uploads` alike. D27's inventory of what is documented-as-unread is therefore wrong about it, and an inventory that is wrong about one entry cannot be trusted about the other four. Correcting the row keeps the inventory usable. | None to behaviour: the variable's default equals the class the buckets took before, so an apply that sets nothing provisions what it did before. |
| D61 | Describe the dependency-audit baseline as it is implemented — fourteen inline `--ignore-vuln` flags — and correct D29. | Refactor `ci.yml` to introduce the `AUDIT_BASELINE` value D29 describes; leave the row unamended. | D29 records "inline as an `AUDIT_BASELINE` value" and explicitly lists "inline the `--ignore-vuln` flags in the command" as a **rejected** alternative — but the rejected form is what shipped. The mechanisms are equivalent in every property D29 argued for: the baseline is committed, reviewable and diffable, adds no artifact, and lists the fourteen measured identifiers. Refactoring working CI to match prose would be change for its own sake, so the prose was corrected instead. | The identifiers are repeated per flag rather than named once, so adding one means adding a line. The step comment still carries the re-measurement instruction. |
| D62 | Describe the document `<meta>` policy as loading-only, and correct D31. | Re-introduce a build-time API-origin substitution; delete the meta policy. | The committed policy names neither `connect-src` nor `default-src`, and its own contract comment says it governs loading only — so nothing renders an API origin into it and D31 describes a mechanism that is not there. Loading-only is also the safer shape for a policy a browser cannot be told to report rather than enforce: because a `<meta>` element cannot carry `Content-Security-Policy-Report-Only`, an over-narrow document policy would block API calls in every configuration with no way to defang it, and the effective policy is the **intersection** of everything the browser receives. Omitting `connect-src` from the document means the served header alone decides where the application may connect. | The document policy no longer contributes to `connect-src` defence in depth; the header delivery points carry it alone. Stated wherever `csp_report_only` is documented, so nobody expects report-only to reach the document. |
| D63 | Delete the `https_cutover_enabled` variable and make the port-80 redirect unconditional, rather than wiring the two-stage cutover it described. | Wire the conditional topology so the variable takes effect; leave the variable and the script's two-stage logic in place. | The variable had **zero** reads: `main.tf` created the port-80 rule unconditionally while `variables.tf` documented a staged apply and `scripts/deploy.sh` implemented one, treating the rule's absence as expected. An input that is declared, described as a security control and never read is worse than absent, because an operator can satisfy it and change nothing. Wiring it was rejected: a deployment without the rule leaves port 80 with **no listener**, which a client experiences as a connection failure on the plaintext URL rather than an upgrade to TLS, and nothing guarantees a later apply ever adds it. Sequencing belongs to DNS and to the certificate-ACTIVE gate, which the script already enforces. | An operator following older notes will look for a toggle that is gone; a tombstone comment in `variables.tf` records why. The script now also asserts that the port-80 proxy serves the **redirect** url map, closing a gap the toggle's presence had hidden: a rule pointing at the application's own map would have served every response in cleartext on the same address. |
| D64 | Restore owner-only Firestore delete, and pin the authority in two places. | Keep collaborator delete and amend the contract to match; leave the divergence and rely on the emulator check. | The rules file's own authority table states the collaborator's authority as read plus a content-confined update, and `firestore_rules_emulator_check.py` expects a collaborator delete to be **denied** — the rule was the only artifact that disagreed, and it granted every shared user the power to destroy work it does not own, irreversibly and with no owner approval. Amending the contract to match the rule would have ratified that. Pinned twice because the emulator check needs a running emulator: a static test asserts the committed rule text is exactly `allow delete: if isOwner();`, and a second asserts the rule and the emulator expectation still agree. | A collaborator who needs to remove a workbook must ask its owner. That is the documented contract. Verified behaviourally: the emulator reports `collaborator delete -> denied` and `owner delete -> allowed`. |
| D65 | Prove the API origin terminates TLS with a live handshake, rather than reading the configured origin's text. | Keep the `https://` prefix check; declare an API ingress in Terraform; publish the API through the existing edge. | Every prior check read TEXT, which states what the deployment intends rather than what the endpoint does — an origin configured as `https://` that in fact answers plaintext, or answers with a certificate for another name, published successfully and sent every bearer token into the open. Publishing the API through the existing edge is not available: `google_compute_url_map.excel_app` declares one default service and no host rule, so every path on that origin resolves to the static bucket. Declaring a new ingress would mean owning the API's Kubernetes Service or Ingress, which this configuration does not. So the ingress is named an **external prerequisite** and the script proves it: `openssl s_client -verify_hostname -verify_return_error` makes the handshake and the name check one operation, and a plaintext probe accepts only "nothing listening" or a redirect to https. | The check needs `openssl` and `curl` on the operator's machine. It **fails closed** when either is missing rather than skipping, because skipping would report a verified endpoint having verified nothing. |
| D66 | Keep the three blunt source-text guards strict and reword the prose that tripped them. | Relax each guard to tolerate occurrences in comments; delete the guards. | Four separate comments I wrote — naming `api_origin`, `get_settings()`, `make_public` and `--allow-unauthenticated` — failed tests that assert those literals appear nowhere in a given file. The guards are blunt on purpose: a substring search catches a re-added call anywhere, including inside a string or an `eval`, which a syntax-aware check would miss. Trading a real control for the freedom to name a token in a comment is the wrong direction, so the comments were reworded. | Future contributors will hit the same friction, and the failure message does not explain it. Recorded here so the next person recognises it: `api_origin` may not appear under `backend/app/`, `database.py` must contain exactly one `get_settings()` literal, `make_public` may not appear in `file_storage.py`, and `--allow-unauthenticated`, `target-http-proxies create` and `--ports=80 ` may not appear in `scripts/deploy.sh`. |
| D67 | Report the reviewed `api.test.ts` type error as **not reproducing**, and still reshape the function. | Claim the error was found and fixed; change nothing and record the finding as invalid. | The finding named a `TS2322` at the `return` of `apiSource()`. It does not exist: the committed file type-checks with **zero diagnostics** under `frontend/tsconfig.json`'s own options against the declared `typescript@4.9.5`, and a minimal reduction of the pattern is clean too — TypeScript narrows a module-scope `let` when the guard, the assignment and the reference share one function. Claiming a fix for it would put a false statement into this record and into anything written from it. The concern behind the finding is still sound, though: the return depended on narrowing that holds only because those three statements happen to share a function and would stop holding if any moved or the function became `async`. The reviewer offered "a narrowed local value or `transpiledApiSource!`"; the local was taken, because the assertion would suppress the check that reports a real future problem. | None to behaviour — transpile once, cache, reuse, unchanged. The finding is recorded as not reproducing so nobody looks for a defect that was never there. |
| D68 | Remove the `slowapi==0.1.10` pin from `backend/requirements.txt` entirely, and replace the annotation beside it with one that points here. | Keep the pin and correct only its comment; keep the pin and re-introduce a slowapi-based tier so the pin becomes true; remove the pin silently. | R32 replaced slowapi's application-limit tier with `limits`, because the shape the plan authorised could not deliver a global ceiling without a `request` parameter on every handler — which the change set may not add. R32 recorded that reversal but left the pin in place, describing it as "pinned but no longer imported", and the annotation beside it said the code used it. So the manifest asserted the opposite of what `TestSupplyChainGates::test_the_manifest_declares_nothing_no_module_imports` already proved. A pin with no importer is not inert: it ships its transitive surface into the runtime image and widens the audit surface for no control in return. Re-introducing a slowapi tier to make the pin honest would undo R32 for a cosmetic reason. Removing it silently would leave the AAP's §0.7.1 pin unexplained, which the explainability rule treats as a defect. | This is a **logged deviation from AAP §0.7.1**, which pins slowapi by name for V9. The control V9 required is delivered — by `limits`, which is pinned and imported for both tiers — so the deviation is in the mechanism, not the outcome. Measured effect: 89 pins to 88, 23 direct to 22, and `pip-audit` unchanged at 14 advisories because slowapi carried none of them. The count appears in the onboarding guide and the traceability matrix, both updated in the same change. |
| D69 | Add a `terraform fmt -check` and `terraform validate` gate to CI, **scoped** to `main.tf` and `variables.tf` copied into an empty directory, followed by an assertion that the pre-existing `outputs.tf` baseline is unchanged. | Validate the real directory and accept a permanently red step; mark the step `continue-on-error`; keep asserting the `.tf` files as text only; repair `outputs.tf` to make an unscoped gate pass. | Every cloud-side control here is delivered only by `terraform apply`, and every assertion this suite made about the `.tf` files read them as **text**. A configuration Terraform refuses to *load* therefore passed all of them while delivering none of the controls — which is exactly what an unescaped `${var.project_id}` inside a `variable` description did, and no test saw it. Validating the real directory is not an option because it fails on `outputs.tf`, a file outside this change set, and a gate that is red on every run is one the team learns to ignore; `continue-on-error` produces the same outcome more quietly. Repairing `outputs.tf` would be a scope breach. Scoping plus a baseline assertion keeps the gate green on a correct tree, red on a broken one, and self-retiring: when `outputs.tf` is repaired the baseline assertion fails and the scoping can be removed. | The gate cannot say an `apply` succeeds, only that the configuration loads and is well formed — a live project is needed for the rest. **Verified in both directions rather than assumed:** the step body was executed against the current tree (`FMT OK`, "Success! The configuration is valid.", `OBSERVED=7 OTHER=0`, exit 0) and against the reconstructed pre-fix tree, where it fails with "Variables not allowed … variables.tf line 100" — so it demonstrably catches the defect that motivated it. |
| D70 | **Superseded by `D85`, which closed the disclosure this entry tracked. Retained because it is the reason the defect survived to be closed rather than being quietly deleted.** Restore a **tracked disclosure** marker above the `detail=str(e)` line in `cells.py` and `collaboration.py`, and pin it with a test, rather than restoring the deleted developer bullet lists wholesale. | Restore every deleted bullet verbatim; restore nothing and rely on the residual list; fix the disclosure itself. | Four route modules had their `HUMAN ASSISTANCE NEEDED` bullet lists deleted while this change set closed only *one* of the items in them — authentication. Most of the rest were feature requests (pagination, caching, logging) which belong in the follow-up table, not in the source. But one was not: the marker sitting directly above `raise HTTPException(status_code=500, detail=str(e))` in `cells.py` described a defect that is **still present**, and deleting it removed every trace of a live CWE-209 disclosure from the module — a reviewer reading the file afterwards saw clean code. `collaboration.py` carries the identical defect and lost the same kind of bullet, so both were restored. Fixing the disclosure itself is not available: the handler body is business logic the change set may not edit. The marker is phrased as `KNOWN OPEN DEFECT, tracked as follow-up F7` rather than a bare `TODO`, so the defect is visible at the site without re-introducing an untracked placeholder against the zero-placeholder policy. | **Outcome, recorded after the fact:** the mechanism worked exactly as designed and is the reason this entry is now superseded rather than still open. At the time, the defect remained open and an authenticated caller could still receive a driver or constraint message; what this decision fixed was its **visibility**, by requiring the marker within six lines of the offending line so that the regression which had already happened once — marker deleted, defect kept — could not recur silently. The test was written to retire itself, failing with instructions to delete the residual if the disclosure were ever actually fixed. That is what happened in the runtime-profiling round: `D85` closed the disclosure, the test failed with those instructions, and the test, residual 32 and follow-up `F7` were all removed together. The other three modules' deleted bullets are accounted for here rather than in source: authentication is closed (V1), the disclosure is closed, and the remainder are tracked as F2 and F27. |
| D71 | Classify `401`, `403`, `429` and `503` in the client response interceptor and expose a user-facing message, `reauthenticate` flag and `retryAfterSeconds` for callers to consume. | Leave the interceptor status-blind and let each page invent its own message; surface the server's response body; retry automatically on `429`. | The server distinguishes these outcomes deliberately — D38 keeps `401` and `503` separate precisely so a verification *outage* is not reported as a rejected credential — and the client discarded that distinction, rendering "Failed to load workbook" for all of them. With `check_revoked=True` an expired credential is an expected outcome rather than an edge case, so the most common failure was also the least explicable. Surfacing the response body was rejected outright: it would carry server-side exception text into the interface, which is the exposure residual 16's own `detail=str(e)` defect already represents. Retrying automatically on `429` was rejected because it converts a throttle the operator configured into load the operator did not. | The message is derived from **the status code alone** — no body, header value or exception text reaches it — which is asserted by test, so this path cannot become a disclosure channel. Two disclosed gaps remain, both blocked by the markup freeze and recorded as residuals 17 and 18: the strings sit in containers with no `role="alert"`, so assistive technology is not told the session ended, and `Dashboard.tsx` has no visible error surface at all, so its classification is computed and never shown. |
| D73 | Q-1 (final TESTS verification: three wildcard regressions in the entry point's CORS call left the suite green): make the probe application **derive** its cross-origin policy from `backend/app/main.py`'s syntax tree, and assert the four keywords against that file, rather than restating the policy in the test module. | Add the four assertions and leave the probe's hardcoded literals in place; assert only the registration order, as before; import `main.py` and read the middleware stack from the application object. | The literals were the defect, not the missing assertions: `_build_application` composed its own CORS configuration, so the five behavioural cross-origin tests validated the test module's copy of the policy and could not see `allow_methods=["*"]`, `allow_headers=["*"]` or `allow_origins=["*"]` in the entry point — each of which was measured leaving all 556 cases green. Deriving the values instead fixes the whole class rather than this instance: the 21 places that compose the probe now exercise whatever `main.py` configures, so a future keyword change is exercised without anyone remembering to mirror it. Importing the entry point is not available — `backend.app.main` cannot be imported in this tree, which `TestKnownResiduals` pins. | The derivation adds a parse of `main.py` to a helper 21 tests depend on, so a syntax error there fails many tests at once rather than one. Deliberate: that file failing to parse is a deployment-stopping fault. A keyword written in a form the resolver does not understand raises rather than being skipped, so the policy cannot become partly unasserted without a failure saying so. |
| D74 | Q-2 (a delivered control with no behavioural test: neutering the 10 MiB request-body ceiling left the suite green while a constant assertion looked like coverage): test the **413 itself**, in both framings, on a probe route that reads its body. | Keep the constant assertion only; assert the declared-`Content-Length` framing alone; delete the control as untested. | The constant test asserted the value the middleware is constructed with and nothing about what it does, which is why total removal of the enforcement was invisible while halving the constant failed. Both framings are needed because they take different paths: a declared length is refused before the body is read at all, while an undeclared one is bounded as it streams — and the streaming counter only fires when the application reads, so a probe route that ignores its body returns 200 for an oversize chunked body. That is why the probe application gained a body-reading route rather than reusing an existing one. | The chunked cases depend on the test client sending a generator body as `transfer-encoding: chunked`, which was verified by recording the ASGI scope rather than assumed. The refusal-before-entry case is asserted with a handler-invocation counter, which is what distinguishes the declared check from the stream check — without it, disabling the declared check alone stayed invisible because the stream check still produced a 413. |
| D75 | Q-4 (the break-glass test asserted nothing about its subject): make the test **present a real token** and assert the request is admitted with the bypass marker, rather than asserting a credential-less refusal a sibling already covered. | Rename the test and correct its docstring to what it actually verified; delete it as a duplicate; leave it and rely on the sibling that caught the mutation. | Its name and docstring claimed the unverified-claims read was exercised; its body sent no `Authorization` header, so the bearer scheme refused before the dependency body ran and its three assertions were byte-identical to two siblings. Deleting the whole bypass branch left it passing. Renaming would have been honest but would have left the admitted path — the marking at the end of the lookup, the warning, and the contextvar the marker middleware reads — with no test at all. The token is signed with a key nothing trusts and `verify_id_token` is stubbed to refuse anything it is given, so an admission can only have come from reading the claims unverified. | The test now depends on the identity fixture and the marker middleware together, so it fails for more reasons than the read itself. Each of those reasons is a control this row exists to protect, and the sibling covering the no-credential path is unchanged. |
| D76 | Q-3 / Q-5 (the Decision Log cited a test that does not exist and described a CI mechanism that is not in the workflow): **resolve documented references against the artifacts they name**, with two tests, rather than only correcting the two claims. | Correct the two claims and stop; add a review checklist item; extend the existing documentation tests' hardcoded value list. | Both claims were verifiable and neither was verified, which is the failure the existing documentation tests exist to prevent — they pin counts, header values and switch defaults, and nothing pinned that a cited test node ID resolves or that a named workflow value exists. The corrections alone would have left the next citation unguarded. The two guards are cheap and general: every `Class::test_name` in the five documents is resolved against this module's syntax tree, and every `env:` value the documents attribute to the workflow is resolved against `ci.yml`. | A document may no longer quote a superseded reference verbatim, because the guard cannot tell a citation from a quotation. The two corrected rows therefore describe the false claims instead of reproducing them, and say so — a small loss of literal fidelity in exchange for the drift being caught mechanically. |
| D77 | Q-6 (the frontend audit gate keyed on filesystem presence, so it failed for anyone who followed the documented local setup): probe the lock file with **`git ls-files`**, and treat an untracked one as its own third state. | Keep `test -f` and extend the failure message to distinguish the two cases; drop the lock-file half of the condition; ignore it as a local-only nuisance. | What blocks `npm audit` is that no lock file is **committed** — every document says so — while the documented setup, `cd frontend; npm install`, writes an untracked one. The filesystem probe therefore answered a different question from the one the gate asks, and a developer reproducing the security job locally got a failure whose remedy message described a state that was not theirs. CI was never affected, because `actions/checkout` restores tracked files only; keying on tracked state makes the two agree. `Untracked` is a distinct value rather than a synonym for absent so the failure branch can name which state it is refusing. | The step now depends on `git` being on `PATH` and on running inside a work tree. Both hold wherever this gate is meaningful: `actions/checkout` leaves a repository, and a local run is by definition inside a clone. An export with no `.git` reads as "no committed lock file", which is the same verdict the CI checkout gives. Verified in four states: undeclared with an untracked lock file and undeclared with none both pass with an accurate message, declared without a committed lock file fails naming both halves, and declared with a committed lock file promotes to the real `npm audit --audit-level=high`, which reports 38 advisories and exits 1. |
| D89 | Validate `CSP_HEADER_NAME` and `CSP_CONNECT_SRC_API` in a script the nginx entrypoint runs **before** the template is rendered, refusing to start on any value that would remove or truncate the policy. | Validate inside `nginx.conf` itself; document the constraint and rely on operator discipline; write the guard inline in the Dockerfile with `RUN printf`; drop the two variables and hardcode the policy per environment. | nginx has no conditional that can reject an environment value, so the template itself cannot check anything — an illegal value renders successfully and `nginx -t` reports no error. Two values were shown to weaken the container silently: a header name that is not one of the two policy names emits a header no browser enforces, so the policy disappears while five of the six canonical headers remain; and an API source carrying a quote and semicolon ends the `add_header` directive early, truncating the policy and appending an attacker-chosen header. Documentation is not a control, and the Dockerfile-inline form would be neither reviewable nor testable — as a committed file the guard is checked by `dash -n` and `shellcheck` and exercised directly. Hardcoding the policy per environment would remove the report-only rollback the `csp_report_only` toggle exists to provide. The grammar applied is the one `infrastructure/terraform/variables.tf` already enforces on `var.api_origin`, so a value accepted here is accepted there. | One artifact beyond the planned map, recorded as DEV-16. A legal value now has one more place it must satisfy, so an operator who mistypes the origin gets a container that refuses to start rather than one that serves a policy blocking every API call — the louder failure, deliberately. The guard runs only in the container path, which is not the live SPA delivery path, so it hardens an artifact the Terraform edge does not use; the edge's equivalent values are hardcoded literals no input can reach. |
| D90 | Register the 10 MiB request-body ceiling **unconditionally**, so `rate_limit_enabled=false` no longer removes it, and name the ceiling in the disabled-throttling warning. | Leave the coupling and only document it, which is what the finding's suggested fix offered first; give the ceiling a switch of its own; move it out of `rate_limit.py` into its own module. | The switch is named for the two request-**count** tiers and its documentation describes only those, so removing a third control was undocumented behaviour rather than a documented trade-off — and the two controls bound different things: the tiers bound how many requests a client may make, the ceiling bounds how much work one request may demand, so no operator disabling throttling is asking to accept unbounded bodies. Documenting the coupling instead would have published a switch that withdraws a control it does not name, which is the weaker of the two remedies on offer. A switch of its own was rejected as new surface for a control that has no legitimate reason to be off, and moving the module was rejected as a refactor the finding does not require. Registration order is unchanged for the enforcing case: the ceiling is still added before the tiers, so it still sits inside them and a `413` still leaves through the CORS and security-header wrappers. | The ceiling can no longer be turned off by configuration at all, so an operator who genuinely needs to accept a body over 10 MiB must change `_MAX_REQUEST_BODY_BYTES` and redeploy. That is deliberate: the value is a code constant pinned by a test, not an operator dial. A window expression that fails to parse now raises after this middleware has been added rather than before, which changes nothing observable — the raise propagates out of `register_rate_limiting`, so the application does not start either way. |
| D91 | State the `db_sslmode` contract in one form on every plane that describes it, and correct the operator-facing text that contradicted it - including `scripts/deploy.sh`'s two `DATABASE_URL` failure messages, one of which offered a Unix-socket URL a later gate refuses and the other of which instructed the operator to "set db_sslmode=verify-full", a value `Settings` does not accept. | Correct only the three documents the finding named and leave the script's messages alone; widen the code instead, so `Settings` accepts the modes those messages promise; add the socket URL form to the URL grammar so the first gate's offer becomes true. | The contract is `Literal["disable", "require"]`, with `disable` admissible only when the resolved `DATABASE_URL` names a loopback host, and it was stated across six artifacts of which five carried at least one statement the code refutes. A reader reconciles a set like that by hand and then distrusts all of it, so the remedy is one statement repeated rather than three corrections. The script's messages were included because a failure message is read at the moment an operator is deciding what to do next, so one naming an unaccepted value sends them to a start-up crash - the same defect in the place it does most harm. Widening the code was rejected outright: `verify-ca` and `verify-full` cannot complete against the proxy's certificate-less loopback listener, so accepting them would trade a wrong message for a broken connection. Adding the socket form to the grammar was rejected as a functional change no finding asks for, and one that would require `validate_database_url` to stop demanding a host - the check that makes the loopback guarantee provable. | Six artifacts now repeat one statement, so a future change to the contract has six places to reach and any one left behind is a fresh contradiction. No test asserts the phrasing, so the guard against drift is that `.env.example` holds the canonical form and the others restate it; the two behaviours underneath it - the accepted value set and the loopback condition - are asserted by `TestConfigurationContract`. |
| D72 | Report the `frontend/public/index.html` deletion as a **deliberate, now-logged** removal rather than reverting it. | Revert the deleted block; leave it unlogged. | The file's `HUMAN ASSISTANCE NEEDED` comment and commented-out Google Tag Manager snippet were removed while adding the security meta elements, and that removal was never recorded — an unexplained deviation, which the explainability rule treats as a defect regardless of whether the change itself was right. The change *was* right, and for a reason specific to this work: the file's new Content-Security-Policy sets `script-src 'self'`, which refuses the GTM snippet outright, so re-enabling it by uncommenting would produce a silently blocked script and an inexplicable console error. A commented-out block that cannot work under the policy the same file now carries is a trap, not a convenience. | Anyone wanting analytics must add the host to `script-src` in **all three** header producers — the API middleware constant, `nginx.conf` and the Terraform local — because a browser applies every policy it receives and the effective policy is their intersection. That is a larger change than uncommenting a line, and this entry exists so the next person discovers that before starting rather than after. |

### 3.2 Decisions taken while resolving the final runtime-verification report

The order that prompted these did not read source - it drove the controls, with real HTTP requests
against the real route modules and a real PostgreSQL 13 over TLS. That changes what its findings
are: several describe behaviour nobody had written down rather than a defect, and two are the
framework doing exactly what its documentation says. Recording those explicitly matters as much as
recording the declines, because an observation that is examined and found harmless will otherwise be
raised again by the next reader, and answered again from scratch.

Every entry below was measured before it was written. Where a measurement depended on a stand-in
for one of the four absent service classes, that is said in the row rather than presented as
application behaviour.

| ID | Decision | Alternatives considered | Why this choice | Risks it carries |
|----|----------|-------------------------|-----------------|------------------|
| D92 | Record the two CORS response-header observations as **framework behaviour rather than defects**, and change nothing. | Strip the four safelisted names from `Access-Control-Allow-Headers`; suppress the `Access-Control-*` block on the disallowed-origin refusal; leave both unrecorded. | Both are Starlette's, and both are inert. `SAFELISTED_HEADERS = {"Accept", "Accept-Language", "Content-Language", "Content-Type"}` at `starlette/middleware/cors.py` line 12 is unioned into the allow-list at line 58, so the four names appear whatever the application configures - and all four are CORS-safelisted, meaning a browser sends them with or without the header. Suppressing them would mean reaching into a third-party middleware's construction to remove a permission browsers grant anyway. The refusal is the more important of the two to get right: it carries `Allow-Credentials`, `Allow-Headers`, `Allow-Methods` and `Max-Age`, and it does **not** carry `Allow-Origin` - which is the only one of them a browser reads to grant access. A pre-built header block returned alongside a `400` grants nothing. Leaving both unrecorded was rejected because the next reader measures the same thing and has to re-derive the same conclusion. | None to behaviour. The risk is in the record rather than the code: a future Starlette version could change the constant or the composition, and nothing here would notice. Bounded by the CORS assertions already in the suite, which test the *outcome* - exact-origin echo, never `*`, and the fixed `400` with no `Allow-Origin` - rather than the header list. |
| D93 | Leave the request schemas permissive: no `extra = "forbid"`, and Pydantic v1 coercion as it stands. | Set `extra = "forbid"` on the schemas; add validators that refuse coercion; add `extra = "forbid"` to request use only. | Measured behaviour: an integer `id` becomes `"12345"`, an integer cell value becomes `"42"`, and an unknown field is dropped with no comment. Every one of those is Pydantic v1 doing what it documents, and `workbook_schema.py` sets no `extra` anywhere. Changing it changes a **request** contract that §0.1.2.2 freezes, and it changes it in the refusing direction: a body accepted today would start returning `422`, which is a compatibility break made for tidiness rather than for a threat. There is no injection consequence to weigh against that - a coerced value reaches SQL only as a bound parameter, and the audit of the emitted statements confirms it. A request-only variant was rejected because the same class is both the request body and the response model, so there is nowhere to put the setting that affects one and not the other. | Malformed input is accepted quietly, so a client bug surfaces as wrong data rather than as a `422`. Recorded as residual 33 with follow-up F38, and the residual is pinned by a test that retires itself if `extra` is ever set - so closing it cannot leave a stale residual behind. |
| D94 | Leave `WorkbookSchema` serving as both the request body and the response model of `POST /workbooks`. | Add a `WorkbookCreateSchema` omitting the server-assigned fields; make `id` optional; accept the shape and document it. | The measurement is unambiguous: `id`, `owner_id`, `worksheets`, `created_at` and `modified_at` are all required, and omitting `id` is refused with a `422` naming it - so a caller must invent a primary key and two timestamps for a row that does not exist yet. That is a real design defect. Both remedies are excluded, though, and by the same clause: a new schema and a changed `id` are each a change to the frozen request contract at §0.1.2.2, and `workbook_schema.py` is named in §0.9.2.1. Documenting it is what is left, and documenting it honestly is worth more than it sounds - the shape is invisible from the route signature, which reads `workbook: WorkbookSchema`. | A caller must send fields the server ignores, and cannot tell which. Residual 34, follow-up F39. Whether the server *overrides* the client's `id` could not be established: that behaviour lives in `WorkbookService`, which does not exist, so the harness stand-in supplied it and the row does not claim it. |
| D95 | Add no idempotency control to `POST /workbooks`. | A unique constraint on `(owner_id, name)` in `models.py`; an `Idempotency-Key` header or parameter on the route; a duplicate check inside the handler. | Confirmed absent at every layer: the `workbooks` table declares no unique constraint of any kind, neither the route nor the generated specification mentions an idempotency key, and an identical body submitted twice produced two rows with different ids. All three remedies are outside the permission - the constraint is in `models.py` (§0.9.2.1), the header is a route-signature parameter (§0.9.3), and the check is handler business logic. It is also not a security finding standing alone: a duplicated row is a correctness cost, and the volume a caller can create is already bounded by the write throttling tier, which is the control that exists for that. | A retried or double-clicked create leaves two workbooks. Residual 35, follow-up F40. Stated plainly so nobody reads the write tier as an idempotency guarantee - it bounds the *rate*, not the *effect*. |
| D96 | Leave `/openapi.json`, `/docs`, `/redoc` and `/docs/oauth2-redirect` reachable without a credential. | Set `openapi_url=None` and `docs_url=None`; gate them behind the authentication dependency; leave them open and unrecorded. | All four answer `200` with no credential, which is FastAPI's default and not something any application router declares. What they return is schema shape: the document was searched for the seeded account address, the database password and the signing key and carries none of them, and it still comes back with all six security headers. So the exposure is the route inventory itself, which an authenticated caller can enumerate anyway and which this repository publishes in its own documentation. Closing them means arguments on the `FastAPI(...)` construction in `backend/app/main.py`, and §0.9.1.2 scopes that file to the CORS, header-middleware and throttling wiring - the specification surface is not part of it. Gating them is worse than either: it needs a dependency on a route the application does not own. Leaving it unrecorded was rejected for the same reason as D73. | An unauthenticated reader learns the five operations and their schemas. Residual 36, follow-up F41, and pinned by a test asserting the endpoints are documented as open **and** that the code still leaves them open - so if one moves without the other, that fails rather than drifting. |


### 3.3 Decisions taken while resolving the runtime-profiling report

The round that prompted these profiled the running application under load: rising request
concurrency against a real PostgreSQL 13, backends terminated mid-flight, response bytes counted on
the wire, and a real browser driving the API cross-origin. That produced a different class of
finding from any earlier round. Nothing here is a coding mistake anybody could have seen by reading
the source; each one is a resource or coverage boundary that only appears once something is applied
at a rate, at a size, or in an order.

Two of them are consequences of earlier decisions in this same log, which is worth stating plainly
rather than leaving for a reader to notice. The authentication dependency (`D1`) doubled the pooled
connections a single request needs, which is part of why the pool ran out. And the middleware order
recorded in `main.py` put the cross-origin policy outside the throttling tiers, which is why a
preflight was never counted. Both were correct decisions on the evidence available when they were
made; both needed revisiting once the behaviour was measured.

Every number below was measured before it was written, and each is stated with the value it
replaced so the size of the change is checkable rather than asserted.

| ID | Decision | Alternatives considered | Why this choice | Risks it carries |
|----|----------|-------------------------|-----------------|------------------|
| D78 | Register the cross-origin policy **inside** the throttling tiers, reversing the order `main.py` used, so a CORS preflight is counted like any other request. | Keep the order and correct both docstrings to state that preflights are exempt by design, metering them at the load balancer instead; meter preflights in a separate middleware outside CORS. | `CORSMiddleware` answers a preflight itself and never calls the application inside it, so with the old order the tiers never saw one: measured `200 x 8` against a 3/minute ceiling, and `400 x 8` for a preflight from a disallowed origin. The surface is not marginal - because `Authorization` is not a CORS-safelisted request header, a browser sends a preflight for **every** authenticated call, and Chrome was observed issuing 7 preflights across 15 API requests. Metering at the load balancer was rejected because no load balancer exists in front of the API in this deployment, so it would have moved the control to a place that does not exist. | A preflight refused with `429` is a CORS failure by specification, so the browser reports the API call as failed rather than surfacing the throttling status - the page cannot tell "throttled" from "blocked". Accepted: bounding the surface is the point, and the alternative left it unbounded. The tiers' own short-circuit responses no longer travel back out through CORS, which `D79` addresses. |
| D79 | Give the throttling tiers' own refusals (`429`, `413`, `503`) the cross-origin headers directly, echoing the origin only on an exact allow-list match, and add `Retry-After` to `Access-Control-Expose-Headers`. | Let the refusals travel back out through CORS (impossible under `D78`); echo the origin unconditionally; leave the refusals without cross-origin headers. | Two coupled gaps. First, `D78` means a refusal is produced outside CORS, so without this a throttled cross-origin request would be unreadable to page JavaScript. Second - and this predates `D78` - `Retry-After` is not on the CORS response safelist, so the client seam that reads it to build its "try again in N seconds" message (`D71`) could **never** have read it from a browser: the value was permanently `undefined` cross-origin, and no test had caught it because no test drove a browser. The allow-list is captured **once at registration**, never per refusal, because `get_settings()` is uncached and performs three Secret Manager reads (residual 9) - reading it per refusal would have made a throttled client an amplifier for that cost. | A refused origin gets no `Access-Control-Allow-Origin`, so a disallowed origin cannot read its own `429`. That is deliberate: a refusal must not admit the origin it is refusing. |
| D80 | Attach an application log handler at bootstrap from a new `backend/app/core/logging_config.py`, gated so it never overrides a handler or level an operator configured. | Configure logging inline in `main.py`; rely on uvicorn's `--log-config`; emit the start-up confirmation at `WARNING` so `logging.lastResort` carries it. | Under a bare uvicorn the root logger sits at `WARNING` with no handler, so every `INFO` record the application emitted was dropped - including the one naming the window store the throttling tiers count in, which is the record that distinguishes a deployment enforcing the configured quota from one enforcing a multiple of it (residual 19, `D88`). Raising the severity of a purely informational record to make it visible would have been misreporting it. A separate module rather than inline code because `main.py` cannot be imported, so an inline bootstrap could only ever be asserted by reading its source; a module can be imported and driven. | Two independent gates (level raised only when `NOTSET`, handler attached only when nothing in the propagation chain has one) mean a misconfigured deployment could still silence the record - but that is the operator's own logging configuration doing what it says, which is the correct precedence. |
| D81 | Size the pool explicitly - `pool_size=5`, `max_overflow=15`, so `DB_POOL_CAPACITY=20` - and **shorten** `pool_timeout` from the 30-second default to 10 seconds. | Leave the defaults; raise the pool to match the 40-token thread pool; lower the thread pool to the pool. | At capacity 15 the measured failure rate was 47.3% at concurrency 120, with 322 `QueuePool ... timeout 30.00` records. Raising the pool alone only moves the cliff, because demand is bounded by in-flight requests rather than by pool size - which is why this pairs with `D82` instead of standing alone. 20 was chosen against PostgreSQL's `max_connections=100`, which leaves room for five workers at full overflow. The timeout was **shortened** on the reasoning that thirty seconds of waiting followed by a server error is the worst available answer: it consumes the client's patience and then tells it nothing. Ten seconds still absorbs a transient spike while failing fast enough to be a usable signal. | A deployment running more than five workers per instance will exhaust `max_connections` before it exhausts the pool. `F50` records the reconciliation duty; the capacity is a named module constant so the arithmetic is checkable. |
| D82 | Bound in-flight HTTP requests to exactly `DB_POOL_CAPACITY` with a new ASGI middleware, shedding excess with `503` and `Retry-After` after a bounded wait. | Raise the pool and hope; lower the anyio thread limiter; return `429` instead of `503`; queue without a bound. | Rate throttling bounds **arrival rate**, not concurrency, so a burst well inside the 600/minute ceiling passed the limiter untouched and then starved the pool. The mechanism was established rather than assumed: `get_db` is a synchronous generator dependency, so the Session - and, once it queries, the pooled connection - is held across the `await` boundaries where the thread-pool token is released and reacquired. Corroborated by `pg_stat_activity` showing **idle in transaction = 15, the entire pool**, with only 1-2 backends ACTIVE. Deriving the bound from the pool rather than from the thread pool is what makes it correct, and it is sufficient because each in-flight request holds at most one connection at a time - `_resolve_current_user` closes its own Session in a `finally` before returning, which a test asserts because the invariant depends on it. `503` rather than `429` because the client did nothing wrong: this is the server declining work, not the client exceeding a quota. | A slow endpoint can now fill the slots and shed requests a larger pool would have served. Measured: at concurrency 120 the bound never shed at all and throughput **rose** from 2.2 to 142.9 requests a second with zero failures, so the bound removed a failure mode rather than trading one for another. Shedding was demonstrated separately by driving 30 concurrent multi-second requests against 20 slots, which produced exactly 20 successes and 10 sheds. |
| D83 | Enable `pool_pre_ping` and set a finite `pool_recycle` of 1800 seconds. | Leave both at their defaults and treat the resulting `500` as acceptable; catch the driver error in the handlers; retry at the application layer. | A server-side close - failover, maintenance restart, an idle-connection reaper - handed the next caller an unhandled `500`: measured `{500: 1, 200: 14}` after terminating the process's five backends. A pre-ping makes the event invisible to the caller, verified as `{200: 15}` on the same scenario with zero `server closed the connection unexpectedly` records. Catching the driver error in handler bodies was rejected as both wider and worse - it would touch business logic and would still surface a failure the pool can prevent entirely. | A pre-ping costs one round trip per checkout. Not measurable against this workload, and the alternative is a user-visible error on an ordinary operational event. |
| D84 | Bound both page parameters on both list routes: `skip` gets `ge=0, le=MAX_PAGE_OFFSET`, `limit` gets `ge=1, le=MAX_PAGE_SIZE`, with the values in a new `backend/app/core/pagination.py`. Set `MAX_PAGE_SIZE` equal to the default of 100. | `le=1000` as the earlier follow-up proposed; leave `skip` with only a floor; bound the payload after the query instead of bounding the query; put the numbers in `Settings`. | `limit=1000000` and `limit=9223372036854775807` each returned **every** row in the table - 251 workbooks, 6,809,936 bytes, 13.4 seconds - and `GET /workbooks/{id}/worksheets` accepted no page parameter at all, so its response was bounded only by how many worksheets a workbook held. A ceiling equal to the default follows from the sizes involved: the default page already serializes to 2.7 MB, so no caller is entitled to ask for more than the route produces unasked, and `skip` remains available for paging. `skip` needed a ceiling too, which the report had not established: it measured `limit` at exactly `int64` max, which succeeds, and probing one value higher showed **both** parameters answering `500` above `int64` (`NumericValueOutOfRange`). One shared module rather than two copies so the two routes cannot drift and the documented maximum has a single definition; module constants rather than `Settings` fields because these are contract bounds, not operator thresholds. | Three behaviour changes a client could notice: `limit=0` now answers `422` where it previously returned an empty list; a caller that relied on a large `limit` to fetch everything must page; and the worksheets route now returns at most 100 worksheets where it previously returned all of them. All three are the intended effect, and the last is bounded by a `skip` a client already has. |
| D85 | Answer a failed cell update or share with a fixed message from a module constant, record the cause server-side with `logger.exception`, and re-raise a deliberate `HTTPException` **unchanged** ahead of the catch-all. | Delete the `try`/`except` and let `ServerErrorBoundaryMiddleware` answer; keep `str(e)` and redact known-sensitive substrings; return a correlation identifier to the caller. | A NUL byte in a cell value returned the psycopg2 message verbatim, and the share route returned its service's internal exception text - CWE-209 in both. Deleting the `try`/`except` was rejected because it would change the status the routes answer, which is a contract the report measured: the error boundary answers `500`, so `collaboration.py`'s `400` would silently become a `500`. The `except HTTPException: raise` clause is the part that is easy to omit and expensive to get wrong: without it a deliberate `404` from the service is flattened into the catch-all's status and wording, which is a correctness regression introduced by a security fix. Verified in both directions at runtime - the fixed message out, the full traceback with the driver text in the log, and a deliberate `404` passing through with its own wording. | The caller can no longer distinguish two different internal failures. That is the intent, and the log carries what a caller no longer gets: `ERROR` records naming the workbook and worksheet, with the exception. Whoever operates this must have access to those logs, or a support conversation has nothing to work from. |
| D86 | Compress responses with `GZipMiddleware` at `compresslevel=6` and `minimum_size=500`, registered inside CORS and the tiers and outside the marker and the error boundary. | The library default level of 9; level 1; compress at the edge instead; no compression. | The default page was served at 2,742,333 bytes with no content encoding whatever the caller offered. Measured on that exact payload: level 1 gave 31.8x in 7.3 ms, level 6 gave 74.8x in 16.9 ms, level 9 gave 110.9x in 20.1 ms. Level 6 rather than the library default because compression runs **on the event loop**, so those milliseconds are time no other request can use - the same resource `D82` exists to protect - and the extra ratio above 6 depends on how repetitive the data happens to be while the extra cost does not. Edge compression was rejected for the same reason as edge metering in `D78`: there is no edge in front of this API. The placement is not incidental: inside CORS and the tiers so a response either produces itself is never rewritten, and outside the marker and boundary so a marked or error response is compressed like any other. Both `CORSMiddleware` and `GZipMiddleware` append to `Vary` rather than replacing it, confirmed from the installed library source and then at runtime as `Vary: Accept-Encoding, Origin`. | Compressing a response that mixes a secret with attacker-influenced input is the precondition for BREACH. It does not apply here: these responses carry no CSRF token and no session secret, and the API authenticates with a bearer token rather than a cookie. Worth restating if a future response ever embeds a per-session secret. Measured cost on the default page: +1.1 ms, 0.02%, to remove 98.66% of the bytes. |
| D87 | Leave `Content-Encoding` and `Vary` unexposed to page JavaScript, and send no `Timing-Allow-Origin`. | Add both header names to `Access-Control-Expose-Headers`; add `Timing-Allow-Origin`. | A browser run surfaced that the page cannot read either header, so its own log printed `content-encoding=-` even though the header was on the wire. That is correct and worth recording precisely because it looks like a defect: content encoding is handled by the browser transparently and no client code has any use for it, so exposing it would widen the cross-origin surface for no security purpose. `Timing-Allow-Origin` would unlock in-page size telemetry, which is an observability enhancement rather than a security fix and therefore outside the minimal-change mandate. Confirmed as specification opacity rather than a broken API by a same-origin resource reporting real sizes in the same timing list. | Anyone measuring compression from inside a page will read zeroes and may conclude compression is off. Mitigated by recording here, in `SECURITY.md` and in `F51` that the measurement must be taken at the network layer. |
| D88 | Change no code for the four findings the profiling round raised as accepted or forbidden: the unimportable entry point, `check_revoked=True`, the uncached `get_settings()`, and per-process quota multiplication. | Fix each; fix the cheap ones. | Each was already a recorded decision, and re-verification confirmed each is still exactly what the record says. The entry point still raises `ImportError: cannot import name 'get_db' from 'backend.app.db'`, and the missing names were enumerated rather than assumed: `get_db` and `init_db` in `backend.app.db`, all four schemas in `backend.app.schema`, and all four services in `backend.app.services` - excluded by the change authorisation and tracked as `F27`. `check_revoked=True` costs a measured ~100 ms median per successful authentication, which `R4`, `D58` and residual 9 already record with that cost named. The uncached `get_settings()` adds ~700 ms and is forbidden to change verbatim, and the rollback toggles depend on it being uncached to propagate without a restart. Quota multiplication by worker count is `.env.example`'s documented expectation, measured at exactly 4.00x on four workers against 1.00x on one. | Nothing new. Each risk is already carried in the residual register, and the profiling round's measurements have been folded into those entries so the numbers are no longer only in a report. |

### 3.2 Decisions taken while resolving the final full-stack runtime QA findings

The checkpoint that prompted these drove the running system rather than the sources: nineteen
findings, three of them blockers. All three blockers, and eight of the remaining sixteen, are
pre-existing functional gaps this change set is forbidden to close — they are answered by naming
the exclusion and the follow-up that already tracks each one, in section 5 and in the resolution
record, not by widening the change set. The rows below cover the seven findings that fall inside
it. Two of them are notable for the opposite reason to the usual: they were already anticipated
here as follow-ups F33 and F34, whose own text said to take them "with the first authorized
markup change". A finding raised against the running application is that authorisation.

| ID | Decision | Alternatives considered | Why this choice | Risks it carries |
|----|----------|-------------------------|-----------------|------------------|
| D97 | Require an `exp` claim at the `jwt.decode` call in the `legacy_jwt` path, with `options={"require_exp": True}`. | Reject a claim-less token in `get_current_user` after decoding; leave it, since `create_access_token` always sets `exp`; refuse it in the client. | The runtime check found that `GET /workbooks` answered **200** to a bearer carrying only `{"sub": "1"}`: python-jose validates `exp` when present and requires nothing when absent, so a locally-minted credential with no expiry was admitted for ever — the exact opposite of the lifetime `ACCESS_TOKEN_EXPIRE_MINUTES` exists to govern. Enforcing it as a decode option rather than as a later check means no future caller of that decode can inherit the permissive form, and the failure arrives as the `JWTError` the surrounding clause already maps to the 401, so no new exception path and no new response shape appear. | Nothing the application issues is affected — `create_access_token` always sets `exp` — so the only credential this refuses is one minted outside the application, which already needs `SECRET_KEY`. The `firebase` verifier is unaffected: the Admin SDK requires `exp` itself. A deployment that had been relying on a hand-minted eternal token for a health check or a smoke test would start receiving 401s; there is no such caller in this repository. |
| D98 | State the `worksheet_id` resolution rule at the cells route rather than validating it there. | Coerce or validate the parameter in the route; add an `id` to `WorksheetSchema` now; leave it undocumented on the server side. | The runtime check confirmed the ambiguity is invisible at the transport layer: `1`, `Sheet1` and a unicode worksheet name all answer **200**, because the parameter is annotated `str`. The rule itself is settled — a client only ever has the NAME, since the frozen `WorksheetSchema` declares no identifier — but the code that must obey it is `CellService`, which does not exist and which this change set may not build. Validating in the route would be business logic beyond the authentication dependency; changing the schema would break a frozen contract. Stating the rule where the implementer will stand, and pinning that statement with a test, is the whole of what is available. | The rule is documented, not enforced: whoever writes `CellService` can still read the value as a primary key and every cell write would land on the wrong worksheet or none. That is residual 23 and follow-up F26, unchanged in substance — this decision only moves the statement to the server side of the seam, where the mistake would be made. |
| D99 | Add `'unsafe-inline'` to `style-src-elem` in the `index.html` **meta** policy alone, leaving all three header producers at `'self'`. | Relax `style-src-elem` everywhere; drop `style-src`/`style-src-elem` from the meta policy so styles fall through to the absent `default-src`; add a nonce; leave it and accept an unstyled development build. | A browser enforces **every** policy it receives, so the effective policy is their intersection. In both delivered paths a response header carrying `style-src-elem 'self'` accompanies the document, so production is unchanged and still refuses an injected `<style>`. The development server sends no such header, and there the meta policy is the only one — which is exactly where Create React App's `style-loader` injects the application's own stylesheet as a `<style>` element and had it refused. A meta element cannot carry a nonce or a report-only mode, so no narrower instrument exists for that path. Dropping the directives instead would have admitted stylesheets from any origin in development rather than same-origin and inline ones. | The meta policy is no longer a strict subset of the served policy, which was a property a test asserted and two documents published. Both now state the divergence instead, and a test pins it to **exactly one** directive and **exactly one** extra source, so a second relaxation cannot arrive quietly. If the compiled bundle were ever published somewhere that serves no security headers, the meta policy would be the only one and would admit inline styles there. Both delivery points this repository configures emit the header, and `var.api_origin` is required, so that is a deployment this configuration does not describe. |
| D100 | Reduce the `index.html` comment to a one-line threat marker and move its contract text here. | Keep it; delete it entirely; move it into a build-time-stripped comment. | The comment was ~470 bytes of a 1,866-byte document — a quarter of what every visitor downloads — and it named `infrastructure/terraform/main.tf` and `infrastructure/docker/nginx.conf` to anyone who viewed source. Naming internal layout in a client-delivered artifact is free reconnaissance, and the half explaining *why* the three directives are absent was rationale, which this log is the single source of truth for. The marker that remains states the threat, which is the convention the code carries everywhere else. | The reason `connect-src`, `default-src` and `frame-ancestors` are absent from the meta policy is no longer visible in the file that omits them. Mitigated by keeping it asserted: `TestStaticDeliveryPolicies` fails if any of the three appears there, so a well-meaning addition is refused by a test rather than by a comment nobody re-reads. The full reasoning is D9 and R35. |
| D101 | Delete the hardcoded `<script src="%PUBLIC_URL%/bundle.js">` element. | Keep it; point it at the real emitted bundle; leave it and document the console error. | Create React App injects its own hashed script tags into this template, so the element requested a path the build never emits. Every load fetched it, received the SPA document through the history fallback (`decodedBodySize` 1,866 against the real bundle's 1,701,660) and parsed HTML as JavaScript, raising a `SyntaxError` on every page view. That is noise in the one signal a reviewer uses to judge whether the policy is working — a console with a permanent error in it trains everyone to ignore it — and pointing the element at a hashed filename cannot be done from a static template. | If this application were ever built by something other than react-scripts, no script would load at all. Every build path in the repository is react-scripts (`Dockerfile.frontend` and `scripts/deploy.sh` both invoke `npm run build`), and both are asserted to do so. |
| D102 | Fail the frontend container's start when `CSP_CONNECT_SRC_API` is empty or does not begin with a space, using a guard the Dockerfile **writes into the image** rather than a file copied from the repository. | Validate in the nginx template; validate at deploy time only; copy a `.sh` from the repository; leave the empty default with its comment. | Terraform rejects an empty `api_origin` at `plan`, and rejects one equal to the edge domain — the container path had no equivalent, so an image run without the override served a policy admitting no API origin and a browser enforcing it blocked every API call. nginx cannot express the check, and a deploy-time check does not cover an image run by hand. The guard is generated inside the image because this working tree is CRLF: a `.sh` copied from it would reach Alpine with `\r` line endings and fail on its own shebang. `05-` sorts before the image's own `10-`, `15-` and `20-` scripts, so it runs before `envsubst` renders the template, and the image entrypoint runs `/docker-entrypoint.d/*.sh` under `set -e`, so a non-zero exit aborts start before nginx binds a port. | The leading-space requirement is enforced as well as non-emptiness, which is one check more than the finding asked for; the value is concatenated directly onto the `connect-src` list, so a missing space produces a fused, silently invalid source and is the same class of quiet misconfiguration. A container that previously started with the empty default now refuses to start — that is the point, but an operator who had been serving a broken policy will see it as a new failure. The message names the variable, the consequence and the form of the value. |
| D103 | Render request failures beside the content on both pages, and clear the signed-in identity when the API refuses the credential. | Leave `Dashboard.tsx` logging to the console; keep `Workbook.tsx`'s early return; navigate to a sign-in route; read the response body's `detail[]` for field-level text. | F33 and F34 were already recorded here, and F33's own text said to take it "with the first authorized markup change". `Workbook.tsx`'s error state was rendered by an **early return**, so one failed cell write replaced the entire workbook view — grid included — with a bare `Error:` div, no retry and no indication of which cell failed. `Dashboard.tsx` wrote only to `console.error`, so an expired credential was indistinguishable from an empty account. Both are single-component changes over a classification `api.ts` already computes. There is no sign-in route to navigate to, so clearing the identity and saying so is the honest equivalent. `detail[]` is deliberately **not** read: D71 derives the user-facing message from the status alone, and echoing server-supplied text into the page would reopen exactly that. | Adds markup to two pages, which DEV-10 recorded as untouched; that entry is corrected in the same change. The messages are the client's own classified strings, so a 422 field error still reads as a generic failure — the `detail[]` array is not surfaced. The pages remain unbuildable for the reasons Issue 1 records, so this behaviour is asserted by a render harness and by the type checker rather than by a browser. |



### 3.2 Decisions taken while resolving the QA testing findings

The QA checkpoint exercised the running application rather than reading it, and that is what
separates these findings from the ones above: two of them are cases where the code did exactly what
its own comments said it did, and the comment was wrong about the environment it runs in. D104 is the
clearest example — the resolver faithfully metered the peer the server reported, and the server had
already replaced that peer with a value from a request header. Several rows therefore record a
**test that reproduces the mechanism** rather than one that asserts the fix's shape, because a claim
about how the server behaves is only worth what the server can be made to demonstrate.

| ID | Decision | Alternatives considered | Why this choice | Risks it carries |
|----|----------|-------------------------|-----------------|------------------|
| D104 | QA F-G: meter the **transport** peer, and when the reported peer may have come from a forwarded header meter every such request against one fixed shared bucket. Detect that case by the port the rewriting layer reports — zero, which an accepted connection never carries — or by the reported host appearing in the forwarded chain. Validate a genuine peer as an IP address before it becomes a bucket name. Supersedes the claim D6 recorded, that metering the peer is unforgeable. | Validate the value's shape only, leaving per-address buckets; trust `X-Forwarded-For` behind a configurable trusted-proxy list; hash the forwarded value into a bounded number of buckets; treat the mere presence of the header as disqualifying, whatever the port; require `--no-proxy-headers` in the launch command and assert it at start-up. | The resolver was correct about its own input and wrong about where that input comes from: uvicorn enables `proxy_headers` **by default** and rewrites `scope["client"]` from `X-Forwarded-For` whenever the real peer is loopback, which an in-pod sidecar, a compose network and a local run all are. Measured, eight requests each naming a different address drew **no `429` at all** — the control was absent, not weakened. Shape validation alone leaves that rotation working. A trusted-proxy list was withdrawn once already as beyond the authorised configuration surface, and the fields are asserted absent. Hashing still multiplies the quota by the number of buckets. Disqualifying on the header alone would collapse the Google load-balancer topology too, where the peer is genuine because it is not loopback, and it would over-throttle a deployment that is not affected. The launch command lives in `infrastructure/docker/Dockerfile.backend`, which the change authorisation excludes — and a control that depends on a flag nobody passes is the position this finding already found us in. | While the peer is being rewritten, every client is metered as one, so the ceiling becomes an aggregate. That is the residual a proxy has always carried here and what the generous defaults are sized for, but it is now reached by *detection* rather than by an operator's choice, so a sidecar deployment meets it without asking. The remedy is to preserve the transport peer, not to widen the trusted set. The two signals are also heuristics about a server's behaviour rather than a protocol guarantee: a server that rewrote the peer while inventing a plausible port and was not named in the chain would be trusted, which is why the value is validated as an address in any case. |
| D105 | QA F-C: configure logging once, at the entry point, in a dedicated `backend/app/core/logging_config.py` — level, ISO timestamp and logger name on every record, root level `INFO`, and control characters escaped in the message. | Leave the loggers unconfigured and let the deployment's log pipeline infer severity; call `logging.basicConfig` inline in `main.py`; extend uvicorn's `LOGGING_CONFIG` instead; add a `log_level` field to `Settings`; escape the entire formatted record including exception text. | Every security control here records what it refused, and with no configuration those records reached `logging.lastResort`: the bare message, no level, no timestamp, no logger name — and nothing below `WARNING` emitted at all, which is why the record naming the throttling window store had never once appeared. A pipeline cannot infer a severity that was never written. A dedicated module rather than an inline call keeps the entry point to wiring and makes the behaviour testable directly. Extending uvicorn's configuration would tie the application's records to one launch command and would not cover a process started any other way. A `Settings` field is refused on its own terms below. Escaping the exception text as well would flatten every traceback to one line, and that text is generated by the interpreter rather than supplied by a caller. | The level is fixed at `INFO` unless a caller passes another, so changing it in a deployment means changing the call rather than an environment value — accepted deliberately, since the alternative is a configuration field the authorised surface does not have. Escaping is applied to the formatted message, so a record whose *arguments* are rendered by another handler's formatter is covered only through the filter installed on the server's loggers; a future logger that owns its handlers and does not propagate would need adding to that list. |
| D106 | QA F-B: record `check_revoked=True` as an explicit **deviation** from the specified default verification, and keep it non-configurable, rather than exposing it as a setting. | Expose a `firebase_check_revoked` field defaulting to the specified behaviour, which is what the finding's suggested fix offers first; expose it defaulting to `True`; drop revocation checking to return to the specification; leave the existing rows and treat the finding as already answered. | The finding is fair and it has two halves. The undocumented half is real: R4, D58 and residual 9 all record the *decision* to check revocation, and none of them says it departs from a plan that specifies default verification and names revocation checking "a tunable rather than adopting it" — so a reader reconciling the code against the plan finds an unexplained difference, which the explainability rule treats as a defect. This row is that record. The configurable half cannot be delivered: the authorised configuration surface is exactly the six original fields plus eleven named ones, an earlier round **withdrew** three fields for exceeding it, and three tests assert the surface as an equality against `.env.example` — so a twelfth field is not a smaller change than this row, it is an unauthorised one. Dropping the check to match the plan literally would reopen the replay window M-4 raised, invalidate the `roles/firebaseauth.viewer` grant and the deployment preflight that exists for it, and contradict what `SECURITY.md` now tells operators. Where the plan and a later security finding disagree about strength, the delivered behaviour stays with the finding and the difference gets written down. | The cost stays: one Identity Toolkit lookup on every authenticated request, and a hard dependency on a read-only IAM role whose absence refuses every *valid* credential. Both are recorded as residuals and asserted by tests binding the call to the grant. An operator who needs the latency back has no switch — they must change the call and accept the replay window, which is the trade-off this row exists to make legible. |
| D107 | QA F-A: add `UserNotFoundError` to the caller-fault set that maps to `401` with a `WARNING` naming only the exception type. | Leave it in the provider clause; catch it separately and log at `INFO`; suppress the traceback for every provider fault. | A token whose Firebase account has since been deleted is an ordinary stale credential, indistinguishable in kind from an expired or revoked one — the caller's problem, not the deployment's. Because `UserNotFoundError` inherits from `NotFoundError` and so from `FirebaseError`, it reached the provider clause instead, which logs at `ERROR` **with `exc_info=True`**: an alertable error for a routine rejection, and a traceback that printed the account's Firebase UID into the log. Adding it to the caller-fault tuple restores the contract the module already documents. A separate `INFO` clause would hide a case worth counting, and suppressing tracebacks for genuine provider faults would remove the context that makes an outage diagnosable. | The record no longer carries the UID, so correlating a rejection to an account needs the provider's own logs. That is the intended trade: the account identifier was reaching the log only through an exception path that should not have been taken. |
| D108 | QA F-E and F-D: document that a Firestore denial is carried in the response payload and returns HTTP 200, and that the emulator's "evaluation error" line on a deny path is its own verbose formatting rather than a rules defect. | Say nothing, since the committed harness already checks the rules correctly; add a status assertion to the harness; investigate the emulator message as a defect. | Both are traps for the next person to verify these rules rather than defects in them. A check that asserts on HTTP status would report a confident **PASS** against wide-open rules, because every denial — owner, stranger, anonymous, missing `ownerUid` — comes back `200` with the refusal inside the payload. QA also formed and then disproved the hypothesis that the emulator message indicated a rules bug; recording that disproof is what stops it being investigated a third time. Neither costs a code change, and the existing harness is already correct. | Documentation drifts. Mitigated by the harness remaining the referenced procedure in both documents, so a reader is pointed at an executable check rather than at prose. |
| D109 | QA I-9 and I-2: apply D100's rule to the two comments in `frontend/public/index.html` that still broke it — drop the `D9, D99 and D100` citation from the policy marker, and reduce the `og:url` comment to a threat marker, moving its contract text here. The absent property must be the absolute canonical URL, which is a per-deployment value the file cannot know: the domain lives in the `domain_name` Terraform variable and Create React App substitutes nothing into this document. It previously carried a placeholder domain this project does not own, which a crawler that trusted it would attribute the page to. With the property absent a crawler falls back to the URL it requested, which is correct for every deployment. | Keep both comments, since neither names a repository path and the existing test therefore passed; delete the markers along with the text; move only the `og:url` half and keep the citation. | D100 reduced this document's comments for a reason that applies to both of these and not only to the ones it happened to catch: every visitor downloads this file, so anything written in it is published. A list of decision identifiers tells a reader which internal record to ask for and how that record is organised, and a Terraform variable name tells them how the deployment is parameterised — both are free reconnaissance in a client-delivered artifact, and neither is a threat statement. The rationale half is rationale, which this log is the single source of truth for. The two markers that remain state the threat, which is the convention the code carries everywhere else. The check that was meant to enforce this asserted five repository-path fragments and nothing else, which is why two instances survived it; it now also forbids a decision identifier and the Terraform variable names. | The reason `og:url` is absent is no longer readable from the file it is absent in. Mitigated by this entry, by follow-up `F42` which tracks giving the property a real value, and by the extended assertion, which fails with the rule rather than leaving a future author to rediscover it. |




---

## 4. Accepted residual risks

Recorded so that a reader does not mistake this remediation for a complete security posture.

Numbering note: these IDs are stable references, not a sequence — 8, 10 and 32 are absent because
those residuals were closed and their numbers were not reused, so an existing citation never
silently points at a different risk. The same applies to `F10`, `F18`, `F7` and `F37` in section 5.

`32` closed in the runtime-profiling round: the pagination parameters on `GET /workbooks` are now
bounded (see `D84`), so the unhandled `500` it described cannot occur. `F37`, which tracked it, and
`F7`, which tracked the exception-text disclosure closed by `D85`, are both retired for the same
reason. The tests that pinned those two gaps open have been retired with them — each carried
instructions to remove itself and its tracking entries the moment the gap closed, which is what
happened.

Three review rounds appended to this register independently and collided, so the later series was
renumbered. The number each entry kept is in every case the one earlier entries already cite:

| Was | Now | Entry |
|-----|-----|-------|
| `19` (second occurrence) | `23` | the cells route is addressed with the worksheet's *name* |
| `19` (third occurrence) | `24` | throttling meters the socket peer, so one identity behind the load balancer |
| `20` (second occurrence) | merged into `19` | a window-store failure coarsens counting; the same risk, stated twice |
| `21` (second occurrence) | `25` | Node 14 is pinned in four artifacts |

`26` and `27` are new rows for two residuals that were cited by `D44`, `F27`, `F29`, `F30` and the
traceability matrix without ever appearing here — the citations resolved to whichever entry held
the number at the time. `SECURITY.md` keeps its own residual numbering, and a citation of it says
so explicitly.

| # | Residual | Why it is accepted | Where it is tracked |
|---|----------|--------------------|---------------------|
| 1 | An authenticated user can address another user's `workbook_id` — measured as read, write, share, and attribution of a new workbook to somebody else through the body's `owner_id`. | Owner scoping requires the excluded domain service layer (D13). | F2 |
| 2 | Identity rests on a mutable `email` claim rather than the immutable Firebase UID. | No `firebase_uid` column and no migration tooling (D2). | F5 |
| 3 | On a direct connection, `sslmode=require` encrypts but does not authenticate the database server; on GKE the proxy authenticates the instance and the application's own mode is `disable` over loopback (D54). | Stronger modes need CA distribution or a new dependency (D7). | F8, F9 |
| 4 | `roles/iam.serviceAccountTokenCreator` grants more than `signBlob` and cannot be narrowed by IAM conditions, and the account it is bound on is also the runtime identity - so a compromise of the workload is a compromise of the signer. | Signed URLs were the chosen mechanism (D3); the grant is bound on one account with that account as its own member, which is the only form that authorizes a workload signing as itself (R36, superseding R8). Splitting the two identities needs a second address in `Settings`, a second Workload Identity binding and every runtime IAM member re-pointed. | F32 |
| 5 | Dependency advisories whose fix versions require Python 3.10 or newer cannot be cleared, and `ecdsa` PYSEC-2026-1325 has no fix at all. | The Python 3.9 base image is outside the allowed change set (D14, D19). | F1 |
| 6 | Firestore rules deny all client access until an out-of-scope writer populates `ownerUid`. | Fail-closed is the only safe in-scope option (D12). | F4 |
| 7 | The V3 client half is not installable until `firebase` is declared in the frontend manifest. | The manifest is explicitly excluded (R18). | F3 |
| 11 | A response served while throttling is disabled by configuration carries no marker, so a client cannot distinguish it from a throttled one. | The marker middleware it needed is a second public definition in a module authorized to expose one (R21). Narrowed rather than closed: registration now logs a warning naming the consequence and records `app.state.rate_limit_enabled`, so the state is visible to an operator (D45); only the per-response marker is missing. | F19 |
| 12 | ~~Nothing tracked in this repository prevents a populated `.env` being staged: there is no root `.gitignore`.~~ **Closed.** A root `.gitignore` is committed and ignores `.env`, service-account keys, certificates and Terraform state; `.env.example` stays tracked deliberately as the documented template. | Superseded text, retained for the reasoning it records: the ignore file was held to be outside the authorized artifact map, and the hazard was mitigated by documentation instead - `.env.example`, `README.md` and the onboarding guide each gave the `echo ".env" >> .git/info/exclude` step as a required action before real values were entered. That reasoning did not survive the observation that this change set *creates* the hazard by shipping `.env.example` and telling a developer to copy it. | DEV-4, F17 |
| 13 | ~~The bypass marker header depends on middleware registration order and nothing enforces it.~~ **Closed.** `backend/tests/test_security.py::TestMiddlewareRegistrationOrder` now asserts both ends of the stack: `SecurityHeadersMiddleware` at index 0 of `app.user_middleware`, which is outermost, and `AuthEnforcementBypassMarkerMiddleware` last, which is innermost. A reordering that would silently drop either the header coverage or the bypass audit marker now fails a test. | Was accepted because the assertion needed a test module that did not exist; that module now exists, so it was closed rather than carried. | Closed — see F20 |
| 14 | ~~A Firebase account that never proved control of an email address can authenticate as the local user holding that address.~~ **Closed.** `email_verified` must be exactly the boolean `True` before the `email` claim selects a `User` row — see R44, which supersedes R1. What replaces it is an *operator* obligation rather than a risk: a user whose Firebase address is unconfirmed is now refused after a successful sign-in, so either the sign-in flow must require verification (F21) or account creation in the Firebase project must be restricted. | Superseded text, retained for the reasoning it records: the address-verification gate that closed this locked out every user of the preserved sign-in flow, which does not verify addresses, and the specified bridge requires no verification state (R1). | F5, F21 |
| 9 | Revocation checking costs a user-record lookup per request. | Accepted as the price of closing the replay window (R4). | — |
| 15 | The user a request resolves to is detached from any Session, so an attribute that would need a further query raises rather than lazy-loading. | Releasing the Session deterministically means closing it before the user crosses back to the request context (R22). `User` declares exactly one relationship, `workbooks`, which DEV-6 reinstated so the mappers configure at all, and `TestOrmSeam::test_the_owner_relationship_pair_is_complete` asserts that it is present and paired with `Workbook.owner`. That relationship is the one attribute on a resolved user that would need a further query, and reading it after the lookup's Session is closed raises. The residual is bounded rather than theoretical: `TestAuthenticatedIdentityResolution::test_the_resolved_user_is_readable_after_its_session_closed` asserts the instance comes back detached with its loaded columns readable, and no handler reads `current_user` at all (D13), so nothing in the delivered application reaches the relationship. **Corrected twice.** An earlier version said `User` declares one relationship and a later one said it declares **none**, on the grounds that DEV-6 had been withdrawn and `backend/app/db/models.py` was byte-identical to baseline; it also cited, as the proof of that claim, a test in `TestKnownResiduals` asserting that `User` "has no property 'workbooks'". All three claims were false against the tree: DEV-6 is reinstated, `git diff 45a6b7b HEAD -- backend/app/db/models.py` reports six added lines carrying `workbooks = relationship("Workbook", back_populates="owner")`, and no test of the cited name has ever existed in the suite — so a reader was told a verification existed for a residual that had in fact been closed. The citation is described here rather than reproduced, because `TestDocumentedFactsMatchTheCode::test_every_documented_test_reference_resolves` now resolves every `Class::test_name` this document names against the suite's own syntax tree: a citation to a test that is absent, renamed or moved fails a test, and that guard is worth more than quoting the name that broke it. | R22 |
| 16 | A worksheet whose `cells` are populated ORM rows cannot be serialised by `WorksheetSchema.from_orm`, so `GET /workbooks/{id}/worksheets` answers `500` for any worksheet holding at least one cell. A worksheet with no cells coerces (`[]` reads as `{}`) and answers `200`, and a workbook with no worksheets answers the documented `404` - so the failure surfaces on exactly the case every real workbook is. | The schema declares a map keyed by cell reference while the ORM stores a list of row/column rows, and the projection between them belongs to the absent `WorksheetService` (DEV-7). Both files a fix could touch, `backend/app/schema/workbook_schema.py` and `backend/app/db/models.py`, are excluded by the change authorisation, and choosing the key format would be product design rather than a security fix: no coordinate format exists anywhere in the repository to project onto, and `CellSchema.value` and `style` are non-optional against nullable columns, so a projection would have to invent null handling too. | F28 |
| 17 | The generated specification documents no 401 on any operation, and the `tokenUrl` in the security scheme names a route that does not exist, so the documentation UI cannot obtain a credential. | Declaring the response means editing route decorators, which may only receive the authentication dependency; the absent token route is D21 on its own merits (R31). | D21 |
| 19 | While the shared window store is unreachable, counting degrades from fleet-wide to per-process, so a client's effective ceiling becomes the configured value times the number of workers. | Enforcement is never dropped and the triggering request is re-counted (R33), so this is a loss of precision rather than of the control. Both alternative directions are worse: fail-open admits everything, and fail-closed turns a window-store outage into a total API outage. The degradation is logged once per tier, but nothing surfaces the per-tier `degraded` flag automatically yet. | R33 |
| 20 | ~~The browser's Firestore document reference is invalid: `collaboration.ts` calls `collection(db, 'workbooks', workbookId)`, which names an even number of path segments.~~ **Closed.** The file builds `doc(db, 'workbooks', workbookId)` (DEV-8), which is the `/workbooks/{workbookId}` path `firestore.rules` matches, so the browser can exercise the deployed rules. `scripts/deploy.sh` Preflight 5 checks the file rather than assuming either state, and passes on the committed tree. Retained as a record because three artifacts published the withdrawn claim after it ceased to be true — this residual, the `deploy.sh` header's blocker list, and D53's rationale. | Nothing open on the reference itself. What remains under F4 is populating `ownerUid` on the documents `backend/app/services/real_time_sync.py` writes: until a writer sets it, the fail-closed rules deny every browser read, which is the correct outcome and not a defect in this file. | F4 |
| 21 | No consent record, retention schedule, erasure path or data export exists. No bucket or table carries an expiry, and there is no way to delete a user's workbooks and uploads on request. | Data-governance controls are categorically outside the allowed change set. The compliance regimes the specification names require all four; nothing here implements any of them, and no code in this repository should be read as a step toward them. | F16 |
| 22 | The frontend dependency audit cannot run at all, so no frontend advisory is visible. | `frontend/package.json` declares neither `firebase` nor `react-scripts` and no lockfile is committed (R18). The CI step asserts exactly that state and becomes a real gate automatically once both are fixed (R53), so the exception expires by itself rather than needing to be remembered. | F3 |
| 23 | The worksheet identifier the cells route is addressed with is the worksheet's **name**. | `WorksheetSchema` is frozen and declares no identifier field, so a response carries no other candidate. Two worksheets sharing a name in one workbook would collide, and renaming one changes the identifier its cell writes use. Closing it means adding a field to a frozen schema. | Follow-up F26; the mapping is stated as a contract in `frontend/src/schema/workbookTypes.ts` and again in R40. |
| 18 | `WWW-Authenticate` is present on every 401 on the wire but is not readable by cross-origin JavaScript. | Adding `Access-Control-Expose-Headers` would widen the CORS contract beyond the two changes V5 requires, and no client reads the challenge header (R31, as corrected). | R31 |
| 24 | Throttling meters the socket peer, so behind the load balancer every client shares one throttling identity and is throttled as one. | The alternative is trusting a forwarded header, which lets any caller that reaches the API directly name the identity its quota is charged to — a control a client can opt out of is not a control. The configured thresholds are sized on the assumption that this is the behaviour. Narrowing it properly means a trusted-proxy list, which is configuration outside the authorized surface (R34). | R34, `.env.example` throttling section |
| 25 | Node 14 is pinned in four artifacts and nothing can be built on it. | `infrastructure/docker/Dockerfile.frontend`, the `build` job matrix in `.github/workflows/ci.yml`, `scripts/setup_dev_environment.sh` and `README.md` all name it; it left support on 30 April 2023 and the `react-scripts` 5 toolchain the sources need does not run there. The CI security job pins Node 22 for itself instead. Moving the four pins is a runtime upgrade rather than a documentation fix, and all four files are outside the authorized change set. | R40, `SECURITY.md` residual 9, onboarding next task 9 |
| 26 | The application cannot start, so every control is evidenced by tests rather than by traffic. `backend/app/main.py` imports an `init_db` that is not defined, the route modules import service classes that do not exist, there is no `__init__.py` under `backend/`, and no DDL or migration tool creates the `users` table the identity lookup queries. | All four are excluded from the authorized change set by name, and every affected module is byte-identical from the pre-remediation baseline through the current commit - this remediation neither caused it nor may close it. `TestKnownResiduals::test_the_application_entry_point_cannot_be_imported` pins the failure so that closing it is visible rather than silent. | DEV-15, F27, F30 |
| 27 | Neither write ceiling is measured. The client coalesces cell writes per worksheet over a 1000 ms window, which caps it at 60 a minute per worksheet, while `rate_limit_write` defaults to `300/minute` shared across every write route and worksheet - so five worksheets edited continuously fill the budget and a sixth is refused. | The two were raised together, from 500 ms and `120/minute`, precisely because at those values a *single* continuously edited worksheet could consume everything (D44, R41). Both remain judgements: setting them from observed cadence needs a running application, which residual 26 blocks. Both are configuration, so neither needs a code change to widen. | R41, F29 |
| 28 | `python-jose` is a thinly maintained component sitting in an authentication path (CWE-1104), and it is the sole reason `ecdsa` is in the dependency tree. | It is pinned at `3.5.0`, above the `CVE-2024-33663` fix floor, so it carries no known unpatched flaw of its own, and it is retained by explicit instruction. The exposure is bounded: after the Firebase bridge landed, the live verification path uses `firebase-admin`, and `python-jose` is reached only by `create_access_token` and the unused `legacy_jwt` verifier. What is *not* bounded is the transitive consequence — `ecdsa` PYSEC-2026-1325 has **no published fix on any runtime**, so it is the one audit-baseline entry the Python upgrade cannot clear. | F36 |
| 29 | ~~The request seam's failure messages are not announced to assistive technology, so a screen-reader user is not told their session ended.~~ **Closed.** Every failure surface on both pages is a `role="alert"` container: `Workbook.tsx`'s blocking load error and its non-destructive save error, and `Dashboard.tsx`'s new one. | Superseded text, retained for the reasoning it records: the attribute was held to require a markup change the change set froze, so the classification was correct and its announcement was not. What lifted the freeze was a finding raised against the running application, which is the authorisation this entry's own follow-up asked for (D103). |
| 30 | ~~`Dashboard.tsx` classifies failures and displays none of them, so an expired credential shows an empty workbook list rather than an instruction to sign in.~~ **Closed.** The classification is rendered in a dismissible `role="alert"` region beside the list, and a refused credential also dispatches `clearUser()`, so an expired session no longer presents itself as a signed-in one with no workbooks. | Superseded text, retained for the reasoning it records: a visible surface needed a new element the markup freeze forbade, so the classification `api.ts` computed was discarded rather than shown. Closed under D103. |
| 31 | One workbook is loaded by fetching the collection — an O(N) read bounded at 100 items — and a workbook past that boundary is unreachable. | No `GET /workbooks/{id}` route exists, and adding one is a new route rather than a security fix. The boundary is now *disclosed* rather than misreported: past a full page the screen says the workbook "was not in the first 100" instead of asserting it does not exist (P10-2), so the limit is visible while the read stays the wrong shape. | F35 |
| 33 | Request bodies are validated permissively: an integer is silently coerced to the declared string type and an unknown field is silently dropped, because no schema in `workbook_schema.py` sets `extra`. | Setting `extra = "forbid"` changes a **request** contract the change set freezes, and changes it in the refusing direction - a body accepted today would begin returning `422`. Bounded: nothing coerced reaches SQL as anything but a bound parameter, so there is no injection consequence; the cost is that a client bug surfaces as wrong data rather than as a refusal. Measured on the real schemas: `id=12345` became `"12345"`, a cell `value=42` became `"42"`, and an unknown field was absent from the response. D93. | F45 |
| 34 | `POST /workbooks` uses one schema as both request body and response model, so a caller must supply an `id`, an `owner_id` and two timestamps for a row that does not exist yet. | Omitting `id` is refused with a `422` naming it, so the requirement is real rather than nominal. Splitting the shapes means a second schema and an optional `id`, both changes to the frozen request contract, and `workbook_schema.py` is excluded by name. The defect is invisible from the route signature, which reads `workbook: WorkbookSchema` - which is why it is written down here. D94. | F46 |
| 35 | Nothing makes a repeated create idempotent: the `workbooks` table declares no unique constraint, no idempotency key exists on the route or in the specification, and an identical body submitted twice produced two rows. | Every remedy is outside the permission - a constraint belongs in `models.py`, a key is a route-signature parameter, a duplicate check is handler business logic. Not a security finding standing alone: the write throttling tier bounds the **rate** at which duplicates can be created, not the effect, and that distinction is the reason this is recorded rather than treated as covered. D95. | F47 |
| 36 | `/openapi.json`, `/docs`, `/redoc` and `/docs/oauth2-redirect` answer `200` with no credential, so the route inventory and its schemas are public. | FastAPI's default, declared by no application router. The document carries schema shape only - searched for the seeded account address, the database password and the signing key, and carrying none - and the responses still carry all six security headers. Closing it means `openapi_url=None` and `docs_url=None` on the application construction, and the entry point is in scope for the CORS, header and throttling wiring only. D96. | F48 |
| 37 | The default `GET /workbooks` page costs 452 SQL statements and roughly 5.4 seconds to build, because each workbook's worksheets and each worksheet's cells are fetched per row rather than eagerly. Compression removed 98.66% of the bytes that page put on the wire (`D86`) but changed none of its query cost, and the concurrency bound (`D82`) protects the pool from the consequence rather than removing it. | The fix is eager loading inside `WorkbookService.get_workbooks`, a class that does not exist and that the change authorisation excludes from this work. Bounded rather than unbounded: `MAX_PAGE_SIZE` (`D84`) caps the page at 100 rows, so the statement count no longer grows with the size of the table - it is now a fixed ceiling rather than an open-ended one. Tracked as `F49`. |
| 32 | The static-assets bucket's direct object path is framable and carries none of the six response headers, so UI redress becomes available on it the moment a compiled bundle ships. | Measured rather than inferred: an unheadered origin serving the same document was framed cross-origin with the body delivered and the console silent, while the header-protected origin was refused with `net::ERR_BLOCKED_BY_RESPONSE` and a `frame-ancestors 'none'` violation, on byte-identical bodies. Cloud Storage serves no security header from object metadata, so only an edge redesign closes it (D5). | F12, SECURITY.md residual 10 |
| 33 | Every visual, responsive, interactive-state and accessibility finding is unmeasurable, because the single-page application does not build and no route renders anything. | The compile fails on modules that were never authored and on prop contracts inside files the change authorisation forbids editing, so no screenshot, breakpoint, focus order or contrast ratio can be observed at all — not merely that they are wrong, but that they are unknown. Reported as such rather than asserted from source reading. | F52 |




---

## 5. Follow-up work this remediation does not perform

Improvements found during the work that fall outside its scope, in security priority order.

| ID | Follow-up | Why it matters | Blocked by |
|----|-----------|----------------|------------|
| F1 | Upgrade the backend runtime off Python 3.9. | Every reported dependency advisory has a fix version requiring Python 3.10 or newer, so no dependency advisory can be closed until this is done. Python 3.9 is also end-of-life and receives no upstream security fixes. | `infrastructure/docker/Dockerfile.backend` is outside the allowed change set. |
| F27 | Complete the backend bootstrap so the application can start: define `init_db`, add the `__init__.py` files the package layout needs, implement `WorkbookService`, `WorksheetService`, `CellService` and `CollaborationService`, correct `Dockerfile.backend`'s `uvicorn main:app` to `backend.app.main:app`, and establish table-creating DDL or a migration tool. | Nothing else on this list can be observed in a running process until it is done. The authentication dependency, the throttling tiers, the response headers and the TLS contract are all delivered and all unexecutable: the entry point raises `ImportError`, and a process that did start would query a table that does not exist. This is residual 26, and it is why no document here claims a running deployment. | Excluded by name at §0.9.2.1, §0.13.4 item 16 and §0.13.5 — a feature to build, not a security fix. |
| F2 | Add owner scoping to the workbook, worksheet, cell and collaboration routes, and take `owner_id` from the verified caller rather than from the request body. | Closes the authenticated cross-tenant read, write, share and attribution spoofing that D13 leaves open. | Requires the excluded domain service layer. |
| F3 | Declare the eight undeclared-but-imported packages in `frontend/package.json` **before** generating `package-lock.json` — declaring them is what makes the lock file correct, and generating it first does not work. | The V3 client half cannot install, build, test or audit until this is done, so the identity bridge is not deployable. Measured while validating this checkpoint: running `npm install` in `frontend/` and then `npm install --no-save` with the eight versions `.github/workflows/ci.yml` pins produces a 202 KB `lockfileVersion` 3 lock file that records **none** of `react-scripts`, `firebase`, `@reduxjs/toolkit`, `react-router-dom`, `mathjs` or `date-fns` — `--no-save` keeps them out of both manifests. Committing that file would be worse than committing nothing: `npm ci` would succeed against it, install 396 packages, and the build would then fail on a missing `react-scripts` binary, which is a later and more confusing failure than today's immediate "no lock file" error. `.gitignore` already anticipates this by declining to ignore the path and requiring that it be "generated correctly"; correctly means after the declarations, not before. | The manifest is explicitly excluded (R18). |
| F4 | Populate `ownerUid` and `collaboratorUids` when workbook documents are written. | The Firestore rules deny all client access until the fields exist (D12). | `backend/app/services/real_time_sync.py` is outside the allowed change set. |
| F5 | Add a `firebase_uid` column and key the identity bridge on the immutable UID. | Removes both the dependence on a mutable email claim (D2) and the ability of an unverified address to select a local account (R1, residual 14). | No migration tooling exists. |
| F6 | Add a collaborators table so sharing can be authorized and persisted. | `POST /workbooks/{id}/share` has no store to write to, and the intended owner/shared-user/admin semantics are asserted against a class that does not exist. | Requires migration tooling. |
| F28 | Project a worksheet's `Cell` rows into the map `WorksheetSchema` declares, and give `Worksheet` the `named_ranges` attribute the schema reads. | `orm_mode` is now present (DEV-7), so an empty worksheet serialises, but a worksheet holding populated cells raises `ValidationError` because the schema declares a map keyed by cell reference while the ORM holds a list of row and column rows. `named_ranges` is absent from the model entirely and passes only because the field is optional. Residual 16. | The projection belongs to the absent `WorksheetService` (F27), and choosing the key format is product design rather than a security fix. |
| F8 | Move the database client to `sslmode=verify-full`. | Authenticates the database server, not just the channel (D7). | Requires CA material distributed to the client and connection by DNS name. |
| F9 | Adopt the Cloud SQL Python Connector. | Automatic encryption plus mutual identity verification regardless of `ssl_mode` (D7). | New dependency and new connection code. |
| F11 | Migrate to Pydantic v2 with `pydantic-settings`. | Returns the project to a supported major version (D16). | Framework change, not a security fix. |
| F12 | Serve the SPA from a private bucket behind Cloud CDN, and re-evaluate the signing mechanism. | Would allow uniform public-access prevention on both buckets (D5) and could remove the `signBlob` grant entirely (D3). | Edge redesign. |
| F13 | Add machine-verified Firestore rules tests to CI. | Rules are currently reviewed by inspection only (D22). | Would add a frontend test dependency. |
| ~~F14~~ | ~~Absorb the corrected operational facts into `README.md` and `documentation/Developer Onboarding.md`.~~ **Done.** Both documents now exist and carry the single deployment path and why a direct storage URL must not be advertised, the runtime-versus-signer split with its must-differ constraint, the shared throttling store, and the frontend build variables. | — | Closed (R20) |
| F15 | Pin artifact hashes in the backend manifest and turn the dependency-audit gate strict. | Pins fix versions but not file contents, and the audit gate is delta-only until the runtime upgrade lands (D19). | F1. |
| ~~F17~~ | ~~Re-introduce a root ignore file as an authorized artifact, or document the clone-local exclude in the onboarding guide.~~ | ~~Nothing tracked prevents a populated `.env`, holding `SECRET_KEY` and the database password, from being staged (DEV-4).~~ | **Done.** The first option was taken: a root `.gitignore` is tracked and authorized in DEV-4 as an artifact beyond the planned thirty. Verified rather than assumed — `git ls-files .gitignore` returns the path and `git check-ignore -v .env` exits 0 reporting `.gitignore:13:.env`. It also ignores service-account keys, Terraform state and `*.tfvars` while deliberately not ignoring `frontend/package-lock.json`, which CI needs to become committable. |
| F19 | Add a response marker to throttling-disabled mode. The warning at registration and the `app.state.rate_limit_enabled` record now exist (D45); a **client** still cannot tell a bypassed response from a throttled one. | An operator can see the state in the log; a caller cannot see it at all (R21, D45). | Needs a second public definition in the throttling module. |
| F29 | Measure real cell-editing and autosave cadence, then set `rate_limit_write` and the client's coalescing window from it. | The two are currently the same number, so one continuously edited worksheet can consume the whole write budget and concurrent editing exceeds it (D44, residual 27). | Needs a running application to measure against — F30. |
| F30 | Obtain an explicit exception to the change authorisation for the four startup blockers, then close them: add package initialisers under `backend/` that export `get_db` from `backend.app.db` and the schema names from `backend.app.schema`; define `CollaboratorSchema`; implement `WorkbookService`, `WorksheetService`, `CellService` and `CollaborationService`; and define `init_db`. Each is specified with its file, line and importing site under [Reaching a running application](./Developer%20Onboarding.md#reaching-a-running-application). | Until then no running application exists, so the onboarding rule cannot be satisfied and every control is evidenced by tests rather than by traffic (DEV-15, residual 26). | Requires a project-level decision; the change authorisation designates all four as functional gaps that must be flagged and not implemented. |
| F31 | Commit `.terraform.lock.hcl` once the constrained provider build is cached, so the exact provider inside `~> 7.0` is fixed. | The constraint bounds the major version; drift within it can still land between a plan and an apply (D40). | A committed lock currently blocks `terraform validate` here, and the file is outside the authorised artifact map (DEV-4). |
| F32 | Split the runtime and signer identities, binding `roles/iam.serviceAccountTokenCreator` on the signer with the runtime as member. | One account currently holds every runtime permission and the signing authority, so a runtime compromise is a signer compromise (D41, residual 4). | Needs a second `Settings` address, a second Workload Identity binding, and re-pointing every runtime IAM grant. |
| ~~F20~~ | ~~Assert the middleware order that the bypass marker depends on.~~ **Done.** `TestMiddlewareRegistrationOrder::test_the_composed_stack_has_the_required_order` asserts the marker is innermost and the header middleware outermost, so a later reordering fails a test instead of silently losing the audit header (D25). | — | Closed — the blocker was the absence of a test module, which DEV-3 removed |
| F21 | Establish address verification at the identity provider — restrict who may create accounts in the Firebase project, or have the sign-in flow require a confirmed address before issuing a usable session. | Until the identity bridge keys on the immutable UID, a self-registered account claiming a known address authenticates as that user (R1, residual 14). Doing it at the provider or in the sign-in flow closes it without the server re-imposing a gate the client cannot satisfy. | `frontend/src/services/auth.ts` is a frozen contract, and project-level identity settings are operator configuration rather than repository content. |
| F22 | Have the cell route return the cells it persisted, and read them on the client. | The client currently returns the cell it sent, so a value the server normalises is not reflected (R23). | The route's list body and message response are frozen contracts, and the route may only receive the authentication dependency. |
| F23 | Make the `@/…` import alias resolve at build time, not only for the type checker — **and, in the same change, supply the six things that resolve to nothing even once it does**. | `frontend/tsconfig.json` now maps `"@/*"`, which satisfies `tsc`, but `react-scripts` honours `baseUrl` alone and ignores `paths`, so every `@/…` import still fails to bundle. The single observable consequence is that **React never mounts**: `#root` stays permanently empty behind an HTTP 200, with `Uncaught Error: Cannot find module '@/app'`. Everything the browser cannot reach depends on this one entry — the whole visual, accessibility and responsive surface is unassessable until it is closed, and so are the two page error surfaces added under D103. | Needs either a webpack alias through a build customisation, or the imports rewritten to relative paths. **The alias alone is not sufficient, and stopping there wastes a cycle.** All of the following must land together, and four of them are authorisation questions rather than coding ones: (1) the alias itself, which `react-scripts` 5.0.1 cannot express without `eject` or a rewiring package, neither of which this change set carries; (2) a new `frontend/src/components/index.ts` barrel — and note that `Dashboard.tsx` imports `WorkbookList` and `RecentActivity`, which **do not exist in any form** and must be written, not merely exported; (3) a new `frontend/src/pages/index.ts` barrel; (4) a new `frontend/src/styles/index.css`, which `app.tsx` imports; (5) `Provider`, `useAppSelector`, `useAppDispatch`, `workbookReducer` and `userReducer` exported from `frontend/src/store/index.ts`, which is **frozen** by the change authorisation covering the Redux store structure — the two slices export defaults, so the named forms every consumer imports resolve to nothing today; (6) an `AuthProvider` in `frontend/src/services/auth.ts`, which `app.tsx` imports and which does not exist, in a file **frozen** as the contractual sign-in front door. Then one interaction to expect rather than discover: the moment the build succeeds, `style-loader` injects the application's own stylesheet as a `<style>` element, which is precisely the case D99 relaxed `style-src-elem` in the meta policy to admit — without that relaxation a repaired build renders unstyled in development while every response-header path stays strict. |
| F24 | Persist user settings. | `frontend/src/pages/Settings.tsx` applies its form to the store only, because no route accepts it, so a change is lost on reload. | Needs a route, a schema and a table, all outside the frozen five-route contract. |
| F25 | Build the screens that use the worksheet-list and share routes. | `GET /workbooks/{id}/worksheets` and `POST /workbooks/{id}/share` are live and authenticated with no caller, so nothing in the application would notice if either regressed. | Feature work: a worksheet-management surface and a sharing dialog. |
| F26 | Give `WorksheetSchema` an identifier field and address the cells route with it. | The route is addressed with the worksheet's **name** because the frozen schema carries no id (residual 23), so names must be unique within a workbook and renaming changes the address. | Changes a frozen response schema and both sides of the cells route. |
| F16 | Container hardening, network segmentation, CMEK and audit logging. | No `.dockerignore`, non-root `USER` or `HEALTHCHECK`; no VPC, subnet, firewall rule or NetworkPolicy is codified; the documented at-rest-encryption and audit-logging requirements are unimplemented. | Outside the allowed change set for the Dockerfiles and Terraform. |
| ~~F33~~ | ~~Announce the request seam's failure messages to assistive technology.~~ **Done.** Every failure container on both pages carries `role="alert"`. | This entry said to take it "with the first authorized markup change", and a QA finding raised against the running application was that authorisation. Closed under D103; residual 29 records the closure. It also cited "residual 17", which was a stale cross-reference — the residual it describes is **29**, renumbered when three review rounds collided. | — |
| ~~F34~~ | ~~Give `Dashboard.tsx` a visible error surface.~~ **Done.** The classification is rendered in a dismissible `role="alert"` region and a refused credential dispatches `clearUser()`. | Closed under D103; residual 30 records the closure. Its "residual 18" citation was stale in the same way — the residual it describes is **30**. | — |
| F37 | Give both page error surfaces a retry affordance, and reach them from a browser. | The surfaces added under D103 can be dismissed but not retried: a caller whose load failed must reload the page, and a caller whose cell write failed must edit the cell again. They are also asserted by a render harness and the type checker rather than by a browser, because the single-page application still does not mount (residual 26, F23). | Wants F23 first — a retry button that nobody can reach is not worth the markup. |
| F35 | Add `GET /workbooks/{id}` and read one workbook by id. | `Workbook.tsx` loads a single workbook by fetching the collection and filtering client-side — an O(N) read bounded at the route's 100-item default. The screen now discloses the boundary rather than misreporting it as "not found", but the read is still the wrong shape and the workbook is unreachable past item 100. | A new route plus its schema; residual 19. |
| F38 | Reconcile the calculation engine with the schema it is handed. | `CalculationEngine.evaluate_formula()` reads `.formula` from the object it receives, while `FormulaSchema` declares the field as `expression`, so every evaluation raises `AttributeError: 'FormulaSchema' object has no attribute 'formula'`. Formula evaluation — the application's central feature — cannot succeed for any input. | The change authorisation excludes the calculation engine outright, and the alternative of renaming the schema field would break a frozen response contract. Whichever side moves, it is a decision this change set may not take. |
| F39 | Implement CSV export, and make the import path reachable. | No CSV export code exists anywhere in the repository, and the import path cannot be executed. Both are named features of the product. | The change authorisation excludes CSV import and export, and the surrounding job module is likewise excluded. |
| F40 | Give `frontend/src/utils/formulaParser.ts` a TypeScript implementation. | It imports a Python module from TypeScript, so it cannot compile or run; two of the 59 standing type errors are its own. | The change authorisation excludes formula parsing. |
| F41 | Bound `skip` and `limit` on the workbook collection route. | The parameters take no bounds, so `limit=-1`, `limit=0`, `limit=2147483647` and a twenty-digit value are all accepted. The route is authenticated, so this is a resource-consumption concern rather than an exposure, and it is bounded by the service layer not existing yet. | Adding `Query(ge=…, le=…)` is validation logic beyond the authentication dependency the route authorisation permits. Best taken with F2, which must revisit the same signature to add owner scoping. |
| F42 | Add the static assets `index.html` references, and give `og:url` a real value. | The favicon, the 192-pixel icon, the web manifest and the Open Graph image are all referenced and none is present, so each load takes four 404s and link previews resolve to a placeholder URL. No security control depends on them; the 404s are noise in the console a reviewer uses to judge whether the policy is working. | The change authorisation scopes `index.html` to security headers, and brand assets are feature additions. Worth taking with F23, since nothing about the page is presentable until then anyway. |
| F43 | Extend the response header set with the cross-origin isolation trio and Trusted Types. | `Cross-Origin-Opener-Policy`, `Cross-Origin-Embedder-Policy`, `Cross-Origin-Resource-Policy` and `require-trusted-types-for` are delivered at no point. The OWASP baseline this remediation followed names the first three; they are absent by scope decision, not by oversight. | The header set is fixed at the Content-Security-Policy plus five named headers by the specification this change set implements, and every one of those six is asserted by name in a test and published in `SECURITY.md`. Widening the set is a specification change, and `require-trusted-types-for` additionally needs application code that has no build (F23). |
| F44 | Build the sign-in screen, and supply the `AuthProvider` the application expects. | `login` and `logout` in `frontend/src/services/auth.ts` have **zero callers**, and `app.tsx` imports an `AuthProvider` that does not exist, so there is no way for a user to obtain the credential the V3 client half attaches — the token interceptor is correct and unreachable. Separately, no Firebase project is provisioned for this application, so even a finished screen has nothing to authenticate against until one exists. | `auth.ts` is frozen as the contractual sign-in front door, the screen itself is a feature addition, and the `AuthProvider` gap is one of the six items F23 enumerates. The project provisioning is an operator action, not a code change. |
| F36 | Decide whether `python-jose` stays in the tree. | It is pinned above the `CVE-2024-33663` floor and carries no known unpatched flaw, but it is thinly maintained (CWE-1104) and is the sole reason `ecdsa` is present — and `ecdsa` PYSEC-2026-1325 has no published fix on any runtime, so it is the one audit-baseline entry the Python upgrade cannot clear. | Retained by explicit instruction, so removal is a decision to take openly. Bounded today: the live verification path uses `firebase-admin`, and `python-jose` is reached only by `create_access_token` and the unused `legacy_jwt` verifier. Replacement with `PyJWT` is roughly six lines; residual 16. |
| F45 | Decide the request-validation posture deliberately: either set `extra = "forbid"` and refuse unknown fields, or state in the API documentation that unknown fields are ignored and that values are coerced to the declared type. | The schemas currently do neither, so a client sending a misspelled field or a wrongly typed value is told nothing and the request succeeds. Measured: `id=12345` was accepted and became `"12345"`; an unknown field was dropped without comment. There is no injection consequence, so this is an interface-clarity fix rather than a hardening one - and it needs the decision recorded because tightening it is a compatibility break. | Changes a frozen request contract; residual 33, D74. |
| F46 | Split the create request from the create response: add a schema omitting `id`, `owner_id` and the timestamps the server assigns, and keep `WorkbookSchema` as the response model. | A caller must presently invent a primary key and two timestamps to create a workbook, and `POST /workbooks` refuses the body with a `422` naming `id` if they do not. The route signature reads `workbook: WorkbookSchema` and reveals none of that. | `backend/app/schema/workbook_schema.py` is excluded by name and the request contract is frozen; residual 34, D75. |
| F47 | Make `POST /workbooks` idempotent: add a unique constraint on `(owner_id, name)`, or accept and honour an idempotency key. | An identical body submitted twice creates two workbooks with different ids, so a retry or a double click silently duplicates. The write throttling tier bounds the rate at which that can happen and nothing bounds the effect. | A constraint needs the excluded model and migration tooling; a key needs a route-signature parameter. Residual 35, D76. |
| F48 | Decide whether the specification and documentation endpoints should be public, and if not set `openapi_url=None` and `docs_url=None` - or gate them - on the application construction. | All four answer `200` unauthenticated today. Nothing sensitive is in the document, so the exposure is the route inventory itself; the decision is worth taking openly rather than inheriting from a framework default. | The entry point is in scope for the CORS, header-middleware and throttling wiring only; residual 36, D77. |
| F49 | Eagerly load a workbook's worksheets and their cells in `WorkbookService.get_workbooks`, so the default page is one query rather than 452. | Residual 37: the default page takes roughly 5.4 seconds to build. The page is now size-capped so the cost is bounded, and compression removed the byte amplification, but the query amplification remains and it holds a pooled connection for the whole of it - which is what made pool exhaustion reachable in the first place. | `WorkbookService` does not exist, and the change authorisation excludes building the domain service layer. |
| F50 | Reconcile the request-concurrency bound with the database pool whenever either is resized, and move both to configuration if operators need to tune them per deployment. | The bound is `DB_POOL_CAPACITY` by construction (`D82`), and a test asserts the two are equal. That invariant is what makes the bound correct; a future change to one that forgets the other would reintroduce the pool-exhaustion `500`s the bound removed. | Nothing blocks it. It is deferred because neither value needs to differ per deployment today, and adding a setting nobody varies is configuration surface for its own sake. |
| F51 | Send `Timing-Allow-Origin` on API responses if in-page size telemetry is ever wanted. | Cross-origin `PerformanceResourceTiming` reports `transferSize`, `encodedBodySize` and `decodedBodySize` as `0` by specification without it, so a browser client cannot measure how much compression saved it. Confirmed as specification opacity rather than a defect: a same-origin resource in the same timing list reported real values. | Nothing blocks it. Declined here as an observability enhancement rather than a security fix, which puts it outside the minimal-change mandate (`D87`). |
| F52 | Author the modules the single-page application needs so the frontend compiles: the seven absent components (`WorkbookList`, `RecentActivity`, `SettingsForm`, `TabGroup`, `Tab`, `FormattingOption`, `Modal`), the `components/index.ts` and `pages/index.ts` barrels, the twelve store exports the screens import, the `react-router` v5-to-v6 migration in `app.tsx`, and the prop and reducer-payload contracts in `Grid.tsx`, `Cell.tsx`, `FormulaBar.tsx` and `Workbook.tsx`. | This is the gate everything visual sits behind: until it passes nothing renders on any route at any viewport, so residual 33 cannot be retired and QA findings 1, 3, 4, 5, 6 and 7 cannot even be measured. | The file transformation map lists no file under `frontend/src/components/`, `frontend/src/pages/` or `frontend/src/store/`, and names neither `index.tsx` nor `app.tsx`; the excluded list separately names the UI components and the Redux store structure as do-not-touch. Needs an explicit exception to the change authorisation, as F30 obtained for the four static screens. |
| F53 | Supply the design source, then author `frontend/src/styles/index.css` from it. | No stylesheet exists anywhere and `document.styleSheets.length` is 0, so there is no typography, spacing, colour or breakpoint to conform to — QA finding 9. | No design reference of any kind is committed, and QA recorded this as a designer-review flag rather than an implementer fix, having deliberately declined to invent values. Inventing them here would fabricate a design contract rather than implement one. Needs the design source as an input, not authorisation. |
| F54 | Add an application-level error boundary and render the request seam's classified messages through it. | `api.ts` already classifies failures into user-facing text and startup preconditions already throw named errors, but nothing catches them, so a thrown precondition is a silent blank page — QA finding 8 and the user-facing half of 27. F34 covers only `Dashboard.tsx`'s own surface, not the app-level case. | Requires authoring a component and wiring it into `app.tsx`, neither of which the file transformation map lists. Needs the same exception as F37 and is best done with it. |
| F55 | Give the document its landmarks, a heading outline, a reachable focus order and a skip link. | Zero landmarks, zero headings and zero focusable elements were measured, ten Tab presses produced no focus event, and Lighthouse aborted with `NO_FCP` so all 155 audits errored — QA findings 21, 22, 23, 24 and 25. | Nothing renders, so there is no structure to annotate; the markup that would carry it lives in the components F37 must author first. Blocked on F37 rather than on authorisation. |
| F56 | Implement CSV import and export. | The capability is described in the requirements and absent from the tree, so a user cannot get data in or out — QA finding 28. | The excluded list names CSV import and export logic as do-not-touch, and the change authorisation categorically excludes feature additions unrelated to security. Needs a product decision, not a security exception. |
| F57 | Instantiate the chart preview that `ChartDialog.handlePreviewChart` leaves as an empty placeholder. | The dialog accepts input and does nothing with it, so charting appears available and silently is not — QA finding 29. | The excluded list names the chart UI components as do-not-touch, and the `addChart` action it would dispatch belongs to the excluded Redux store. Needs a product decision, not a security exception. |
